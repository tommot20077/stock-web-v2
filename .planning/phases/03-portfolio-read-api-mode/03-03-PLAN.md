---
phase: 03-portfolio-read-api-mode
plan: 03
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - ../../vue/stock-v2/vue-app/src/pages/Overview.vue
  - ../../vue/stock-v2/vue-app/src/pages/Overview.test.ts
autonomous: true
requirements:
  - PORT-01
  - PORT-05
must_haves:
  truths:
    - "API mode 的 Overview 只顯示兩張有後端資料的 KPI 卡：總資產 ← totalMarketValue、總報酬 ← roi（D-14）"
    - "API mode 隱藏：今日損益卡、可用現金卡、資產配置 donut（D-14）、資產走勢圖與 range 切換（D-16）——畫面上不出現任何寫死字串或 genSeries 亂數"
    - "API mode 近期交易來自 GET /trades?page=0&size=5，不與交易頁共用狀態（D-09）"
    - "summary 與近期交易兩個區塊各自有 loading/empty/error/retry 狀態，錯誤時顯示錯誤碼 + traceId，retry 只重試該區塊（D-11/D-12、PORT-05）"
    - "mock mode 的 Overview 與現狀完全一致：四張 KPI、donut、走勢圖、近期交易 live 更新全部保留"
    - "Overview.vue 不再 import useMockPortfolioStore（PORT-04、judgment §3）"
  artifacts:
    - path: "../../vue/stock-v2/vue-app/src/pages/Overview.vue"
      provides: "雙模式 Overview：API mode 真實 summary/trades，mock mode 現狀"
      contains: "getRuntimeApiClients"
    - path: "../../vue/stock-v2/vue-app/src/pages/Overview.test.ts"
      provides: "API mode KPI/隱藏清單/四態/近期交易 + mock mode 回歸測試"
  key_links:
    - from: "../../vue/stock-v2/vue-app/src/pages/Overview.vue"
      to: "../../vue/stock-v2/vue-app/src/services/pageApiClients.ts"
      via: "getRuntimeApiClients().portfolio"
      pattern: "getRuntimeApiClients"
    - from: "../../vue/stock-v2/vue-app/src/pages/Overview.vue"
      to: "/api/v1/portfolio/summary"
      via: "portfolio.getSummary()"
      pattern: "getSummary"
    - from: "../../vue/stock-v2/vue-app/src/pages/Overview.vue"
      to: "/api/v1/trades?page=0&size=5"
      via: "portfolio.listTrades({ page: 0, size: 5 })"
      pattern: "listTrades"
---

<objective>
把 `Overview.vue` 從直接 import mock store 改為經 `getRuntimeApiClients().portfolio`；API mode 重建 KPI 區塊資料來源（PORT-01 實為「取代合成假資料」而非接線——四張現有 KPI 卡沒有一張來自真實持倉），依 D-14/D-16 隱藏無後端資料來源的區塊，近期交易改走 API（D-09）；mock mode 行為與現狀完全一致。

Purpose: ROADMAP 成功判準 1（overview 顯示 backend summary 映射資料）與判準 4（四態 + traceId）、判準 5（API mode 不觸 mock store）。

Output: 改寫後的 `Overview.vue` + 新測試檔 `Overview.test.ts`，全量測試與 build 綠。

**API mode 區塊處置清單（D-14/D-16 逐項裁決，executor 逐項打勾，不得中途擴scope）：**

| 區塊 | 處置 | 依據 |
|------|------|------|
| 總資產 KPI 卡 | **真實資料**：`summary.totalMarketValue`；副標顯示 `holdingCount` 檔持倉（真實值取代 '+1.04% yesterday' 假副標） | D-14 鎖定 |
| 總報酬 KPI 卡 | **真實資料**：`summary.roi × 100` 經 fmtPct；副標顯示 `totalPnl` 帶正負號金額（真實值取代 'annualized 18.4%' 假副標） | D-14 鎖定 + D-14 授權 planner 決定其餘欄位落點 |
| 今日損益 KPI 卡 | **隱藏**（寫死 '+$12,481'；需日級快照，deferred todo） | D-14 鎖定 |
| 可用現金 KPI 卡 | **隱藏**（寫死 '$84,210'；需帳戶餘額模型，deferred todo） | D-14 鎖定 |
| 資產配置 donut | **隱藏**（寫死 alloc 陣列；需資產分類，deferred todo） | D-14 鎖定 |
| 資產走勢圖 + range 切換 | **隱藏**（`genSeries(80, 1_000_000, 0.012, 5)` 亂數；需日級歷史） | D-16 鎖定 |
| 近期交易表 | **真實資料**：`listTrades({ page: 0, size: 5 })` | D-09 鎖定 |
| Watchlist 卡 / News 卡 | **維持現狀不動**（非 portfolio 範圍，PORT-06 v2；兩種 mode 都保留） | CONTEXT Phase Boundary |

mock mode：上表全部區塊維持現狀（假資料照舊顯示）。
</objective>

<execution_context>
@C:/Users/user/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/user/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-portfolio-read-api-mode/03-CONTEXT.md
@.planning/phases/03-portfolio-read-api-mode/03-RESEARCH.md
@.planning/phases/03-portfolio-read-api-mode/03-02-SUMMARY.md
@CLAUDE.md

<interfaces>
消費 Plan 02 的 `PortfolioApi`（完整介面見 03-02-SUMMARY.md）：
- `getRuntimeApiClients().portfolio` → `{ mode, getSummary(), listHoldings(), listTrades(params), live? }`
- mock mode：`api.live` 存在 → 近期交易用 `api.live.trades.slice(0, 5)`（保留 reactivity，等同現狀 `portfolio.trades.slice(0,5)`）；KPI/donut/走勢圖沿用現有合成程式碼。
- API mode：`api.live` 為 undefined → `getSummary()` + `listTrades({ page: 0, size: 5 })` 快照 + 區塊內 loading/error/empty 狀態。
- `PortfolioSummaryDto.roi` 是比值（0.0117 = 1.17%），顯示時 ×100 —— 這是格式化縮放，非 D-04 禁止的平行重算。
- TradeDto → 表格欄位：`executedAt` 取日期部分（YYYY-MM-DD）、`symbol`/`type`/`quantity`/`price`、total 欄顯示 `quantity × price`（顯示衍生，與現狀一致）；row :key 用 `TradeDto.id`。
- 測試模式：`vi.stubEnv('VITE_DATA_MODE', 'api')` + `resetRuntimeApiClientsForTests()` + fetch mock（`api-adapter-wiring.test.ts` 與 `testSetup.ts` 既有慣例；Pitfall 7——API mode case 記得 reset，否則 client 快取跨測試污染）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API mode KPI 重建、隱藏清單、近期交易、四態（TDD）</name>
  <files>../../vue/stock-v2/vue-app/src/pages/Overview.vue, ../../vue/stock-v2/vue-app/src/pages/Overview.test.ts</files>
  <read_first>
    - ../../vue/stock-v2/vue-app/src/pages/Overview.vue
    - ../../vue/stock-v2/vue-app/src/services/portfolioApi.ts
    - ../../vue/stock-v2/vue-app/src/services/pageApiClients.ts
    - ../../vue/stock-v2/vue-app/src/api-adapter-wiring.test.ts
    - ../../vue/stock-v2/vue-app/src/testUtils.ts
    - ../../vue/stock-v2/vue-app/src/components/SessionBanner.vue
    - ../../vue/stock-v2/vue-app/src/i18n.ts
  </read_first>
  <behavior>
    - Red test（API mode，fetch mock 回 summary + trades 成功信封）: 恰好出現兩張 KPI 卡；總資產卡顯示 fmtNum 後的 totalMarketValue、副標含 holdingCount；總報酬卡顯示 roi×100 的 fmtPct、副標含帶符號的 totalPnl。
    - Red test（API mode 隱藏清單）: 今日損益/可用現金文案不出現；donut（allocation 標題）不出現；資產走勢圖（assetTrend 標題與 range 按鈕）不出現；'$84,210'、'+$12,481' 等寫死字串不出現在 DOM。
    - Red test（API mode 近期交易，D-09）: fetch 被以含 `page=0` 與 `size=5` 的 URL 呼叫；表格渲染回傳的 5 筆內容；row 數等於 items 數。
    - Red test（四態，D-11/D-12）: fetch pending 時 summary 區塊顯示 loading 文案；summary fetch 回 503 失敗信封（含 error.code 與 meta.traceId）時，KPI 區塊顯示錯誤碼與 traceId 及重試鈕，且近期交易區塊不受影響照常渲染（區塊獨立）；點重試後只重發 summary 請求（fetch 呼叫次數斷言）並在成功後渲染兩張卡；trades 回空 items 時近期交易顯示 noTrades 空狀態。
    - Red test（D-13，全域 banner 邊界）: summary 區塊回 503 業務錯誤時，全域 `SessionBanner`（`data-testid="session-banner"` 或其標題文案）**不出現/不被觸發**——portfolio 讀取錯誤走 inline 區塊,只有 session/認證類錯誤(401/session 過期)才走全域 banner。此斷言確保 portfolio 區塊錯誤不劫持全域錯誤面。
    - Red test（PORT-04）: Overview 模組原始碼不 import `useMockPortfolioStore`（可用 wiring 模式或直接對檔案內容斷言 import 不存在——擇一，與既有測試風格一致）。
    - Red test（watchlist/news 不動）: API mode 下 Watchlist 與 News 卡照常渲染。
  </behavior>
  <action>改寫 `Overview.vue`：移除 `useMockPortfolioStore` import，改 `const api = getRuntimeApiClients().portfolio`。以 `api.live` 存在與否分流：mock 路徑完全保留現有 computed 與模板（kpis 四張、alloc、series、ranges、`api.live.trades.slice(0,5)`）；API 路徑新增 `summaryState` 與 `recentTradesState` 兩組獨立的 ref 狀態機（idle/loading/loaded/error，error 內存 ApiClientError 的 code 與 requestId），onMounted 觸發載入，retry 函式各自只重打自己的請求（D-11）。模板以 `v-if` 依處置清單切換區塊；錯誤區塊樣式參照 SessionBanner 的 code/traceId 呈現慣例（D-12：traceId 只在錯誤狀態出現）。不攔截 session/auth 類錯誤的全域處理——SessionBanner 繼續負責 session 錯誤（D-13），本頁只內嵌呈現自己的區塊錯誤。文案 key 用 Plan 02 新增的 i18n key。onOrder/onNavigate emit 介面與既有按鈕保持不變。測試檔沿用 `testUtils.ts` 的 mountWithPinia/flushAsync 工具與 task4.test.ts 的查詢輔助風格。</action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm test -- src/pages/Overview.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 處置清單八個區塊在測試中逐項有斷言（真實資料三項、隱藏四項、維持現狀一項）。
    - 錯誤狀態斷言同時含錯誤碼與 traceId 字串（D-12）。
    - retry 隔離性有 fetch 呼叫次數證據（D-11）。
    - portfolio 區塊錯誤時全域 `SessionBanner` 不被觸發的斷言存在（D-13，全域 banner 保留給 session/認證類錯誤）。
    - `Overview.vue` 無 `useMockPortfolioStore` import（PORT-04）。
  </acceptance_criteria>
  <done>API mode Overview 只呈現後端有的資料，四態齊備；ROADMAP 成功判準 1 的頁面半部完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: mock mode 回歸鎖定 + 全量 gate（TDD）</name>
  <files>../../vue/stock-v2/vue-app/src/pages/Overview.test.ts</files>
  <read_first>
    - ../../vue/stock-v2/vue-app/src/task4.test.ts
    - ../../vue/stock-v2/vue-app/src/pages/Overview.test.ts
  </read_first>
  <behavior>
    - Red test（mock mode）: 四張 KPI 卡、allocation donut、assetTrend 走勢圖與 range 按鈕全部照常渲染（與 API mode 隱藏清單互為鏡像）。
    - Red test（mock reactivity）: 依 task4.test.ts 既有模式——mount 後將 store `trades` 整個替換為自訂陣列，近期交易表反映新資料（證明 live 委派路徑沒破壞既有 reactivity）。
    - Red test（mock 事件）: 'New trade' 鈕 emit order、'→' 鈕 emit navigate('watchlist')（既有行為鎖定）。
  </behavior>
  <action>在 `Overview.test.ts` 補 mock mode describe 區塊（testSetup 預設即 mock，不需 stubEnv）。之後跑全量 gate：`npm test`（含既有 task4.test.ts 的 Overview case——它直接操作 store 再斷言畫面，是 mock 路徑最強的回歸網）、`VITE_DATA_MODE=api npm test`、`npm run build`。既有測試以不修改為原則；若 task4 的 Overview case 因非同步時序需要調整，只允許加 await/flush 等時序修正、不允許改斷言語意，並在 SUMMARY 記錄。</action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm test && VITE_DATA_MODE=api npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - mock mode 回歸測試綠，task4.test.ts 未改斷言語意。
    - 三道 gate 命令 exit 0。
  </acceptance_criteria>
  <done>Overview 雙模式行為皆有測試證據，mock mode 與 Phase 3 之前完全一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 後端 summary/trades 回應 → Overview 畫面 | 失敗必須可見且可診斷，不得以假資料填補 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Spoofing | API mode KPI 區塊 | mitigate | 隱藏清單測試斷言寫死字串/合成序列不出現——假資料偽裝成真資料是本階段最大的呈現風險（D-14/D-16） |
| T-03-09 | Repudiation | summary/trades 載入失敗 | mitigate | 錯誤狀態顯示 code + traceId（D-12），使用者可回報、可追查 |
| T-03-10 | Information Disclosure | 錯誤狀態內容 | mitigate | 只顯示 code/traceId/通用文案，不渲染後端原始錯誤細節以外的內部資訊 |
| T-03-SC | Tampering | npm 安裝 | accept | 本 plan 零新套件 |
</threat_model>

<verification>
Focused gate:
`cd ../../vue/stock-v2/vue-app && npm test -- src/pages/Overview.test.ts src/task4.test.ts`

Plan gate:
`cd ../../vue/stock-v2/vue-app && npm test && VITE_DATA_MODE=api npm test && npm run build`
</verification>

<success_criteria>
- ROADMAP 成功判準 1：使用者在 API mode 的 Overview 看到 backend summary 映射資料，無資料項隱藏（D-14/D-16）。
- 成功判準 4（本頁範圍）：loading/empty/error/retry + traceId。
- 成功判準 5（本頁範圍）：Overview 不 import mock store。
- 全量測試與 build 綠，mock mode 零行為變化。
</success_criteria>

<output>
完成後建立 `.planning/phases/03-portfolio-read-api-mode/03-03-SUMMARY.md`。
SUMMARY 必須記錄：（1）處置清單逐項落地結果；（2）task4.test.ts 是否有時序性修改（若有，逐條列出）；（3）D-14 其餘 summary 欄位（totalPnl/holdingCount）在本頁的最終落點。
</output>
