---
phase: 03-portfolio-read-api-mode
plan: 04
type: execute
wave: 2
depends_on:
  - 03-02
files_modified:
  - ../../vue/stock-v2/vue-app/src/pages/Positions.vue
  - ../../vue/stock-v2/vue-app/src/pages/Positions.test.ts
autonomous: true
requirements:
  - PORT-02
  - PORT-05
must_haves:
  truths:
    - "API mode 的持倉表由 GET /portfolio/holdings 映射，市值/成本/P&L/ROI 一律讀後端欄位，前端不重算（D-04）"
    - "weight 欄以 marketValue / totalMarketValue 前端衍生（D-04 明文例外），且衍生自後端值而非 qty×price"
    - "每筆持倉顯示 priceTime 行情時間（D-03）"
    - "API mode 彙總條顯示 summary 後端欄位（totalMarketValue/unrealizedPnl/roi/realizedPnl/totalPnl/totalCostBasis），取代 Sharpe/年化/MaxDD 假 KPI（D-14 落點 + D-16）"
    - "API mode 隱藏：Sector breakdown 卡（D-01 更正/D-16）、權益曲線與 range 選擇器、時光機（D-16）"
    - "holdings 與 summary 兩區塊各自 loading/empty/error/retry，錯誤含 code + traceId（D-11/D-12、PORT-05）"
    - "mock mode 的 Positions 與現狀完全一致：sector 卡、時光機、權益曲線、假 KPI、lastFill 高亮全部保留"
    - "Positions.vue 不再 import useMockPortfolioStore（PORT-04、judgment §3）"
  artifacts:
    - path: "../../vue/stock-v2/vue-app/src/pages/Positions.vue"
      provides: "雙模式 Positions：API mode 真實 holdings/summary，mock mode 現狀"
      contains: "getRuntimeApiClients"
    - path: "../../vue/stock-v2/vue-app/src/pages/Positions.test.ts"
      provides: "API mode 映射/衍生/隱藏/四態 + mock 回歸測試"
  key_links:
    - from: "../../vue/stock-v2/vue-app/src/pages/Positions.vue"
      to: "/api/v1/portfolio/holdings"
      via: "portfolio.listHoldings()"
      pattern: "listHoldings"
    - from: "../../vue/stock-v2/vue-app/src/pages/Positions.vue"
      to: "/api/v1/portfolio/summary"
      via: "portfolio.getSummary()"
      pattern: "getSummary"
---

<objective>
把 `Positions.vue` 從直接 import mock store 改為經 `getRuntimeApiClients().portfolio`；API mode 持倉表與彙總條全部改讀後端欄位（D-04 單一真相來源），weight 前端衍生（D-04 例外）、priceTime 顯示（D-03），依 D-01/D-16 隱藏合成區塊；mock mode 行為與現狀完全一致（含時光機與 lastFill 高亮）。

Purpose: ROADMAP 成功判準 2（positions 顯示 backend holdings 映射資料）與判準 4/5。`realizedPnl` 前端根本算不出來（需完整交易歷史），現有 mock 的 client-side 算法本質上不完整——這是 D-04 的核心理由。

Output: 改寫後的 `Positions.vue` + 新測試檔 `Positions.test.ts`，全量測試與 build 綠。

**API mode 區塊處置清單（逐項裁決，executor 逐項打勾，不得中途擴 scope；來源：RESEARCH Pitfall 8 盤點）：**

| 區塊 | 處置 | 依據 |
|------|------|------|
| 持倉表 | **真實資料**：HoldingDto 映射（欄位對應見 interfaces） | PORT-02 核心 |
| 彙總條（現 6 張 stat 卡） | **真實資料**：市值 ← totalMarketValue、未實現 ← unrealizedPnl、ROI ← roi×100、已實現 ← realizedPnl、總損益 ← totalPnl、成本 ← totalCostBasis（六張全來自 summary） | D-14 授權落點 + D-04 |
| Sharpe / 年化 / MaxDD 假 KPI | **隱藏**（寫死 rangeCfg 值），位置由上列真實欄位取代 | D-16 鎖定 |
| 權益曲線圖 + range 選擇器 | **隱藏**（genSeries 合成序列；需日級歷史，deferred todo） | D-16 鎖定 |
| 時光機（按鈕 + scrubber bar + 表格 scrub 效果） | **隱藏**（priceAt 偽隨機） | D-16 鎖定 |
| Sector breakdown 卡 | **隱藏**——D-01 更正：此卡確實存在於 Positions（:118-140），HoldingDto 無 sector，全部持倉會歸 'Other' 等同假資料；後端不加 sector 的結論不變 | D-01 更正 + D-16 |
| Top movers 卡 | **真實推導**：由後端 unrealizedPnl 排序取前 4（衍生自後端值，符合 D-16 例外判準） | D-16 例外 |
| weight 欄 + 表頭排序 | **真實推導**：weight = marketValue / totalMarketValue；client-side 排序保留（holdings 不分頁，排序安全——與 trades 不同，勿套用後端排序） | D-04 例外 + CONTEXT specifics |
| 標題列彙總（$總值 · N 檔） | **真實資料**：totalMarketValue + holdingCount | D-04 |

mock mode：上表全部區塊維持現狀。
</objective>

<execution_context>
@C:/Users/user/.claude/gsd-core/workflows/execute-plan.md
@C:/Users/user/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-portfolio-read-api-mode/03-CONTEXT.md
@.planning/phases/03-portfolio-read-api-mode/03-RESEARCH.md
@.planning/phases/03-portfolio-read-api-mode/03-02-SUMMARY.md
@CLAUDE.md

<interfaces>
HoldingDto → 表格欄位映射（已查證後端 record，2026-07-23）：

| 表格欄 | 來源 | 備註 |
|--------|------|------|
| symbol | `HoldingDto.symbol` | row :key 可用 `assetId` |
| name | `HoldingDto.assetName` | |
| qty | `HoldingDto.totalQuantity` | |
| avgCost | `HoldingDto.avgCost` | |
| price | `HoldingDto.marketPrice` + 副文字 `priceTime`（D-03：行情來自快取可能延遲，顯示時間讓使用者知道新鮮度） | priceTime 格式化為本地日期時間短格式 |
| mktValue | `HoldingDto.marketValue` | **禁止** qty×price 重算（D-04；現狀 :174 的 `qty * effPrice(p)` 在 API mode 不得使用） |
| P&L | `HoldingDto.unrealizedPnl` + `roi × 100` fmtPct | **禁止**前端重算（現狀 :374 的 pnl() 在 API mode 不得使用） |
| weight | 前端算 `marketValue / summary.totalMarketValue × 100` | D-04 明文例外：後端沒有 weight，必須衍生自後端值 |

彙總條映射：現狀 `totalCost = reduce((s,p) => s + p.qty * p.avg)`（:237）與 `totalVal`（:238）在 API mode **不得使用**，改讀 `summary.totalCostBasis` / `summary.totalMarketValue`（D-04 影響條款點名此二處）。

`roi` 是比值（0.0117 = 1.17%），顯示 ×100 是格式化非重算。

client-side 排序：API mode 的 sortedPositions 改以 HoldingDto 欄位為排序值（value → marketValue、pnl → unrealizedPnl、weight → 衍生值），holdings 為完整 List 所以排序正確——這與 trades 的分頁情境不同（CONTEXT specifics 明言勿一併套用後端排序）。

大清單風險（Q5）：holdings 不分頁、筆數無上界。loading 用骨架列呈現；資料到位後一次渲染完整表格（本階段不做虛擬捲動——無證據顯示需要，避免過度設計）；SUMMARY 記錄實測筆數觀察與建議上限。

測試模式：`vi.stubEnv('VITE_DATA_MODE', 'api')` + `resetRuntimeApiClientsForTests()` + fetch mock（Pitfall 7）。
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: API mode 持倉表 + 彙總條 + 隱藏清單（TDD）</name>
  <files>../../vue/stock-v2/vue-app/src/pages/Positions.vue, ../../vue/stock-v2/vue-app/src/pages/Positions.test.ts</files>
  <read_first>
    - ../../vue/stock-v2/vue-app/src/pages/Positions.vue
    - ../../vue/stock-v2/vue-app/src/services/portfolioApi.ts
    - ../../vue/stock-v2/vue-app/src/services/pageApiClients.ts
    - ../../vue/stock-v2/vue-app/src/testUtils.ts
    - ../../vue/stock-v2/vue-app/src/api-adapter-wiring.test.ts
    - ../../vue/stock-v2/vue-app/src/i18n.ts
  </read_first>
  <behavior>
    - Red test（D-04 後端值優先，關鍵 fixture）: fetch mock 回傳一筆 holding，其 `marketValue` 刻意設為與 `totalQuantity × marketPrice` **不相等**的值（例如 qty=10、price=100、marketValue=999）——表格顯示 999 而非 1000，證明讀的是後端欄位不是前端重算。costBasis/unrealizedPnl 同理各設一個不可由前端算出的值並斷言。
    - Red test（weight 衍生，D-04 例外）: 兩筆 holdings（marketValue 750/250）+ summary totalMarketValue=1000 → weight 顯示 75.0% / 25.0%。
    - Red test（D-03）: 每列 price 欄含 priceTime 的格式化輸出。
    - Red test（彙總條）: 六個 stat 值分別等於 summary 的六個後端欄位（roi ×100）；Sharpe/年化/MaxDD 文案不出現。
    - Red test（隱藏清單，D-01/D-16）: sectorBreakdown 標題不出現；assetTrend 權益曲線與 range 按鈕不出現;時光機按鈕與 scrubber 不出現。
    - Red test（Top movers 真實推導）: 依 unrealizedPnl 絕對值排序取前 4 渲染。
    - Red test（排序保留）: 點 P&L 表頭後列順序依 unrealizedPnl 排序切換。
    - Red test（標題彙總）: 標題列顯示 totalMarketValue 與 holdingCount。
    - Red test（PORT-04）: Positions 模組不 import useMockPortfolioStore。
  </behavior>
  <action>改寫 `Positions.vue`：移除 store import，改 `const api = getRuntimeApiClients().portfolio`，以 `api.live` 存在與否分流。mock 路徑：現有全部 computed/模板原樣保留，資料源換成 `api.live.positions` 與 `api.live.lastFill`（等價替換，不動任何計算邏輯——含 rangeCfg、時光機、sectorStats）。API 路徑：`holdingsState` 與 `summaryState` 兩組獨立狀態機；持倉列型別為 HoldingDto，映射與衍生依 interfaces 表；sortedPositions 在 API 路徑改用 HoldingDto 欄位取值;模板依處置清單以 v-if 切換區塊。表格列的 lastFill 高亮（`:161`）在 API mode 無成交事件來源，該 class 綁定在 API 路徑恆為 false（Phase 4 引入 post-trade refetch 時再接）。點列開 OrderTicket 的 emit 介面保持不變。錯誤呈現用 code + traceId + retry（D-12），holdings 失敗不影響 summary 區塊、反之亦然（D-11）。文案用 Plan 02 的 i18n key。</action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm test -- src/pages/Positions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 處置清單九個區塊逐項有測試斷言。
    - 「後端值 ≠ 前端算式」的 fixture 至少覆蓋 marketValue/costBasis/unrealizedPnl 三欄（D-04 的實質證據，不是形式檢查）。
    - weight 由 marketValue/totalMarketValue 衍生有精確數值斷言（D-04 例外落地）。
    - `Positions.vue` 無 `useMockPortfolioStore` import。
  </acceptance_criteria>
  <done>API mode Positions 一律後端真相，衍生欄位有依據；ROADMAP 成功判準 2 的核心完成。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 四態呈現 + mock 回歸鎖定 + 全量 gate（TDD）</name>
  <files>../../vue/stock-v2/vue-app/src/pages/Positions.vue, ../../vue/stock-v2/vue-app/src/pages/Positions.test.ts</files>
  <read_first>
    - ../../vue/stock-v2/vue-app/src/task4.test.ts
    - ../../vue/stock-v2/vue-app/src/pages/Positions.test.ts
    - ../../vue/stock-v2/vue-app/src/components/SessionBanner.vue
  </read_first>
  <behavior>
    - Red test（loading，Q5）: fetch pending 時持倉表區塊顯示骨架/loading 狀態（大清單考量：loading 狀態不依賴資料筆數，先渲染骨架再一次填入）。
    - Red test（empty）: holdings 回空陣列 → noHoldings 空狀態文案，無 NaN/Infinity（對齊既有 task4 空持倉測試的品質要求）。
    - Red test（error/retry 隔離，D-11/D-12）: holdings 回 503 失敗信封 → 表格區塊顯示錯誤碼 + traceId + 重試鈕，彙總條照常顯示（summary 成功時）；點重試只重發 holdings 請求（fetch 次數斷言）。
    - Red test（mock mode 回歸）: sector 卡、時光機按鈕、權益曲線、六張含 Sharpe/年化/MaxDD 的 stat 卡全部照常渲染；表格排序與 weight bar 正常；`executeOrder` 後新持倉列出現且帶 fresh 高亮（lastFill 經 api.live 委派仍有效）。
    - Red test（mock 空持倉）: 既有 task4「renders empty Positions without NaN」情境在新結構下仍成立。
  </behavior>
  <action>補完 API 路徑的 loading/empty/error/retry 模板與狀態機（Task 1 已建立骨架則在此收尾），mock mode describe 區塊鎖定現狀行為。之後跑全量 gate。既有 task4.test.ts 以不修改為原則，僅允許時序性修正並記錄於 SUMMARY。SUMMARY 另記錄 Q5 觀察：mock 資料集的持倉筆數、渲染是否順暢、建議未來觸發分頁討論的筆數量級（CONTEXT specifics 要求）。</action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm test && VITE_DATA_MODE=api npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - 四態各有測試；retry 隔離有 fetch 次數證據。
    - mock mode 回歸測試綠；task4.test.ts 未改斷言語意。
    - 三道 gate 命令 exit 0。
  </acceptance_criteria>
  <done>Positions 雙模式行為皆有測試證據；成功判準 2/4/5 的本頁範圍完成。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 後端 holdings/summary 回應 → Positions 畫面 | 金額/損益數字是使用者決策依據，錯值比缺值更危險 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Tampering | 前端平行重算後端數值 | mitigate | 「後端值 ≠ 前端算式」fixture 測試使重算 regression 必然翻紅（D-04） |
| T-03-12 | Spoofing | API mode 合成區塊（sector/時光機/權益曲線/假 KPI） | mitigate | 隱藏清單逐項測試斷言不出現（D-01/D-16） |
| T-03-13 | Repudiation | holdings/summary 載入失敗 | mitigate | 錯誤狀態顯示 code + traceId（D-12） |
| T-03-14 | Denial of Service | 大量持倉一次渲染 | accept | 本階段後端維持不分頁（CONTEXT 鎖定）；loading 骨架緩解感知；Q5 觀察記錄於 SUMMARY 供未來分頁決策 |
| T-03-SC | Tampering | npm 安裝 | accept | 本 plan 零新套件 |
</threat_model>

<verification>
Focused gate:
`cd ../../vue/stock-v2/vue-app && npm test -- src/pages/Positions.test.ts src/task4.test.ts`

Plan gate:
`cd ../../vue/stock-v2/vue-app && npm test && VITE_DATA_MODE=api npm test && npm run build`
</verification>

<success_criteria>
- ROADMAP 成功判準 2：API mode 的 positions 顯示 backend holdings 映射資料。
- D-04 全面落地：後端有的讀後端、後端沒有的（weight）衍生自後端值，有 fixture 級證據。
- D-01/D-03/D-16 逐項落地。
- 全量測試與 build 綠，mock mode 零行為變化。
</success_criteria>

<output>
完成後建立 `.planning/phases/03-portfolio-read-api-mode/03-04-SUMMARY.md`。
SUMMARY 必須記錄：（1）處置清單逐項落地結果；（2）Q5 大清單觀察（實測筆數、渲染狀況、建議未來分頁的量級門檻）；（3）lastFill 在 API mode 的處置（恆 false，Phase 4 接 post-trade refetch）。
</output>
