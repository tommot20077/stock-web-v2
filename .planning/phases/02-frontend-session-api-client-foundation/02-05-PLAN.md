---
phase: 02-frontend-session-api-client-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 02-04
files_modified:
  - ../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts
  - ../../vue/stock-v2/vue-app/src/services/runtimeDataMode.test.ts
  - ../../vue/stock-v2/vue-app/src/App.vue
  - ../../vue/stock-v2/vue-app/src/api-adapter-wiring.test.ts
  - ../../vue/stock-v2/vue-app/src/App.test.ts
autonomous: true
requirements:
  - FAPI-07
  - FAPI-08
must_haves:
  truths:
    - "Unset runtime mode remains mock for local development."
    - "Explicit runtime modes accept only mock or api."
    - "Explicit invalid VITE_DATA_MODE fails fast and renders an app-level error in the shell."
    - "API-mode backend outage surfaces error/retry behavior and never silently falls back to mock data."
    - "The full frontend test suite and production build pass."
  artifacts:
    - path: "../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts"
      provides: "strict runtime data mode validation"
      exports: ["normalizeRuntimeDataMode", "getRuntimeDataMode"]
    - path: "../../vue/stock-v2/vue-app/src/App.vue"
      provides: "invalid runtime mode shell error surface"
    - path: "../../vue/stock-v2/vue-app/src/api-adapter-wiring.test.ts"
      provides: "API/mock wiring regression coverage"
    - path: "../../vue/stock-v2/vue-app/src/App.test.ts"
      provides: "invalid mode and API outage shell behavior coverage"
  key_links:
    - from: "../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts"
      to: "../../vue/stock-v2/vue-app/src/services/pageApiClients.ts"
      via: "getRuntimeDataMode"
      pattern: "getRuntimeDataMode"
    - from: "../../vue/stock-v2/vue-app/src/App.vue"
      to: "../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts"
      via: "invalid mode error handling"
      pattern: "Invalid|VITE_DATA_MODE|runtime"
---

<objective>
Harden runtime mode behavior and run final frontend verification.

Purpose: D-17 through D-19 and FAPI-07/FAPI-08 require mock/API mode preservation without invalid-mode fallback or API outage masking. This closes Phase 2 by proving the shared transport/session/UI foundation works through focused tests, full Vitest, and build.

Output: Strict runtime mode validation, API-mode failure tests, final frontend suite/build verification, and no backend changes unless a Phase 1 contract mismatch is discovered.
</objective>

<execution_context>
@/home/tom/.codex/get-shit-done/workflows/execute-plan.md
@/home/tom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-frontend-session-api-client-foundation/02-CONTEXT.md
@.planning/phases/02-frontend-session-api-client-foundation/02-RESEARCH.md
@.planning/phases/02-frontend-session-api-client-foundation/02-PATTERNS.md
@.planning/phases/02-frontend-session-api-client-foundation/02-VALIDATION.md
@.planning/phases/02-frontend-session-api-client-foundation/02-UI-SPEC.md
@ai-docs/browser-auth-contract.md
@CLAUDE.md

<interfaces>
From `../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts`:
- `normalizeRuntimeDataMode(value: unknown): RuntimeDataMode`
- `getRuntimeDataMode(): RuntimeDataMode`

Allowed explicit values:
- `mock`
- `api`

Allowed default:
- `undefined`, `null`, or empty string may default to `mock` for local development only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fail fast on invalid explicit runtime mode</name>
  <files>../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts, ../../vue/stock-v2/vue-app/src/services/runtimeDataMode.test.ts, ../../vue/stock-v2/vue-app/src/App.vue, ../../vue/stock-v2/vue-app/src/App.test.ts</files>
  <read_first>
    - ../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts
    - ../../vue/stock-v2/vue-app/src/services/runtimeDataMode.test.ts
    - ../../vue/stock-v2/vue-app/src/services/pageApiClients.ts
    - ../../vue/stock-v2/vue-app/src/App.vue
    - ../../vue/stock-v2/vue-app/src/App.test.ts
    - .planning/phases/02-frontend-session-api-client-foundation/02-PATTERNS.md
    - .planning/phases/02-frontend-session-api-client-foundation/02-CONTEXT.md
    - .planning/phases/02-frontend-session-api-client-foundation/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Red test: `normalizeRuntimeDataMode(undefined)`, `null`, and empty string return `mock`, per D-17.
    - Red test: explicit `mock` returns `mock` and explicit `api` returns `api`.
    - Red test: explicit invalid values such as `local`, `prod`, and `invalid` throw a typed runtime mode error instead of returning `mock`, per D-18/D-19.
    - Red test: App catches invalid mode and renders the UI-SPEC invalid runtime mode copy `資料模式設定無效，請修正 VITE_DATA_MODE。`.
  </behavior>
  <action>Change `normalizeRuntimeDataMode` so only absent/empty values default to `mock`; explicit non-empty values other than `mock` or `api` throw a typed error such as `RuntimeDataModeError`. Update App shell handling so invalid mode becomes a global session/runtime error surface instead of crashing into mock content. Follow the runtimeDataMode/App/App.test analogs mapped in `02-PATTERNS.md`. Keep local-development default mock intact and do not add a new environment classifier unless tests require it.</action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm test -- src/services/runtimeDataMode.test.ts src/App.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert invalid explicit `VITE_DATA_MODE` does not return `mock`.
    - App catches `RuntimeDataModeError` and renders `資料模式設定無效，請修正 VITE_DATA_MODE。` without showing mock content.
    - App-level invalid mode test asserts exact zh copy from UI-SPEC and no mock portfolio/trading content is presented as API data.
    - `getRuntimeDataMode()` still returns `mock` when `VITE_DATA_MODE` is unset for local development.
  </acceptance_criteria>
  <done>Runtime mode guard satisfies D-17 through D-19 and FAPI-08.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove API-mode outage does not fall back to mock</name>
  <files>../../vue/stock-v2/vue-app/src/api-adapter-wiring.test.ts, ../../vue/stock-v2/vue-app/src/App.test.ts</files>
  <read_first>
    - ../../vue/stock-v2/vue-app/src/api-adapter-wiring.test.ts
    - ../../vue/stock-v2/vue-app/src/App.test.ts
    - ../../vue/stock-v2/vue-app/src/services/pageApiClients.ts
    - ../../vue/stock-v2/vue-app/src/services/runtimeDataMode.ts
    - ../../vue/stock-v2/vue-app/src/services/authSession.ts
    - .planning/phases/02-frontend-session-api-client-foundation/02-PATTERNS.md
    - .planning/phases/02-frontend-session-api-client-foundation/02-CONTEXT.md
    - .planning/phases/02-frontend-session-api-client-foundation/02-VALIDATION.md
  </read_first>
  <behavior>
    - Red test: with `VITE_DATA_MODE=api` and rejected fetch/backend outage, App renders backend unavailable copy and retry/sign-in path instead of mock-authenticated or mock portfolio content.
    - Red test: existing API adapter wiring still creates HTTP clients in API mode and mock clients only in mock mode.
    - Red test: API mode service failures preserve `ApiClientError` code/status/requestId where available and do not call mock adapter factories.
  </behavior>
  <action>Extend existing wiring tests to assert API-mode outages remain visible errors and never silently switch to mock data. Use `vi.stubEnv`, `resetRuntimeApiClientsForTests()`, fetch mocks, and App shell test patterns consistent with `02-PATTERNS.md`. Keep this phase frontend-first: do not plan backend changes unless tests uncover a concrete mismatch with `ai-docs/browser-auth-contract.md`; if such a mismatch is found, record it in the plan summary as a contingency blocker rather than modifying backend in this plan.</action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm test -- src/api-adapter-wiring.test.ts src/App.test.ts src/services/runtimeDataMode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - API-mode outage tests assert backend unavailable copy from UI-SPEC.
    - API-mode tests prove mock factories/data are not used when backend fetch rejects or returns auth/session errors.
    - Existing mock-mode adapter tests remain green.
  </acceptance_criteria>
  <done>FAPI-07 is covered for API outage behavior and mock fallback prevention.</done>
</task>

<task type="auto">
  <name>Task 3: Run final Phase 2 frontend verification</name>
  <files>../../vue/stock-v2/vue-app/package.json</files>
  <read_first>
    - ../../vue/stock-v2/vue-app/package.json
    - .planning/phases/02-frontend-session-api-client-foundation/02-VALIDATION.md
    - .planning/phases/02-frontend-session-api-client-foundation/02-CONTEXT.md
  </read_first>
  <action>Run the final frontend gate exactly from the sibling frontend directory. If backend contract mismatch is discovered by frontend tests, stop and document the mismatch with endpoint, expected contract, actual response, and the failed test; do not modify backend unless Yuan explicitly authorizes a contingency backend fix.</action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `npm run build` exits 0.
    - No backend files are modified in Phase 2 unless a documented Phase 1 contract mismatch is approved.
    - Final summary records the focused test commands and full gate output status.
  </acceptance_criteria>
  <done>Phase 2 frontend verification gate passes or a concrete contract mismatch is documented as the only blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build/runtime environment -> Vue app | `VITE_DATA_MODE` controls whether the app uses mock or backend APIs. |
| Backend outage -> user-visible UI | Network/auth failures must not become mock data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Integrity | runtime mode normalization | mitigate | Explicit invalid mode throws instead of silently becoming mock. |
| T-02-17 | Repudiation | API-mode verification | mitigate | Tests prove API failures show error state with diagnostics rather than mock fallback. |
| T-02-18 | Spoofing | shell session UI | mitigate | Backend outage does not present authenticated/mock identity as if API session exists. |
| T-02-19 | Denial of Service | invalid config | accept | Invalid explicit config blocks API-mode app with actionable error because running in mock would hide integration failure. |
| T-02-SC | Tampering | npm installs | accept | No package-manager install is planned. |
</threat_model>

<verification>
Focused gate:
`cd ../../vue/stock-v2/vue-app && npm test -- src/services/runtimeDataMode.test.ts src/api-adapter-wiring.test.ts src/App.test.ts`

Phase gate:
`cd ../../vue/stock-v2/vue-app && npm test && npm run build`
</verification>

<success_criteria>
- FAPI-07/FAPI-08 are covered by tests.
- Invalid explicit runtime mode cannot silently fall back to mock.
- API-mode backend outage is visible and actionable.
- Full frontend tests and production build pass.
</success_criteria>

<source_audit>
GOAL covered: Vue API mode has consistent session state and shared HTTP transport through Plans 01-05.

REQ covered:
- AUTH-03: Plans 03 and 04.
- AUTH-04: Plans 02, 03, and 04.
- FAPI-01: Plan 01.
- FAPI-02: Plan 01.
- FAPI-03: Plan 02.
- FAPI-04: Plan 01.
- FAPI-05: Plan 02.
- FAPI-06: Plan 03.
- FAPI-07: Plans 04 and 05.
- FAPI-08: Plan 05.

RESEARCH covered:
- Shared transport cleanup: Plan 01.
- CSRF bootstrap and unsafe header: Plan 02.
- Single-flight refresh/replay: Plan 02.
- Auth API/session model: Plan 03.
- App shell UI and UI-SPEC focal point: Plan 04.
- Runtime mode hardening and final verification: Plan 05.

CONTEXT decisions covered:
- D-01, D-02, D-03: Plan 03.
- D-04, D-05, D-06, D-07: Plan 02 and Plan 03 callbacks.
- D-08, D-09, D-10: Plan 02 and Plan 04 startup bootstrap.
- D-11, D-12, D-13: Plan 04.
- D-14, D-15, D-16: Plan 04.
- D-17, D-18, D-19: Plan 05.
- D-20, D-21, D-22: Plan 01.

Deferred items excluded: portfolio reads, manual trade creation, broker/order lifecycle, AI/broker policy, cross-repo browser smoke, password reset, email verification, and multi-device session management.
</source_audit>

<output>
Create `.planning/phases/02-frontend-session-api-client-foundation/02-05-SUMMARY.md` when done.
</output>
