---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 09
type: execute
wave: 4
depends_on: ["04-08"]
files_modified:
  - "[FE] src/components/OrderTicket.vue"
  - "[FE] src/components/OrderTicket.test.ts"
  - "[FE] src/task4.test.ts"
autonomous: true
requirements: [TRAD-01, TRAD-02]

must_haves:
  truths:
    - "OrderTicket.vue 不再 import 任何 mock store，一律經 getRuntimeApiClients() 取得 adapter"
    - "送出流程沒有 routing/match 假進度、沒有 Math.random 產生的成交價或訂單編號"
    - "ticket 有手續費欄位（預設 0）與成交時間欄位（預設現在），兩者都有常駐說明文字"
    - "API mode 看不到訂單類型 / TIF / 交易後現金；mock mode 這三樣仍然看得到"
    - "API mode 的任何文案、aria 標籤、測試斷言都不含下單 / 委託 / 撮合 / 已成交 / 有效期語意"
    - "關閉鈕有可讀的 accessible name，每個表單標籤都與其控件程式化關聯"
  artifacts:
    - path: "[FE] src/components/OrderTicket.vue"
      provides: "三步驟 ticket 骨架、D-02/D-03 新欄位、D-04 隱藏、a11y 修補"
      min_lines: 300
    - path: "[FE] src/components/OrderTicket.test.ts"
      provides: "OrderTicket 的專屬測試檔（此前不存在）"
      contains: "OrderTicket.vue?raw"
  key_links:
    - from: "[FE] src/components/OrderTicket.vue"
      to: "[FE] src/services/pageApiClients.ts"
      via: "getRuntimeApiClients()"
      pattern: "getRuntimeApiClients"
    - from: "[FE] src/components/OrderTicket.vue"
      to: "[FE] src/i18n.ts"
      via: "t(lang, key)"
      pattern: "t\\(props.lang, 'recordTrade'\\)"
---

<objective>
重建 `OrderTicket.vue` 的**骨架**：三步驟狀態機、D-02／D-03 的新欄位、D-04 的隱藏、U-16 的假進度與亂數移除、judgment §3 的 mock store 脫鉤、以及 UI-SPEC 的 a11y／字級／間距遷移。

Purpose：查證後確認該檔目前**沒有一項資料是真的** —— symbol 清單來自本地 `data.ts`（`:200, :209`）、走勢圖是 `genSeries()`（`:256-258`）、fee 是 `Math.max(1, estTotal*0.001)`（`:253`）、`cashAfter` 寫死 `124_580`（`:254`）、成交價是 `px * (1 + (Math.random()-0.5)*0.002)`（`:373-374`）、`orderId` 是亂數（`:375`），而且它直接 `import { useMockPortfolioStore }`（`:202`）。四步 wizard 的 `routing/match` 三階段（`:242, :357-367`）是 judgment §1 反例明文點名的東西。

Output：可運作的三步驟 ticket 骨架 + 專屬測試檔 + `task4.test.ts` 的三個 case 遷移。

**範圍界線（重要）**：本 plan **不做** symbol typeahead／報價卡／走勢圖的真實資料接線（04-10），也**不做**送出路徑的 key 生命週期／錯誤分派／SELL 預檢（04-11）。本 plan 讓 ticket 用 `market` adapter 的**既有 mock/API 統一介面**取得標的資料的最小接線即可（04-10 才做七態 typeahead 與 debounce/abort）；送出仍可先呼叫 `trading.createTrade` 的最簡路徑，錯誤處理留到 04-11 完善。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/judgment.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-CONTEXT.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-PATTERNS.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-08-SUMMARY.md
</context>

<repo_and_branch>
sibling 前端 repo `D:\end\workspace\vue\stock-v2`（真專案根 `vue-app/`），分支 `feature/phase-04-manual-trade-creation`。`[FE]` 前綴表示相對於 `vue-app/`。
</repo_and_branch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：建立 OrderTicket.test.ts 並寫下骨架契約的失敗測試</name>
  <files>[FE] src/components/OrderTicket.test.ts</files>
  <read_first>
    - [FE] src/pages/Positions.test.ts（**本專案元件測試的最完整樣板**：`?raw` 原始碼斷言（`import positionsSource from './Positions.vue?raw'`）、「刻意矛盾」fixture（後端值 ≠ 前端算式）、`vi.stubEnv('VITE_DATA_MODE','api')`、`mountWithPinia` / `flushAsync` / `cleanupMounted` 的用法）
    - [FE] src/testUtils.ts（已提供 `mountWithPinia` / `flushAsync` / `clickButton` / `buttonByText` / `rowByText` / `cleanupMounted` —— **不要**自己重寫 mount helper）
    - [FE] src/task4.test.ts（`:13-28` 有一份 legacy 私有 mount helper —— **那是要被淘汰的，不是樣板**）
    - [FE] src/testSetup.ts（預設鎖 mock mode；api 測試各自 `vi.stubEnv`）
    - [FE] src/components/OrderTicket.vue（**全檔**，尤其 `:8` step dots 的 `v-for="(_, i) in 4"`、`:12` 關閉鈕、`:18/53/63/71/75/86` 六個沒有 `for` 的 `<label class="lab">`、`:80` MKT 鎖價、`:86-89` TIF、`:154-163` placing 步塊、`:200-202` 三個 import、`:212/215/221/242` Step 型別與 placeSteps、`:253-254` fee 與 cashAfter、`:344/348/358` 的 placing 守衛、`:357-375` 假進度與亂數、`:517-532` placing CSS）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §Interaction Contract 1 / 4 / 10、§Accessibility Contract、§Test-Visible Contract
  </read_first>
  <behavior>
    新建 `[FE] src/components/OrderTicket.test.ts`（此前**沒有**專屬測試檔；**不得**續加進 `taskN.test.ts`）：
    - Test 1（**judgment §3 最直接的紅燈**）：`import orderTicketSource from './OrderTicket.vue?raw'` → `expect(orderTicketSource).not.toContain('useMockPortfolioStore')` 與 `not.toContain('useMockNotificationsStore')`。
    - Test 2（**U-16**）：`expect(orderTicketSource).not.toContain('Math.random')`。
    - Test 3（**U-16 / D-09**）：`orderTicketSource` 不含 `'placing'`、`placeStage`、`placeSteps`、`routingMatch`、`avgFillPx`、`placeOrder`、`fillPx`、`orderId`。
    - Test 4（**U-01**）：step dots 為 3 顆（`orderTicketSource` 不含 `in 4`；掛載後 `.step-dots` 的子元素數為 3）。
    - Test 5（D-04，API mode）：`vi.stubEnv('VITE_DATA_MODE','api')` 掛載後 `document.body.textContent` **不含** `Cash after`、`MKT`、`LMT`、`DAY`、`GTC`（en locale）。
    - Test 6（D-04，mock mode）：預設 mock mode 掛載後上述四樣**仍然**出現（反向斷言 —— D-04 明文「mock mode 四樣全部保留」）。
    - Test 7（judgment §1）：兩個 mode 下 `document.body.textContent` 皆**不含** `Routing`、`Avg fill`、`Place order`、`Pending`、`Filled`。
    - Test 8（D-02）：兩個 mode 下都有 `[data-testid="ticket-fee"]` 輸入框，初值為 `0`，且畫面上出現 `tradeFeeHint` 的文案。**不得**出現任何 0.1% 估算值。
    - Test 9（D-03）：兩個 mode 下都有 `[data-testid="ticket-executed-at"]`（`type="datetime-local"`），有預設值（非空），有 `max` 屬性，且畫面上出現 `tradeExecutedAtHint` 的文案。
    - Test 10（D-04 連帶效果）：API mode 下價格欄位**可編輯**（不是 `readonly`/`disabled`），且在選定標的後預填該標的的 `latestPrice`。
    - Test 11（a11y，FLAG-E）：`[data-testid="ticket-close"]` 的 `aria-label` 等於 `closeTicket` 的文案（en/zh 各驗一次），且 `✕` 字符節點有 `aria-hidden="true"`。
    - Test 12（a11y）：`ticket-qty` / `ticket-price` / `ticket-fee` / `ticket-executed-at` / `ticket-note` 五個輸入框各有一個 `<label for>` 指向其 `id`（用 `document.querySelector('label[for="trade-qty"]')` 等驗證）。
    - Test 13（三步驟流程）：填完必要欄位 → 點 `ticket-review-advance` → 進入 review（畫面出現 `reviewTrade` 標題與 `tradeIrreversibleNote`）→ 點 `ticket-back-to-edit` → 回到 ticket 且欄位值保留。
  </behavior>
  <action>
    **只寫測試，不改任何實作。** 這 13 條在現況下必然大量紅（沒有 testid、沒有 fee/executedAt 欄位、有 mock store import、有 Math.random、step dots 是 4 顆）。跑一次記錄 RED 輸出。

    測試檔照 `Positions.test.ts` 的形狀：從 `../testUtils` import `mountWithPinia` / `flushAsync` / `cleanupMounted` / `buttonByText`（**不要自寫 mount helper**）；`afterEach(cleanupMounted)`；API mode 的 case 各自 `vi.stubEnv('VITE_DATA_MODE','api')`；需要 adapter stub 時用 `vi.mock('../services/pageApiClients')` 或 `vi.mock('../services/marketApi')`（照 `api-adapter-wiring.test.ts:16-35` 的 hoisted + doUnmock 樣板）。

    `data-testid` 一律用 `04-UI-SPEC.md` §Test-Visible Contract 的 kebab-case 清單，**不得自創名稱**（它是 04-10 / 04-11 / 04-12 共用的驗收接點）。

    **`?raw` 原始碼斷言為什麼重要**：Test 1~3 斷言的是「這段程式碼不存在」，這是行為測試抓不到的（mock mode 下畫面看起來一樣正常）。`Positions.test.ts` 已在 Phase 3 用同一手法鎖 PORT-04。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npx vitest run src/components/OrderTicket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/components/OrderTicket.test.ts` 存在且含 `OrderTicket.vue?raw`
    - 該檔含 13 個 `it(`，且含 `not.toContain('useMockPortfolioStore')`、`not.toContain('Math.random')`
    - 該檔**不含**自訂的 mount helper 定義（`grep -n "function mount" src/components/OrderTicket.test.ts` 無輸出），一律 import `../testUtils`
    - 本 task 完成當下 `npx vitest run src/components/OrderTicket.test.ts` **必須失敗**（RED），且失敗數 ≥ 10
  </acceptance_criteria>
  <done>13 條骨架契約測試就位並全部紅；RED 輸出已記錄。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：重建 OrderTicket.vue 骨架（GREEN）</name>
  <files>[FE] src/components/OrderTicket.vue</files>
  <read_first>
    - [FE] src/components/OrderTicket.vue（全檔）
    - [FE] src/pages/Positions.vue（`:336-368` 四態機 + `describeError` + `BlockError`；`:369-374` `onMounted(() => { if (live) return; ... })` 的 mock/API 分支樣板 —— **分支依據永遠是 `api.live` 是否存在，不是 mode 字串**）
    - [FE] src/services/pageApiClients.ts（04-08 後含 `trading` / `market`）
    - [FE] src/services/marketApi.ts、[FE] src/services/tradingApi.ts（04-06 / 04-07 的介面）
    - [FE] src/i18n.ts（04-08 新增的 42 個 key）
    - [FE] src/components/AuthPanel.vue（`:27` `<h2 tabindex="-1">` 的焦點管理既有做法）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §Spacing Scale（**23 列遷移表 + 自我驗證 grep 指令**）、§Typography（**4 字級 2 字重 + 18 列遷移表 + 自我驗證 grep 指令**）、§Interaction Contract 1 / 4 / 10、§Layout Contract、§Accessibility Contract、§Decisions U-01 / U-02 / U-13 / U-14 / U-15 / U-16 / U-18、§U-16 的實作範圍表（**九列逐行刪除清單**）
  </read_first>
  <behavior>
    Task 1 的 13 條全部轉綠，且 `npm test` / `VITE_DATA_MODE=api npm test` / `npm run build` 皆綠（`task4.test.ts` 的三個 case 由 Task 3 處理）。
  </behavior>
  <action>
    **A. 移除 mock store 直接 import（judgment §3 / Discretion 明文）**
    刪掉 `:200-202` 附近的 `useMockPortfolioStore` 與 `useMockNotificationsStore` import 及其所有用法，改為 `const api = getRuntimeApiClients()`，用 `api.trading` / `api.market` / `api.portfolio`。mock 的通知推送已在 04-07 搬進 `createMockTradingApi()`，元件不再負責。
    分支判斷一律用 `const live = api.trading.live`（存在性），**不是** mode 字串。

    **B. U-16 的九列逐行刪除**（`04-UI-SPEC.md` §U-16 實作範圍表，Yuan 已授權，不需再確認）：
    - `:373` `const slip = (Math.random() - 0.5) * 0.002;` → 刪除
    - `:374` `fillPx.value = ...` → 刪除，連同 `fillPx` ref
    - `:375` `orderId.value = String(Math.floor(Math.random() * 90_000_000) + 10_000_000);` → 刪除，連同 `orderId` ref。mock 的 result 改顯示 mock adapter 回傳的交易識別，**不得**再生成亂數
    - `:358` `if (placing.value || step.value === 'placing') return;` → 改為 `if (submitting.value) return;`
    - `:361-368` `step.value='placing'` + `placeStage` 0→1→2 + `await wait(420/640/380)` → **整段刪除**；送出中停留在 `review`，只切 `submitting`
    - `:344, :348` `onMaskClick` / `onClose` 的 `if (step.value === 'placing') return;` → 守衛條件改為 `if (submitting.value) return;`
    - `:154-163` `<!-- STEP 3: Placing -->` 整塊（spinner + `.placing-steps` + `.p-step` / `.p-mark`）→ **整段刪除**；原 `STEP 4: Filled`（`:165`）遞補為第 3 步
    - `:212, :215, :221, :242` `Step` 型別含 `'placing'`、`stepIdx` 的 4 元陣列、`stepTitle` 的 `case 'placing'`、`placeSteps` computed → 全部移除；`Step` 收斂為 `'ticket' | 'review' | 'result'`
    - `:517-532` `/* Placing */` 到 `@keyframes pulse` 的整段 CSS → 刪除

    **C. 步驟機（U-01，DP-9 已裁定保留 review）**
    三步 `ticket → review → result`，step dots 由 `v-for="(_, i) in 4"`（`:8`）改為 3 顆並整組 `aria-hidden="true"`。三步的標題／內容／唯一 Display 焦點／footer 按鈕依 `04-UI-SPEC.md` §Interaction Contract 1 的表格。`mock mode 也走同一個三步機`（U-16）。
    送出中**不切換步驟**，停在 `review`，只切 `submitting` boolean。

    **D. 表單欄位（D-02 / D-03 / D-04）**依 §Interaction Contract 4 的表格：
    - 手續費：新增 `type="number" inputmode="decimal"` `min="0"`，預設 **`0`**，`data-testid="ticket-fee"`，`id="trade-fee"`，下方常駐 `tradeFeeHint`（`--fg-dim` 12px，**不是**錯誤色）。**刪除** `:253` 的 `Math.max(1, estTotal * 0.001)` 估算公式 —— fee 會被 `HoldingCalculator` 算進 `avg_cost` 與 `realized_pnl`，而 `transactions` 是 append-only，寫錯的費用永久留在帳本裡且改不回來。
    - 成交時間：新增 `type="datetime-local"`，預設現在（本機時區），`max` = 現在，`data-testid="ticket-executed-at"`，`id="trade-executed-at"`，下方常駐 `tradeExecutedAtHint`。送出時轉為帶 offset 的 ISO 字串 —— **重用** `Trades.vue:218-226` 既有的 `toLocalIso(date)` helper，**不得**新造也不得裝 `date-fns`/`dayjs`。
    - 備註：`data-testid="ticket-note"`，`id="trade-note"`，500 字上限；取代原本 `note: tif === 'GTC' ? 'GTC' : ''` 的 hack。
    - D-04 隱藏：訂單類型（MKT/LMT）、TIF（DAY/GTC）、「交易後現金」一律用 **`v-if="live"`**（mock 才渲染），**不是** `disabled` 也不是 `visibility: hidden` —— 隱藏的欄位不得留下空白版位。
    - D-04 連帶效果：MKT 鎖價機制（`:80, :339-341`）移除，價格一律**預填 `AssetDto.latestPrice` 但可編輯**。
    - 前端自檢（Q8.4 建議的 (ii)）：送出前擋掉未來時間，顯示 `tradeErrExecutedAt`。**這是 UX，不是後端驗證的替代品**；後端的 executedAt 未來時間驗證屬 PR #15，**不在 Phase 4 範圍**（DP-1 (c)）。

    **E. a11y（§Accessibility Contract）**
    - 六個 `<label class="lab">`（`:18/53/63/71/75/86`）目前**既沒有 `for` 也沒有包住 input** → 全部改為 `for` + `id`（`trade-symbol` / `trade-qty` / `trade-price` / `trade-fee` / `trade-executed-at` / `trade-note`）。
    - 關閉鈕（`:12`）加 `:aria-label="t(lang, 'closeTicket')"`，`✕` 字符加 `aria-hidden="true"`，觸控目標 44px，可見 focus ring。
    - `✓` 成功圓標、`↗`/`↘` 方向字符、step dots 一律 `aria-hidden="true"`。
    - result 標題用 `<h2 tabindex="-1">` 並在成功後移入焦點（沿用 `AuthPanel.vue:27`）。
    - ticket 開啟時焦點移到 symbol 輸入框 `.inp.big`。
    - `aria-describedby` 指向說明文字節點（`tradeFeeHint` / `tradeExecutedAtHint`）。

    **F. 字級與間距遷移（U-15 / U-18）**
    依 `04-UI-SPEC.md` §Typography 與 §Spacing Scale。**規則優先於表格** —— 執行時必須實跑該兩節提供的自我驗證指令：
    `grep -nE 'font-(size|weight)' src/components/OrderTicket.vue` 與
    `grep -nE '(padding|margin|gap)[^:]*:[^;]*[0-9]+px' src/components/OrderTicket.vue`，
    把表外的違規值一併改掉，**不得**因為「表上沒列」而放過。
    收斂目標：字級只允許 12/13/16/20，字重只允許 400/600；間距只允許 4/8/12/16/24/32/48 與 §Spacing 列舉的 5 類 Exceptions。
    U-18：每一步**恰好一個** Display 級元素 —— step 1 是 `.inp.big`（20px/600），報價卡最新價**降為** Heading 16px。
    `letter-spacing` 一律 `0`（取消 `.lab` / `.qm-l` / 報價卡小標的 uppercase + letter-spacing）。
    交易編號 / `error.code` / traceId 一律 `overflow-wrap: anywhere`，**不得**截斷。

    **G. 最小資料接線（本 plan 的範圍下限）**
    symbol 清單與報價卡改由 `api.market.searchAssets(...)` 提供最基本的結果渲染即可（**七態 typeahead、250ms debounce、AbortController 競態處理留給 04-10**）；送出走 `api.trading.createTrade(request, key)` 的最簡路徑（**key 生命週期、錯誤分派、SELL 預檢留給 04-11**）。本 plan 的驗收只到「不再讀 `data.ts`、不再讀 mock store」。

    **絕對禁止**：
    - 不得保留任何 `import { SYMBOLS, CRYPTO, FX, BONDS } from '../data'`（改由 `marketApi` 的 mock 實作提供）。
    - 不得在 API mode 顯示任何無後端來源的數字（`cashAfter`、估算 fee、隨機成交價）。
    - 不得引入任何新 npm 套件。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npx vitest run src/components/OrderTicket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "useMockPortfolioStore|useMockNotificationsStore|Math\.random|placeStage|placeSteps|routingMatch|fillPx|orderId" src/components/OrderTicket.vue` 為 0
    - `grep -c "from '../data'" src/components/OrderTicket.vue` 為 0
    - `grep -nE 'font-(size|weight)' src/components/OrderTicket.vue` 的每一個值都屬於 {12,13,16,20} ∪ {400,600}
    - `grep -nE '(padding|margin|gap)[^:]*:[^;]*[0-9]+px' src/components/OrderTicket.vue` 的每一個值都是 4 的倍數，或屬於 §Spacing 的 5 類 Exceptions
    - `src/components/OrderTicket.vue` 含 `data-testid="ticket-close"`、`ticket-fee`、`ticket-executed-at`、`ticket-note`、`ticket-review-advance`、`ticket-back-to-edit`、`ticket-submit`
    - 該檔含 `aria-label` 且含 `for="trade-qty"`（label 關聯已建立）
    - `npx vitest run src/components/OrderTicket.test.ts` exit 0（13 條全綠）
  </acceptance_criteria>
  <done>13 條骨架契約全綠、mock store 脫鉤、U-16 九列全部刪除、新欄位與 a11y 就位、字級間距通過自我驗證 grep。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3：遷移 task4.test.ts 依賴假進度的三個 case</name>
  <files>[FE] src/task4.test.ts</files>
  <read_first>
    - [FE] src/task4.test.ts（**三個受影響 case 已由 UI-SPEC 實跑定位**：`:178` `places filled orders through Pinia and adds an unread order notification`（依賴 `:197` `clickButtonByText(…, 'Place order')` 與 `:198` `advanceTimersByTimeAsync(1600)`）、`:375` `records only one order when place order is invoked twice while placing`（`:389` / `:392`）、`:398` `resets side, order type, and TIF from the filled state when starting a new order`（`:415` / `:416` / `:418` `'New order'`）；另注意 `:210` 斷言 `notifications[0].text` 含 `filled`）
    - [FE] src/components/OrderTicket.vue（Task 2 後的新文案與新流程）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §U-16 的「會動到的既有 mock 測試」表與**範圍界線**段落
  </read_first>
  <behavior>
    - Test 1（`:178` 遷移）：按鈕文字改 `Record trade`；移除 `advanceTimersByTimeAsync(1600)`，改為等 promise（`flushAsync`）；測試名去掉 `filled` 語意。斷言仍驗證 Pinia 有記錄交易且有一則未讀通知。
    - Test 2（`:375` 遷移）：同上換按鈕文字與等待方式；斷言改為**驗證 `:disabled` guard**（U-02）而非「placing 期間」。
    - Test 3（`:398` 遷移）：同上；`New order` → `Record another`；測試名去掉 `filled state`。
    - 回歸：`task4.test.ts` 其餘所有 case 仍綠。
  </behavior>
  <action>
    Task 2 完成後這三個 case 會自動變紅（按鈕文字與流程都變了）—— 那是 RED，先跑一次記錄。

    逐一遷移三個 case。遷移的**判準**是「測試意圖是否改變」：
    - 按鈕文字與等待機制的變更 → 純粹的實作細節遷移，直接改。
    - `records only one order when place order is invoked twice while placing` 的**意圖有變**：從「靠 placing 期間按鈕消失」變成「靠明確的 `:disabled` guard」（U-02 / Q6.2：現況 `:185` 完全沒有 `:disabled`，只靠副作用擋連點）。改寫斷言為 `expect(submitBtn.disabled).toBe(true)` + 呼叫次數為 1，並在測試名反映新意圖。
    - 三個測試名都要去掉 `filled` / `place order` 語意（judgment §1 + U-13）。

    **U-16 的範圍界線（不得越界）**：mock store 的通知**文字**（`notifications[0].text` 含 `filled`，`:210` 斷言）屬 D-04 明列「mock 保留通知推送」的範圍，**不在本階段改動**。若想順手改，必須另立決策 —— 本 plan 明確**不改**。

    **不要**把這三個 case 搬到 `OrderTicket.test.ts`。`task4.test.ts` 是既有的 mock mode 行為測試檔，它的存在有其歷史意圖；本 plan 只做最小遷移。新的 Phase 4 契約測試一律寫在 `OrderTicket.test.ts`。

    順帶處理：Task 2 移除了 `OrderTicket.vue` 對 `placeOrder` / `filled` / `avgFillPx` / `routingMatch` / `placing` 五個 i18n key 的引用。本 task 用
    `grep -rn "placeOrder\|avgFillPx\|routingMatch" src/ --include=*.vue --include=*.ts`
    確認是否還有其他元件在用；**完全沒有引用的才可以從 `i18n.ts` 刪除**，有引用的一律保留。`filled` 已知仍被 mock notification 文字使用，**必須保留**。刪除結果寫進 SUMMARY。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npx vitest run src/task4.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/task4.test.ts` **不含** `advanceTimersByTimeAsync(1600)`
    - 該檔**不含** `'Place order'`、`'New order'`（已換成 `'Record trade'` / `'Record another'`）
    - 該檔含對送出鈕 `disabled` 的斷言
    - 該檔 `:210` 附近對 mock 通知文字的斷言**未被改動**（U-16 範圍界線）
    - `npx vitest run src/task4.test.ts` exit 0
    - `npm test` exit 0；`VITE_DATA_MODE=api npm test` exit 0；`npm run build` exit 0
  </acceptance_criteria>
  <done>三個 case 遷移完成且意圖變更已在測試名反映；mock 通知文字未被越界改動；兩個 mode 全套件綠。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 元件 → 資料來源 | 元件若直接 import mock store 或 `data.ts`，API mode 的「真實性」就無法被驗證（judgment §3） |
| 前端顯示的數字 → 使用者的財務決策 | 任何前端發明的數字（估算 fee、隨機成交價、寫死現金）都會被使用者當成事實 |
| fee 輸入 → append-only 帳本 | fee 進 `avg_cost` 與 `realized_pnl`，寫錯永久留存且改不回來 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | `Math.random()` 的殘留 | mitigate | U-16 九列逐行刪除；`?raw` 原始碼斷言 `not.toContain('Math.random')` 讓任何回歸直接紅。**`Math.random()` 不是密碼學隨機** —— 若被用於 idempotency key，碰撞會導致別人的交易被當成你的重試回傳 |
| — | Tampering | 前端發明的 fee 費率進入帳本 | mitigate | 刪除 `Math.max(1, estTotal*0.001)`，fee 預設 **0** 並由使用者手動輸入（D-02）；`tradeFeeHint` 常駐揭露「會計入成本基礎，記錄後無法修改」（U-14） |
| — | Repudiation | 假的 `orderId` / 假成交價 | mitigate | 兩者刪除。result 畫面只渲染後端回傳的 `TradeDto`（04-11 完成），不得出現「平均成交價」等暗示撮合的欄位（D-09 / judgment §1） |
| — | Tampering | API mode 送出未支援欄位 | mitigate | `ordType` / `tif` / `cashAfter` 在 API mode 以 `v-if="live"` **不渲染**；payload 形狀由 04-07 的 `CreateTradeRequest` 型別與 sorted-key 測試鎖住 |
| T-04-09 | Information Disclosure | 元件顯示的診斷資訊 | mitigate | 本 plan 不新增任何錯誤顯示（留給 04-11）。既有顯示不得洩漏 token / cookie / 後端 `error.message`（`04-UI-SPEC.md` §Security & Diagnostics） |
| — | Elevation of Privilege | 元件繞過 adapter 直接讀 mock 資料 | mitigate | `?raw` 斷言禁止 `useMockPortfolioStore` / `useMockNotificationsStore`；`grep -c "from '../data'"` 為 0 |
| T-04-SC | Tampering | npm installs | accept | 零新增依賴。日期轉換重用 `Trades.vue:218-226` 的 `toLocalIso`，**不得**裝 `date-fns`/`dayjs`（§Registry Safety：本階段不引入任何新套件、icon 套件、component block 或 UI registry） |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Vue 元件結構（`src/components/OrderTicket.vue`）**
- `Step` 型別收斂為 `'ticket' | 'review' | 'result'`
- `submitting` ref（取代 `placing` + `step === 'placing'`）
- 新表單欄位：手續費、成交時間、備註

**移除的符號**
- `fillPx`、`orderId`、`placeStage`、`placeSteps` refs／computed
- `'placing'` step 與其整段模板與 CSS（`:154-163`、`:517-532`）
- `useMockPortfolioStore` / `useMockNotificationsStore` 的 import

**`data-testid`（`04-UI-SPEC.md` §Test-Visible Contract）**
`ticket-close`、`ticket-symbol-input`、`ticket-qty`、`ticket-price`、`ticket-fee`、`ticket-executed-at`、`ticket-note`、`ticket-review-advance`、`ticket-submit`、`ticket-back-to-edit`、`ticket-result`

**HTML id（label 關聯）**
`trade-symbol`、`trade-qty`、`trade-price`、`trade-fee`、`trade-executed-at`、`trade-note`

**新檔案**
- `[FE] src/components/OrderTicket.test.ts`
</artifacts_this_phase_produces>

<verification>
- `npx vitest run src/components/OrderTicket.test.ts src/task4.test.ts` 綠
- `npm test` 綠、`VITE_DATA_MODE=api npm test` 綠、`npm run build` 綠
- 兩條自我驗證 grep（字級／間距）的輸出全部合規
- **人工檢視**（UI-SPEC 明列為不可自動化）：三步驟的版面比例、loading 骨架位置、日期選擇器的實際外觀。斷言行為不斷言像素 —— 這幾項在 plan 中誠實標為人工檢視，**不假裝有自動化**。
</verification>

<success_criteria>
- OrderTicket 不再 import 任何 mock store 或 `data.ts`
- U-16 的九列全部刪除，`Math.random` 在該檔零命中
- fee 預設 0 且由使用者輸入；成交時間可選且預設現在；兩者都有常駐說明
- API mode 隱藏訂單類型／TIF／交易後現金；mock mode 保留
- 關閉鈕有 accessible name；六個 label 都與控件關聯
- 字級收斂為 4 級 2 重、間距收斂為 scale 值（兩條 grep 自我驗證通過）
- `task4.test.ts` 三個 case 完成遷移且意圖變更已記錄
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-09-SUMMARY.md`。

SUMMARY 必須記錄：
1. U-16 九列刪除的逐項對照（哪一列刪了、哪一列改了守衛條件）。
2. `task4.test.ts` 三個 case 的**意圖變更**：`records only one order ... while placing` 從「靠 placing 期間按鈕消失」改為「靠明確 `:disabled` guard」，並說明為什麼這是合法的測試遷移而不是「改測試遷就實作」。
3. 五個舊 i18n key 的 grep 結果與刪除／保留決定（`filled` 必須保留）。
4. 字級與間距自我驗證 grep 的輸出，以及是否找到表外的違規值。
5. 誠實列出本 plan **未**完成的部分（typeahead 七態 → 04-10；key 生命週期／錯誤分派／SELL 預檢 → 04-11）。
</output>
