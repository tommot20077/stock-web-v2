---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 12
type: execute
wave: 7
depends_on: ["04-11"]
files_modified:
  - "[FE] src/pages/Overview.vue"
  - "[FE] src/pages/Overview.test.ts"
  - "[FE] src/pages/Positions.vue"
  - "[FE] src/pages/Positions.test.ts"
  - "[FE] src/pages/Trades.vue"
  - "[FE] src/pages/Trades.test.ts"
  - "[FE] src/App.vue"
autonomous: true
requirements: [TRAD-05, TRAD-06]

must_haves:
  truths:
    - "交易成功後，當下有掛載的 portfolio 頁會重新讀取自己的資料並顯示後端真相"
    - "重讀期間畫面保留舊資料並顯示「更新中…」，不會把表格清空成骨架"
    - "重讀失敗時舊資料留在畫面上並明示「可能不是最新」，ticket 的成功畫面完全不受影響"
    - "Trades 頁重讀時保留篩選與排序、頁碼歸零；新交易不在結果集內時明確告知"
    - "剛成交的那一列在 API mode 會有 fresh 高亮，且有非顏色線索的「新」標記"
    - "mock mode 不會因為 revision 變動而發出任何網路請求"
  artifacts:
    - path: "[FE] src/pages/Positions.vue"
      provides: "watch revision + refreshing 旗標 + API mode fresh 高亮"
      contains: "portfolioRevision"
    - path: "[FE] src/pages/Trades.vue"
      provides: "D-11 保留篩選排序、頁碼歸零、不在檢視範圍提示"
      contains: "applyQueryChange"
  key_links:
    - from: "[FE] src/pages/Positions.vue"
      to: "[FE] src/services/portfolioRevision.ts"
      via: "watch(portfolioRevision, ...)"
      pattern: "watch\\(portfolioRevision"
    - from: "[FE] src/pages/Trades.vue"
      to: "applyQueryChange"
      via: "D-11 走既有的單一重置入口"
      pattern: "applyQueryChange"
---

<objective>
落實 D-10 ~ D-13：交易成功後的 portfolio 重讀、部分失敗的分開呈現、Trades 頁的篩選保留與「不在檢視範圍」提示、以及 API mode 的 fresh 高亮。

Purpose：D-12 要防的是最糟的失敗模式 —— 使用者看到整體錯誤 → 以為交易沒成功 → 再送一次。冪等有保護，但他若重開 ticket（換了 key，D-14）或改了數量，就會**真的**建出第二筆。因此「交易成功」與「某個區塊沒更新成功」必須是畫面上兩件分開的事。

D-10 的架構事實：`App.vue:36` 用 `v-if` 切頁，**非當前頁是卸載的**（下次 mount 本來就會重抓）；而 OrderTicket 是全域 overlay，使用者可能在 Markets/Chart 頁下單（三個 portfolio 頁都沒掛載）。所以「一律打三個 domain 請求」在目前架構下是純粹的無效工。

Output：三頁的 `watch(portfolioRevision)` + `refreshing` 旗標、D-11 提示、D-13 fresh 高亮與「新」標記。

**⚠️ 本 plan 包含一個既有測試的明確反轉（DP-10）**：Phase 3 的 `587e84e test(positions): 鎖定 API mode 持倉列不帶 lastFill 高亮` 與 D-13 直接矛盾。這是 judgment §10「改測試遷就實作」的**合法例外** —— D-13 是新的使用者決策，測試意圖本身變了，不是實作遷就。但必須**明確處理**（改寫斷言 + 更新測試名 + SUMMARY 交代），**不可默默刪掉**。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/judgment.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-CONTEXT.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-07-SUMMARY.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-11-SUMMARY.md
</context>

<repo_and_branch>
sibling 前端 repo `D:\end\workspace\vue\stock-v2`（真專案根 `vue-app/`），分支 `feature/phase-04-manual-trade-creation`。`[FE]` 前綴表示相對於 `vue-app/`。
</repo_and_branch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：三頁 watch revision + refreshing 旗標（D-10 / D-12）</name>
  <files>
    [FE] src/pages/Overview.test.ts,
    [FE] src/pages/Positions.test.ts,
    [FE] src/pages/Trades.test.ts,
    [FE] src/pages/Overview.vue,
    [FE] src/pages/Positions.vue,
    [FE] src/pages/Trades.vue
  </files>
  <read_first>
    - [FE] src/pages/Overview.vue（`:232` `loadSummary()`、`:241` `loadRecentTrades()`、`:251` `onMounted(() => { if (live) return; ... })`；`:225-249` 的區塊狀態機）
    - [FE] src/pages/Positions.vue（`:351` `loadSummary()`、`:360` `loadHoldings()`、`:369-374` `onMounted`；**`:323-331` 的區塊狀態機是 `loading | loaded | error`** —— refetch **不得**重用 `loading`；`:336-368` `describeError` 與 computed 投影；`:857-861` 診斷列 CSS；`:868-873` skeleton）
    - [FE] src/pages/Trades.vue（`:255` `loadTrades(allowOverflowFallback = true)`、**`:281-286` `applyQueryChange(mutate)` 是 D-11 的現成單一入口**、`:232-243` `filterParams()`、`:245-247` `queryParams()`、`:179-181` `sortKey`/`sortDir`/`pageNo`）
    - [FE] src/services/portfolioRevision.ts（04-07 的 `portfolioRevision` / `apiLastFill` / `lastCreatedTradeId` / `resetPortfolioRevisionForTests`）
    - [FE] src/App.vue（`:36` 的 `v-if` 切頁、`:52-59` OrderTicket overlay 掛載處）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §Interaction Contract 8（**refetch 的畫面契約表格是權威**）、§Decisions U-05 / U-06、§Test-Visible Contract 的 `{page}-refresh*` testid
    - **三頁目前都沒有任何 `watch(`** —— 這是新增（`grep -n "watch(" src/pages/{Overview,Positions,Trades}.vue` 零命中）
  </read_first>
  <behavior>
    每一頁的測試檔各自 `afterEach(() => resetPortfolioRevisionForTests())`（**不得**加進 `testSetup.ts`）：
    - Test 1（Overview，API mode）：計數 `fetch` 呼叫 → `bumpPortfolioRevision()` + `flushAsync()` 後 `/portfolio/summary` 與 `/trades` 各**多一次**。
    - Test 2（Positions，API mode）：同形 —— `/portfolio/summary` 與 `/portfolio/holdings` 各多一次。
    - Test 3（Trades，API mode）：`/trades` 多一次。
    - Test 4（**Pitfall 12 —— mock mode 零網路**）：三頁各在**預設 mock mode** 下 `vi.stubGlobal('fetch', spy)` → bump 後 `expect(spy).not.toHaveBeenCalled()`。mock mode 走 Pinia reactivity，`executeOrder` 直接改 store，不需要也不應該觸發 refetch。
    - Test 5（**D-10 的核心論證**）：只掛載 Positions（不掛 Overview/Trades）→ bump → 只有 holdings/summary 的請求，**沒有** `/trades`。未掛載的頁不會被觸發。
    - Test 6（**U-05：refetch 不得重用 `loading`**）：頁面已 `loaded` 狀態下 bump → 表格中的**既有資料列仍在 DOM**（不被骨架取代）；同時 `[data-testid="{page}-refreshing"]` 出現、區塊 `aria-busy="true"`、有可讀文字「更新中…」。
    - Test 7（U-05）：refetch 成功 → `{page}-refreshing` 消失，新值替換舊值。
    - Test 8（**U-06 / D-12：refetch 失敗**）：bump 後該請求回 503 → **舊資料仍在畫面上**；`[data-testid="{page}-refresh-error"]` 出現並顯示 `portfolioStaleAfterTrade`；`{page}-refresh-error-code` 顯示 `error.code`；`{page}-refresh-trace-id` 顯示 traceId；`{page}-refresh-retry` 可點且重試會再送請求。區塊**不進** `status: 'error'`（那會清掉舊資料）。
    - Test 9（D-12 各自獨立）：Positions 的 summary 回 503、holdings 回 200 → summary 區塊顯示 stale 提示，holdings 區塊顯示**新資料**，兩者互不影響。
    - Test 10（**D-12 最重要的一條**）：`createTrade` 200 之後某個 refetch 失敗 → OrderTicket 的 `ticket-result` 仍顯示成功與 trade id，**ticket 上沒有任何整體失敗訊息**，ticket 也沒有被關閉。
  </behavior>
  <action>
    RED 先行：10 條全部先寫（三頁都沒有 `watch(`、沒有 `refreshing` 旗標、沒有 `{page}-refresh*` testid）。

    **A. 三頁的 watch（形狀統一）**
    每頁新增一個 `watch(portfolioRevision, callback)`，callback 的第一行一律是 `if (live) return;` 早退，之後呼叫該頁**現有的** load 函式並帶上 refresh 旗標：
    - Overview：`loadSummary` + `loadRecentTrades`
    - Positions：`loadSummary` + `loadHoldings`
    - Trades：見 Task 2（走 `applyQueryChange`）

    **`if (live) return;` 是必要的**（Pitfall 12）：mock mode 完全走 `live` reactive 委派、不打網路。三頁的 `onMounted` 註解都已明文寫了這件事，watch 必須保持一致。

    **B. `refreshing` 旗標（U-05 —— 新增，不重用 `loading`）**
    Phase 3 的區塊狀態機是 `loading | loaded | error`（`Positions.vue:323-331`），其中 `loading` 會把表格換成骨架列。**refetch 不得重用它** —— 交易剛成功卻讓整頁資料消失再長回來，是「看起來像出錯了」的典型誤導，也會讓 D-12 想避免的「以為交易沒成功」重演。
    因此在每個區塊的狀態旁**並存**一個 `refreshing: boolean`（或把 load 函式改成接受一個 `refresh` 選項，refresh 模式下不進 `loading` 而是設 `refreshing`）：

    | 情境 | 狀態 | 視覺 |
    |---|---|---|
    | 首次載入（mount） | `status: 'loading'` | 既有骨架列 + 「載入中…」（**不改**） |
    | revision 觸發的重讀 | `status: 'loaded'` + `refreshing: true` | **保留現有資料**；區塊套 `.block-refreshing`（`opacity: .72; transition: opacity .15s`）；頂部一條 12px `--fg-dim` 文字 `portfolioRefreshing`；`aria-busy="true"` |
    | 重讀成功 | `refreshing: false` | 恢復 opacity；新值直接替換（**不做逐格動畫**，避免與 `fresh` 高亮打架） |
    | 重讀失敗 | `refreshing: false` + 額外 `refreshError` | **舊資料留在畫面上**，上方插入 `portfolioStaleAfterTrade` + 診斷列（code/traceId）+ `authRetry` 按鈕。**不進** `status: 'error'` |

    - `portfolioStaleAfterTrade` 節點用 `role="status"`（**不用 `alert`** —— 交易已成功，這不是需要打斷的錯誤）。
    - `{page}-refreshing` / `{page}-refresh-error` / `{page}-refresh-error-code` / `{page}-refresh-trace-id` / `{page}-refresh-retry` 的 `page` ∈ `overview` / `positions` / `trades`，沿用既有前綴慣例。
    - 診斷列沿用 Phase 3 已驗收的形狀（`Positions.vue:95-100` + CSS `:857-861`）：只露 `error.code` 與 `requestId`，**不露**後端 `error.message`。
    - 版面：refresh 指示放在既有 `.card` **內部頂端**，**不新增卡片、不改 grid span、不得造成版面高度跳動**（§Layout Contract）。

    **C. 四個資料源各自獨立（D-12）**
    Overview summary、Overview recent trades、Positions summary+holdings、Trades 列表 —— 一個失敗不影響其他三個拿到新資料。**不得**用一個共用的 `refreshing` 旗標涵蓋整頁。

    **D. `App.vue` 的處置**
    只在確實需要時調整 overlay props（例如把 `lang` 傳給新的 refresh 文案）。**不得**在 `App.vue` 加任何「refetch 失敗 → 全域錯誤」的邏輯 —— refetch 失敗**不觸發** toast，也**不進** `SessionBanner`（除非是 401，那由 `apiClient` 的 session 路徑處理）。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npx vitest run src/pages/Overview.test.ts src/pages/Positions.test.ts src/pages/Trades.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 三個 `.vue` 各含 `watch(portfolioRevision` 與 `if (live) return`
    - 三個 `.vue` 各含 `data-testid="overview-refreshing"` / `"positions-refreshing"` / `"trades-refreshing"`（依頁），以及對應的 `-refresh-error` / `-refresh-error-code` / `-refresh-trace-id` / `-refresh-retry`
    - 三個 `.vue` 各含 `role="status"` 用於 `portfolioStaleAfterTrade` 節點
    - 三個 `.vue` **不含** `error.message`
    - 三個測試檔各含 `resetPortfolioRevisionForTests`；`[FE] src/testSetup.ts` **未被修改**
    - `VITE_DATA_MODE=api npx vitest run src/pages/*.test.ts` exit 0
    - `npx vitest run src/pages/*.test.ts` exit 0（mock mode，含 Test 4 的零網路斷言）
  </acceptance_criteria>
  <done>三頁在 revision 變動時重讀自己的資料、保留舊值顯示更新中、失敗時舊值留存並明示 stale、mock mode 零網路、四個資料源各自獨立。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：D-11 Trades 重讀規則與提示 + D-13 fresh 高亮（含 DP-10 測試反轉）</name>
  <files>
    [FE] src/pages/Trades.test.ts,
    [FE] src/pages/Positions.test.ts,
    [FE] src/pages/Trades.vue,
    [FE] src/pages/Positions.vue
  </files>
  <read_first>
    - [FE] src/pages/Trades.vue（**`:281-286` `applyQueryChange(mutate)` —— javadoc 明文「任何篩選或排序變更都經此入口，頁碼一律重置為 0 後再請求」**；`:107` 的註解「整份列表就是後端當前頁的回應，前端不再做任何本地篩選或排序」；**`:109` 是要清掉的 Phase 3 TODO 註解**；`:162` `mockLastFill` computed；`:94` fresh class 的綁定表達式；`:462-465` `tbody tr.fresh` 動畫；`:470-473` `.pill` 樣式）
    - [FE] src/pages/Positions.vue（`:320` `mockLastFill` computed；`:235` fresh class 的綁定表達式；**`:260-264` 是要清掉的 Phase 3 TODO 註解**；`:762` `tbody tr.fresh` 動畫）
    - [FE] src/pages/Positions.test.ts（**含 Phase 3 commit `587e84e` 建立的測試 `鎖定 API mode 持倉列不帶 lastFill 高亮` —— DP-10 要求明確反轉它**）
    - [FE] src/services/portfolioRevision.ts（`apiLastFill`、`lastCreatedTradeId`、`clearLastCreatedTrade`）
    - [FE] src/services/portfolioApi.ts（`:36` mock `lastFill` 的形狀 `{ sym, type, qty, px }`）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §Interaction Contract 8 的 Trades 段、§9（**fresh 高亮的完整契約表**）、§Decisions U-12 / DP-10
  </read_first>
  <behavior>
    - Test 11（**D-11 —— 走既有單一入口**）：先選 Sell chip + 切排序 + 翻到第 2 頁 → bump → 斷言新請求的 query string **仍含** `type=SELL` 與該 sort/direction，但 `page=0`。
    - Test 12（**不得複製第二條重置邏輯**）：`?raw` 斷言 `Trades.vue` 的 `watch(portfolioRevision` 區塊內**不含** `pageNo.value = 0`（必須走 `applyQueryChange`）。
    - Test 13（D-11 提示，命中）：refetch 回的 `items` **含** `lastCreatedTradeId` → `[data-testid="trades-not-in-current-view"]` **不出現**。
    - Test 14（D-11 提示，未命中）：refetch 回的 `items` **不含** `lastCreatedTradeId` → 該提示出現並顯示 `tradeNotInCurrentView`，樣式為 `--fg-dim` 12px 說明列（**不是**錯誤色 —— 這不是錯誤）。
    - Test 15（**判定只能比 id**）：`?raw` 斷言判定邏輯**不重算篩選條件**（不含對 `type`/`dateFrom`/`dateTo` 的本地比較）。前端重算等於複製一份後端的篩選邏輯，第一個邊界情況（半開區間、時區）就會不一致（Phase 3 D-04 / judgment §7）。
    - Test 16（提示清除時機）：變更任何篩選／排序／頁碼後提示消失（呼叫 `clearLastCreatedTrade()`）。
    - Test 17（**D-13 / DP-10 測試反轉 —— Positions**）：把 Phase 3 的 `鎖定 API mode 持倉列不帶 lastFill 高亮` **改寫**為「API mode 持倉列依 `apiLastFill` 帶 fresh 高亮」：`notifyTradeCreated` 後，symbol 相符的那一列有 `fresh` class。測試名同步更新，**不可默默刪除**。
    - Test 18（D-13 / Trades）：`notifyTradeCreated` + refetch 後，第 0 列且 symbol 相符者有 `fresh` class。
    - Test 19（**U-12 非顏色線索**）：該列出現 `[data-testid="positions-fresh-badge"]` / `[data-testid="trades-fresh-badge"]`，文字為 `freshBadge`（`新` / `New`）。色盲、高對比模式、或動畫已結束的使用者都必須能看出是哪一列。
    - Test 20（來源切換）：mock mode 下 fresh 來自 `live.lastFill`；API mode 下來自 `apiLastFill`。**綁定表達式不改，只換來源**。
    - Test 21（**U-12 不用計時器**）：`?raw` 斷言兩頁的 fresh 相關程式碼**不含** `setTimeout`。計時器會讓元件測試變得時間相依而 flaky，而 `App.vue:36` 的 `v-if` 切頁卸載已界定實際壽命。
    - Test 22（`prefers-reduced-motion`）：CSS 含 `@media (prefers-reduced-motion: reduce)` 且在其中取消 `highlight` 動畫；`新` 標記照常顯示。
  </behavior>
  <action>
    RED 先行：12 條全部先寫。Test 17 會讓 Phase 3 的既有測試在改寫前後各紅一次 —— 先跑一次記錄「既有測試因 D-13 而必須反轉」的證據。

    **A. D-11：Trades 頁的重讀**
    在 `Trades.vue` 新增 `watch(portfolioRevision, callback)`，callback 為「`if (live) return;` 之後呼叫 `applyQueryChange` 並傳入一個**空的** mutate 函式」。
    傳空 mutate 是**刻意的** —— 重用既有的單一重置入口（`Trades.vue:281-286` 的 javadoc 已宣示「任何篩選或排序變更都經此入口」），而不是複製 `pageNo.value = 0; void loadTrades();` 兩行。這正好就是 D-11 要的三件事：保留 `activeFilter`、保留 `sortKey`/`sortDir`、頁碼歸零、重新請求。

    **B. 「不在目前檢視範圍」提示（D-11）**
    - refetch 完成後比對 `lastCreatedTradeId.value` 是否出現在回傳的 `items` 中：判定式為「`lastCreatedTradeId` 為 `null`，**或** `items` 中存在某筆 `t.id === lastCreatedTradeId`」→ 命名為 `inResultSet`。
    - `inResultSet === false` → 在列表**上方**顯示 `[data-testid="trades-not-in-current-view"]`，文字 `tradeNotInCurrentView`，樣式 `--fg-dim` 12px 說明列。
    - **判定只能比 `id`**。篩選與排序都在後端執行；前端重算「這筆是否符合條件」等於複製一份後端邏輯，且第一個邊界情況（半開區間、時區）就會不一致。
    - 清除時機：使用者變更任何篩選／排序／頁碼，或再次開啟 ticket → 呼叫 `clearLastCreatedTrade()`。

    **C. D-13：fresh 高亮**
    - 兩頁各加一個 computed，命名為 `effectiveLastFill`，其值為「`live` 存在時取 `live.lastFill`，否則取 `apiLastFill.value`」。
    - 把既有的 `mockLastFill` 引用（`Positions.vue:320` / `Trades.vue:162`）改為 `effectiveLastFill`。**因為 `LastFill` 的形狀與 mock 的 `{ sym, type, qty, px }` 逐字一致，`fresh` class 的綁定表達式（`Positions.vue:235` / `Trades.vue:94`）完全不用改，只是來源換了** —— 這是最小改動路徑。
    - **清掉兩條 Phase 3 的 TODO 註解**（D-13 明文）：`Positions.vue:260-264` 的「lastFill 在 API mode 無成交事件來源，故不綁 fresh class（Phase 4 引入 post-trade refetch 時再接）」與 `Trades.vue:109` 的同義註解。
    - 動畫沿用既有 `tbody tr.fresh { animation: highlight 1.6s ease-out; }`（`Trades.vue:462-465` / `Positions.vue:762`），底色由 `color-mix(in oklch, var(--accent) 22%, transparent)` 漸變到 transparent。**不新增動畫**。
    - **U-12 非顏色線索**：該列的 symbol 欄位旁加一個 12px `.pill` 文字標記（`freshBadge`），`data-testid="{page}-fresh-badge"`。`.pill` 樣式可複用 `Trades.vue:470-473`。
    - **不用 `setTimeout` 清除** —— 標記在 `effectiveLastFill` 仍匹配期間常駐；清除時機是再次開啟 ticket、Trades 頁任何篩選／排序／頁碼變更、或頁面 unmount（`App.vue:36` 的 `v-if` 切頁本來就會卸載）。計時器會讓元件測試時間相依而 flaky。
    - `@media (prefers-reduced-motion: reduce)` 下取消 `highlight` 動畫，`新` 標記照常顯示。

    **D. DP-10 的合法例外處理（judgment §10）**
    Phase 3 commit `587e84e` 的測試 `鎖定 API mode 持倉列不帶 lastFill 高亮` 與 D-13 直接矛盾。處置是**明確反轉**：
    1. 改寫斷言（由「不帶 fresh class」改為「帶 fresh class」）。
    2. 更新測試名為「API mode 持倉列依 apiLastFill 帶 fresh 高亮（Phase 4 D-13）」。
    3. 在測試上方加繁中註解說明「這條在 Phase 3 是刻意鎖住『不高亮』，Phase 4 的 D-13 讓測試意圖本身改變，因此反轉而非刪除」。
    4. SUMMARY 交代反轉的理由。
    **不可默默刪掉** —— 改動非自己產生的測試落在 judgment §9 第 2 條的邊緣，必須留下可追溯的紀錄。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npx vitest run src/pages/Trades.test.ts src/pages/Positions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/pages/Trades.vue` 含 `applyQueryChange`，且其 `watch(portfolioRevision` 區塊**不含** `pageNo.value = 0`
    - `Trades.vue` 含 `data-testid="trades-not-in-current-view"` 與 `lastCreatedTradeId`
    - 兩頁各含 `effectiveLastFill` 與 `apiLastFill`
    - 兩頁各含 `data-testid="positions-fresh-badge"` / `"trades-fresh-badge"`
    - 兩頁的 fresh 相關程式碼**不含** `setTimeout`
    - 兩頁 CSS 含 `@media (prefers-reduced-motion: reduce)`
    - `grep -n "Phase 4 引入 post-trade refetch 時再接\|Phase 4 接 post-trade refetch" src/pages/Positions.vue src/pages/Trades.vue` **無輸出**（兩條 TODO 註解已清掉）
    - `Positions.test.ts` 含反轉後的測試名（含 `D-13` 字樣）與說明註解；`git log` 可證明該測試是**改寫**而非新增+刪除
    - `VITE_DATA_MODE=api npx vitest run src/pages/Trades.test.ts src/pages/Positions.test.ts` exit 0
    - `npm test` exit 0；`VITE_DATA_MODE=api npm test` exit 0；`npm run build` exit 0
  </acceptance_criteria>
  <done>Trades 走既有單一入口重讀、不在檢視範圍的提示只比 id、兩頁 API mode fresh 高亮含非顏色線索、兩條 TODO 註解清除、DP-10 測試反轉已明確處理。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 交易成功 → portfolio 畫面 | 若畫面顯示得像「整體失敗」，使用者會重送 —— 換了 key 就會真的建出第二筆 |
| 後端篩選邏輯 → 前端「是否在結果集內」的判定 | 前端若重算篩選條件等於複製後端邏輯，邊界情況必然分歧 |
| refetch 失敗 → 畫面上的舊數值 | 舊數值若不標示為可能過期，使用者會把 stale 值當成後端真相 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Repudiation | refetch 失敗的畫面呈現 | mitigate | D-12：ticket 的 `result` 畫面**不受任何 refetch 結果影響**（Test 10）；區塊失敗只在該區塊顯示 `portfolioStaleAfterTrade`。這直接防止「以為交易沒成功 → 重送 → 換新 key → 真的建出第二筆」 |
| — | Tampering | refetch 重用 `loading` 清空表格 | mitigate | U-05：新增並存的 `refreshing` 旗標，保留舊資料 + `.block-refreshing` opacity。清空再長回來是「看起來像出錯了」的典型誤導 |
| — | Repudiation | stale 值被當成後端真相 | mitigate | U-06：失敗時舊資料留存但**明示**「交易已記錄，但這個區塊未更新成功，以下數字可能不是最新」+ 診斷列 + 重試鈕 |
| T-04-09 | Information Disclosure | refetch 錯誤的診斷資訊 | mitigate | 只露 `error.code` 與 `requestId`（沿用 Phase 3 D-12 的既有形狀），**不露**後端 `error.message`。acceptance criteria 用 grep 強制三頁不含 `error.message` |
| — | Information Disclosure | mock mode 意外對後端發請求 | mitigate | 三頁的 watch 都有 `if (live) return;` 早退，且 Test 4 用 `expect(fetchSpy).not.toHaveBeenCalled()` 鎖住（Pitfall 12） |
| — | Tampering | 前端重算「是否符合篩選」 | mitigate | 判定**只比 `id`**（Test 15 用 `?raw` 鎖住）。前端重算會複製後端邏輯，踩 Phase 3 D-04 / judgment §7 |
| — | Denial of Service | fresh 高亮用計時器 | mitigate | U-12：不用 `setTimeout`（Test 21 鎖住）。計時器讓測試時間相依而 flaky；`v-if` 切頁卸載已界定壽命 |
| T-04-SC | Tampering | npm installs | accept | 零新增依賴 |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Vue 元件符號（三個頁面）**
- `watch(portfolioRevision, ...)`（三頁各一，皆含 `if (live) return` 早退）
- `refreshing` 旗標（與 Phase 3 的 `status: 'loading' | 'loaded' | 'error'` **並存**，不取代）
- `refreshError` 狀態
- `effectiveLastFill` computed（`Positions.vue` / `Trades.vue`）
- `inResultSet` 判定（`Trades.vue`）

**`data-testid`**
`overview-refreshing`、`overview-refresh-error`、`overview-refresh-error-code`、`overview-refresh-trace-id`、`overview-refresh-retry`、
`positions-refreshing`、`positions-refresh-error`、`positions-refresh-error-code`、`positions-refresh-trace-id`、`positions-refresh-retry`、`positions-fresh-badge`、
`trades-refreshing`、`trades-refresh-error`、`trades-refresh-error-code`、`trades-refresh-trace-id`、`trades-refresh-retry`、`trades-fresh-badge`、`trades-not-in-current-view`

**CSS class**
- `.block-refreshing`（`opacity: .72; transition: opacity .15s`）
- `@media (prefers-reduced-motion: reduce)` 下停用 `highlight` 動畫的規則

**移除的內容**
- `Positions.vue:260-264` 與 `Trades.vue:109` 的 Phase 3 TODO 註解
- `mockLastFill` 的直接引用（改為 `effectiveLastFill`）

**測試變更（DP-10）**
- `Positions.test.ts` 的 `鎖定 API mode 持倉列不帶 lastFill 高亮` **反轉**為「API mode 持倉列依 apiLastFill 帶 fresh 高亮（Phase 4 D-13）」
</artifacts_this_phase_produces>

<verification>
- `VITE_DATA_MODE=api npx vitest run src/pages/*.test.ts` 綠
- `npm test` 綠、`VITE_DATA_MODE=api npm test` 綠、`npm run build` 綠
- **人工檢視**（`04-UI-SPEC.md` §Layout Contract 要求，不假裝自動化）：refresh 指示是否造成版面高度跳動、`fresh` 動畫與新值替換是否互相打架、`新` pill 是否改變列高

**不得宣稱覆蓋**：真實後端的完整下單→重讀流程 —— 屬 **Phase 5 / VER-03**。本 plan 證明的是「revision 變動時每一頁在其層級做對了事」，端到端串接不在 Phase 4。
</verification>

<success_criteria>
- 三頁在 revision 變動時重讀自己的資料，未掛載的頁不被觸發
- refetch 保留舊值、明示更新中；失敗時舊值留存並明示可能過期
- ticket 的成功畫面不受任何 refetch 結果影響
- Trades 保留篩選排序、頁碼歸零，且走既有單一入口
- 「不在檢視範圍」只比 id 判定
- API mode fresh 高亮生效且有非顏色線索
- mock mode 零網路請求
- DP-10 的測試反轉已明確處理並可追溯
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-12-SUMMARY.md`。

SUMMARY 必須記錄：
1. **DP-10 測試反轉的完整交代**：Phase 3 commit `587e84e` 的 `鎖定 API mode 持倉列不帶 lastFill 高亮` 為什麼是「測試意圖改變」而非「改測試遷就實作」（D-13 是新的使用者決策），以及反轉後的測試名與斷言。
2. `refreshing` 為什麼與 `loading` 並存而非重用（U-05：清空表格會讓交易成功看起來像出錯）。
3. D-11 走 `applyQueryChange` 傳空 mutate 而非複製兩行的理由。
4. 人工檢視項目的實際結果（版面跳動、動畫衝突、列高）。
</output>
