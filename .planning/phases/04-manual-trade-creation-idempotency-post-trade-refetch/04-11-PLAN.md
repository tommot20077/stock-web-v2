---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 11
type: execute
wave: 6
depends_on: ["04-10"]
files_modified:
  - "[FE] src/components/OrderTicket.vue"
  - "[FE] src/components/OrderTicket.test.ts"
autonomous: true
requirements: [TRAD-02, TRAD-04, TRAD-06]

must_haves:
  truths:
    - "送出期間整張 ticket 被凍結，送出鈕明確 disabled，連按兩次只呼叫一次 createTrade"
    - "idempotency key 在按下送出時才產生；同一次嘗試的重試沿用同一把；使用者改過任何欄位後換新的"
    - "驗證失敗（400）後改欄位再送會成功建立，不會吃 409 TRADE_IDEMPOTENCY_KEY_REUSED"
    - "後端 error.fields 的 key 綁到對應輸入框，其英文 value 絕不出現在畫面上"
    - "底部錯誤依 error.code 分派，不依賴錯誤出現的順序"
    - "SELL 時顯示可賣數量，但預檢通過仍可能被後端 409 拒絕且能正確顯示"
    - "成功畫面只渲染後端回傳的 TradeDto，不含任何前端計算的成交價或訂單編號"
  artifacts:
    - path: "[FE] src/components/OrderTicket.vue"
      provides: "D-09 兩態送出、D-14 key 生命週期、D-15 預檢、D-16 錯誤分派"
      contains: "crypto.randomUUID"
  key_links:
    - from: "[FE] src/components/OrderTicket.vue"
      to: "[FE] src/services/tradingApi.ts"
      via: "api.trading.createTrade(request, idempotencyKey)"
      pattern: "trading.createTrade"
    - from: "[FE] src/components/OrderTicket.vue"
      to: "[FE] src/services/portfolioRevision.ts"
      via: "成功後 notifyTradeCreated(trade)"
      pattern: "notifyTradeCreated"
    - from: "[FE] src/components/OrderTicket.vue"
      to: "[FE] src/services/portfolioApi.ts"
      via: "SELL 預檢讀 listHoldings()"
      pattern: "listHoldings"
---

<objective>
完成 ticket 的送出路徑：重複送出阻擋（TRAD-04）、idempotency key 的生命週期（D-14）、SELL 預檢（D-15）、錯誤分派（D-16）、以及只渲染後端 `TradeDto` 的成功畫面（D-09）。

Purpose：本 plan 是 judgment §5「前端 guard 只是 UX、server-side 冪等才是防護」在 UI 層的落實，也是 D-14 與 D-07 **互鎖**的解法。這個互鎖是設計時發現的實際問題不是理論風險：若採「一張 ticket 一個 key」，驗證失敗（400）後使用者改數量再送就會吃 409 `KEY_REUSED`，卡住且不知道要關掉 ticket 才能繼續。

Output：完整的送出路徑 + 對應的元件測試。

**必須實作否則使用者會卡死（U-04）**：收到 `TRADE_IDEMPOTENCY_KEY_REUSED` 時前端**必須丟棄當前 key**（等價於 `dirtySinceSubmit = true`），否則使用者照文案「重新送出」會用同一把 key 再吃一次 409，形成無出路的迴圈。相反地，`TRADE_CONFLICT` 與網路失敗必須**保留** key（意圖沒變，重送安全）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/judgment.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-CONTEXT.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-07-SUMMARY.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-10-SUMMARY.md
</context>

<repo_and_branch>
sibling 前端 repo `D:\end\workspace\vue\stock-v2`（真專案根 `vue-app/`），分支 `feature/phase-04-manual-trade-creation`。`[FE]` 前綴表示相對於 `vue-app/`。
</repo_and_branch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：重複送出阻擋 + D-14 key 生命週期 + D-09 成功畫面</name>
  <files>
    [FE] src/components/OrderTicket.test.ts,
    [FE] src/components/OrderTicket.vue
  </files>
  <read_first>
    - [FE] src/components/OrderTicket.vue（04-10 之後的狀態；注意 **`:185` 的送出鈕目前完全沒有 `:disabled`** —— 現況靠「`step='placing'` 讓 footer 整塊被替換、送出鈕從 DOM 消失」這個副作用擋連點，那不是明確的 guard）
    - [FE] src/services/tradingApi.ts（04-07 的 `createTrade(request, idempotencyKey)`）
    - [FE] src/services/portfolioRevision.ts（04-07 的 `notifyTradeCreated(trade)`、`resetPortfolioRevisionForTests()`）
    - [FE] src/services/apiTypes.ts（`TradeDto` 的 11 個欄位）
    - [FE] src/pages/Trades.vue（`:218-226` 既有的 `toLocalIso(date)` helper —— executedAt 轉 ISO 一律重用它）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §Interaction Contract 1（三步驟表）、§5（重複送出阻擋的六列凍結清單 + `dirtySinceSubmit` 的互動後果）、§6（冪等命中的可見行為 + replay 也要 `notifyTradeCreated`）、§Decisions U-02 / U-03 / U-04
  </read_first>
  <behavior>
    在 `OrderTicket.test.ts` 追加（API mode；`vi.stubGlobal('crypto', { randomUUID: () => \`key-${++n}\` })` 讓 key 可斷言）：
    - Test 22（TRAD-04）：用**未 resolve** 的 promise stub `createTrade`，點送出後 `[data-testid="ticket-submit"]` 的 `disabled` 為 `true`，標籤變成 `recordingTrade`。
    - Test 23（TRAD-04）：連按送出兩次 → `createTradeSpy` 只被呼叫 **1** 次。
    - Test 24（**§5 凍結清單**）：送出中，`ticket-back-to-edit` 為 `disabled`、五個表單輸入皆 `disabled`、點遮罩與 `ticket-close` 都**不會**關閉 ticket、ticket 容器 `aria-busy="true"`、`[data-testid="ticket-submitting-status"]` 存在且有 `role="status"` `aria-live="polite"` 與可讀文字（**不得只有 spinner**）。
    - Test 25（D-14 規則 1，產生時機）：開啟 ticket 時 `crypto.randomUUID` **未**被呼叫；按下送出時才被呼叫**一次**。
    - Test 26（D-14 規則 1，重試沿用）：第一次因網路錯誤失敗 → 按重試（不改任何欄位）→ 第 2 次 `createTrade` 收到的 key 與第 1 次**相同**。
    - Test 27（D-14 規則 2，改欄位換新）：失敗後改 `ticket-qty` → 再送 → key 與第 1 次**不同**。
    - Test 28（**D-14／D-07 互鎖 —— CONTEXT.md 明文要求、Pitfall 11 的驗收**）：第 1 次 stub 回 400 `VALIDATION_FAILED` + `fields:{quantity:'must be...'}` → 改 quantity → 第 2 次 stub 回 200 → 斷言 (a) 第 2 次的 key ≠ 第 1 次，(b) 最終狀態是 `ticket-result`（已記錄）而**不是** 409。
    - Test 29（**DP-11 dirtySinceSubmit 的語意**）：失敗後把 quantity 改掉再**改回原值** → 下次送出仍換新 key（不做 form 物件深比較 —— 「改了又改回來」在使用者心智模型裡是「我動過了」）。
    - Test 30（D-09 成功畫面，**刻意矛盾 fixture**）：stub `createTrade` 回一個 `TradeDto`，其 `price` 與 `quantity` **刻意與表單輸入不同**（例如表單送 10 @ 190.20，回傳 7 @ 188.88）→ 斷言 `ticket-result-price` 顯示 **188.88**、result 的數量顯示 **7**。任何一處回到用表單值渲染，斷言就會紅。
    - Test 31（D-09）：`ticket-result-trade-id` 顯示回傳的 `TradeDto.id`（UUID 全長不截斷）；`ticket-result-executed-at` 顯示回傳的 `executedAt`；畫面**不含** `Avg fill`、`Order ID`、`成交均價`、`訂單號`。
    - Test 32（**U-03 冪等命中**）：stub 第 2 次送出（同 key）回**與第 1 次相同**的 `TradeDto` → 畫面與首次建立**完全相同**（沒有「這筆已存在」的變體）。
    - Test 33（**U-03 連帶契約**）：上述 replay 情境下 `notifyTradeCreated` **仍被呼叫**（重讀同一份資料無害；少做會讓「網路失敗後重試成功」的使用者看不到 portfolio 更新）。
    - Test 34（TRAD-02 payload）：送出時傳給 `createTrade` 的 request，其 key 集合恰為七個合約欄位，`executedAt` 是帶 offset 的 ISO 字串，`fee` 是使用者輸入值（預設 0）。
  </behavior>
  <action>
    RED 先行：13 條全部先寫。

    **A. `submitting` 與凍結（U-02，§5 的六列清單）**
    - 送出鈕加**明確** `:disabled="submitting || !canSubmit"`，標籤在 `submitting` 時換成 `t(lang,'recordingTrade')`，`min-width` 固定使標籤變化不造成版位跳動。
    - `ticket-back-to-edit` 在 `submitting` 時 `disabled`（回到表單會讓 in-flight 的 payload 與畫面不一致）。
    - `onClose` / `onMaskClick` 的守衛條件為 `if (submitting.value) return;`（04-09 已改）。
    - 所有表單輸入在 `submitting` 時 `disabled`（即使當前在 review 步驟不可見，回退後也不得可編輯）。
    - ticket 容器 `:aria-busy="submitting"`。
    - `[data-testid="ticket-submitting-status"]`：`role="status" aria-live="polite"`，文字 `recordingTrade`。

    **為什麼要凍結整張 ticket 而不只鎖送出鈕**：key 在「按下送出」那一刻產生（D-14），而規則 2 說「改過任何欄位就換新 key」。若送出期間允許編輯，`dirtySinceSubmit` 的語意會變成「in-flight 的那次要不要換 key」—— 一個無解的競態。凍結讓 D-14 的兩條規則保持互斥且可測。

    **B. key 生命週期（D-14 + DP-11）**
    - `const currentKey = ref<string | null>(null)`、`const dirtySinceSubmit = ref(false)`。
    - 按下送出時：若 `currentKey.value === null || dirtySinceSubmit.value` → `currentKey.value = crypto.randomUUID()`；然後 `dirtySinceSubmit.value = false`。
      **key 必須用 `crypto.randomUUID()`（Web Crypto，CSPRNG），絕不用 `Math.random()`。**
    - 任何欄位的 `@input` / `watch` 設 `dirtySinceSubmit = true`。**不做 form 物件深比較**（DP-11）。
    - 送出成功後 `currentKey.value = null`（下一筆是新意圖）。
    - 送出失敗後：**保留** key（同一次嘗試的重試沿用）—— 除了 `TRADE_IDEMPOTENCY_KEY_REUSED`（Task 2 處理丟棄）。
    - 關閉並重開 ticket → 重置為 `null`。

    **C. 送出流程（D-09 兩態）**
    - 送出中**不切換步驟**，停在 `review`，只切 `submitting`。
    - `const trade = await api.trading.createTrade(request, currentKey.value)`。
    - `request.executedAt` 用 `Trades.vue:218-226` 既有的 `toLocalIso(date)` helper 轉成帶 offset 的 ISO 字串（**不得**新造，**不得**裝 `date-fns`/`dayjs`）。
    - 成功後：`notifyTradeCreated(trade)` → `step = 'result'` → 焦點移到 result 的 `<h2 tabindex="-1">`。

    **D. result 畫面（D-09 / §Interaction Contract 1）**
    - `rev-grid` **只能**顯示後端回傳的 `TradeDto` 欄位：`id` / `type` / `quantity` / `price` / `fee` / `executedAt` / `symbol`。
    - `estTotal` 可顯示，但必須標示為 `quantity × price`（不含 fee）**且來源是回傳值而非表單值**。
    - **不得**出現「平均成交價」「訂單號」、隨機 slippage、或任何前端計算的成交價（D-09 / judgment §1）。
    - 交易編號是 36 字元 UUID：`overflow-wrap: anywhere`，**不得**截斷或 ellipsis（唯一用途是除錯回報）。
    - `✓` 圓標 48px 且 `aria-hidden="true"`；成功語意由 `<h2>` 標題承擔。

    **E. U-03 冪等命中的可見行為**
    - **不做**「這筆已存在」的變體 —— 目前 API 契約**沒有**任何 replay 訊號；要區分就得新增 response header 或信封欄位，那是 API 契約 shape 變更（judgment §9 要求先問 Yuan），**planner 與 executor 都不得自行加**。
    - replay 也要呼叫 `notifyTradeCreated(trade)`（§6 連帶契約）。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npx vitest run src/components/OrderTicket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/OrderTicket.vue` 含 `crypto.randomUUID()` 且**不含** `Math.random`
    - 該檔含 `:disabled="submitting` （送出鈕的明確 guard）、`aria-busy`、`role="status"`
    - 該檔含 `dirtySinceSubmit` 與 `notifyTradeCreated`
    - 該檔含 `data-testid="ticket-submitting-status"`、`ticket-result-trade-id`、`ticket-result-price`、`ticket-result-executed-at`
    - 該檔的 result 區塊**不含** `avgFillPx`、`orderId`、`estFee`（估算費）
    - 該檔含 `toLocalIso`（重用既有 helper）
    - `VITE_DATA_MODE=api npx vitest run src/components/OrderTicket.test.ts` exit 0
  </acceptance_criteria>
  <done>連點只送一次、整張 ticket 凍結、key 三條生命週期規則各有測試、D-14/D-07 互鎖路徑走得通、成功畫面只渲染後端值。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：D-16 錯誤分派 + D-15 SELL 預檢</name>
  <files>
    [FE] src/components/OrderTicket.test.ts,
    [FE] src/components/OrderTicket.vue
  </files>
  <read_first>
    - [FE] src/components/OrderTicket.vue（Task 1 之後）
    - [FE] src/pages/Positions.vue（`:336-368` `describeError` + `BlockError`；`:95-100` 與 CSS `:857-861` 的**診斷列既有形狀**（code + traceId 常駐低調單列））
    - [FE] src/services/apiClient.ts（`ApiClientError` 的 `status` / `code` / `requestId` / `fields`；`:212` `ensureCsrfToken()` 在 bootstrap 後仍拿不到 cookie 時丟 `AUTH_CSRF_TOKEN_MISSING`（`status: 0`）；`:315` / `:323` 是全檔**唯二**呼叫 `sessionHandlers.onRefreshFailed?.()` 的位置，兩處都在 401 路徑，與 CSRF 403 無關）
    - [FE] src/services/apiClient.test.ts（`:395` `it('keeps CSRF 403 distinguishable as typed backend errors')` —— 既有測試已明鎖「CSRF 403 只 reject 成 typed `ApiClientError`，不進 session 升級路徑」）
    - [FE] src/services/portfolioApi.ts（`:50` `listHoldings(): Promise<HoldingDto[]>`；`:184` HTTP 實作）
    - [FE] src/services/apiTypes.ts（`HoldingDto` 的欄位 —— D-15 **只能碰** `symbol` 與 `totalQuantity`）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §Interaction Contract 4 的 SELL 預檢段、§7（**錯誤展示與分派的完整表格 + key 處置表 + U-08 CSRF 覆寫說明**）、§Copywriting Contract 的兩張錯誤對照表、§Security & Diagnostics Display Contract
  </read_first>
  <behavior>
    在 `OrderTicket.test.ts` 追加（API mode）：
    - Test 35（D-16 欄位級）：stub 400 + `fields:{quantity:'must be greater than or equal to 0.00000001'}` → `[data-testid="ticket-field-error-quantity"]` 出現並顯示 `tradeErrQuantity` 的**前端文案**；該輸入框 `aria-invalid="true"` 且 `aria-describedby` 含錯誤節點 id；**ticket 自動退回 `ticket` 步驟**（否則使用者在 review 看不到出錯的欄位）。
    - Test 36（**D-16 最重要的 negative test**）：同一情境下 `document.body.textContent` **不含** `must be greater than or equal to`（`fields` 的英文 Bean Validation value 絕不出現在 DOM）。
    - Test 37（多欄位）：`fields:{quantity:..., price:...}` → 兩個輸入框都出現對應錯誤節點。
    - Test 38（**Pitfall 10**）：stub 409 且**無** `fields`（後端 `fieldsFrom` 空 map 回 `null` 而非 `{}`）→ 底部顯示錯誤，**不 crash**。
    - Test 39（**依 code 分派，參數化**）：對 `TRADE_INSUFFICIENT_HOLDING` / `ASSET_NOT_FOUND` / `TRADE_CONFLICT` / `TRADE_IDEMPOTENCY_KEY_REUSED` / `TRADE_UNSUPPORTED_TYPE` / `TRADE_INVALID_QUANTITY` / `TRADE_INVALID_PRICE` / `VALIDATION_FAILED`(fields 為 null) / `AUTH_CSRF_TOKEN_INVALID` / 403 非 CSRF / 未知 code 各跑一次 → `[data-testid="ticket-error"]` 顯示 §Copywriting 對照表指定的文案，`ticket-error-code` 顯示該 code，`ticket-error-trace-id` 顯示 `requestId`。**分派依 `error.code`，絕不假設錯誤出現順序**（Q0/PR #15 的明文警告：PR #15 會把「type 打錯 + symbol 不存在」的優先序從 `ASSET_NOT_FOUND` 改回 `TRADE_UNSUPPORTED_TYPE`）。
    - Test 40（network）：`createTrade` reject 成非 `ApiClientError`（`new TypeError('fail')`）→ 顯示 `tradeErrNetwork`，**不顯示** raw Error message，且 traceId 那一格**不渲染**（不顯示 `null`）。
    - Test 41（Phase 3 D-12）：成功態下 `document.body.textContent` **不含**任何 traceId（診斷資訊只在錯誤狀態出現）。
    - Test 42（**U-04 key 處置表**）：收到 `TRADE_IDEMPOTENCY_KEY_REUSED` → 下一次送出的 key 與前一次**不同**（key 已丟棄）；收到 `TRADE_CONFLICT` 或 network 錯誤 → 下一次送出的 key **相同**（key 保留）。
    - Test 43（**401 不在 ticket 顯示**）：`createTrade` reject 成 401 且 refresh 失敗 → ticket 內**無**錯誤節點；走全域 `SessionBanner`（`apiClient` 的 `onRefreshFailed` handler 被呼叫）。
    - Test 44（D-15 顯示）：切到 SELL → 呼叫 `portfolio.listHoldings()` **一次** → `[data-testid="ticket-sellable-qty"]` 顯示該 symbol 的 `totalQuantity`；換 symbol **不再**呼叫（該 ticket 生命週期內快取）。
    - Test 45（D-15 三態）：載入中顯示 `ticket-sellable-loading`（`sellableQtyLoading`）；失敗顯示 `ticket-sellable-failed`（`sellableQtyFailed`）且**不阻擋送出**（後端仍是權威）。
    - Test 46（D-15 零持倉的文案）：`listHoldings` 回的陣列不含該 symbol → 顯示「可賣數量：0」，**不得**顯示「您未持有此標的」（後端 SQL 有 `total_quantity > 0` 過濾，`JdbcTradingRepository.java:210`，讓「從未持有」與「已全數賣出」不可分）。
    - Test 47（D-15 超量）：qty > sellableQty → 顯示 `tradeErrOversell` 且送出鈕 `disabled`。
    - Test 48（**judgment §5 的實質驗收**）：`listHoldings` 回 `totalQuantity: 100`、送 qty 10（**預檢通過**）→ `createTrade` stub 回 409 `TRADE_INSUFFICIENT_HOLDING` → 底部正確顯示該錯誤。前端 guard 不是防護。
    - Test 49（D-15 快取失效）：一筆 SELL 成功後（`notifyTradeCreated` 觸發）→ ticket 內的 holdings 快取失效，下次切 SELL 會重新 `listHoldings()`。
    - Test 50（Phase 3 D-04 / judgment §7）：`?raw` 斷言 `OrderTicket.vue` 的 SELL 預檢區塊**不引用** `avgCost` / `costBasis` / `unrealizedPnl` / `realizedPnl` / `roi`（預檢只比數量上限，不重算成本或損益）。
  </behavior>
  <action>
    RED 先行：16 條全部先寫。

    **A. 錯誤狀態的資料形狀**
    `submitError` 存一個 `{ code, traceId, fields }`（`fields` 可為 `null`）。`describeError` 的形狀直接複製 `Positions.vue:353-356`，但 D-16 多一層：`error instanceof ApiClientError && error.fields` → 綁欄位；否則依 `error.code` 分派到底部。

    **B. 欄位級錯誤（D-16，§7 版位表）**
    - `const fieldErrorKeys = computed(() => Object.keys(submitError.value?.fields ?? {}))` —— **只用 `fields` 的 key 判斷「哪個欄位錯了」，文案完全由前端 i18n 提供**。
    - 對應：`symbol`→`tradeErrSymbol`、`type`→`tradeErrType`、`quantity`→`tradeErrQuantity`、`price`→`tradeErrPrice`、`fee`→`tradeErrFee`、`note`→`tradeErrNote`。（CONTEXT.md 列的清單漏了 `type`，已補上。）
    - 節點 id 為 `trade-{field}-error`、`data-testid="ticket-field-error-{field}"`；對應輸入框 `aria-invalid="true"` 且 `aria-describedby` 含該 id（多個 id 以空白分隔）。
    - **欄位錯誤節點不加 `role="alert"`**（6 個同時觸發會連續朗讀 6 次）；由 `aria-describedby` 在聚焦時朗讀。
    - 有 `fields` 時自動把 `step` 退回 `'ticket'`。
    - **絕對禁止** `{{ submitError.fields.quantity }}` 這種直接顯示 —— `fields` 的 value 是英文 Bean Validation 預設訊息，D-16 明文禁止（且版本可能變動）。Test 36 是這條的機械化保證。
    - `executedAt` **不會**出現在 `fields`（該欄位在後端沒有任何 Bean Validation 註解）—— 它的前端自檢錯誤（未來時間）走 `tradeErrExecutedAt`，後端若回相關錯誤則落到底部 `tradeErrValidation`。

    **C. 底部錯誤（§7 + §Copywriting 對照表）**
    - 單一區域 `[data-testid="ticket-error"]`，`role="alert"`（一次只有一條，適合立即播報），沿用 `.form-error` 樣式。
    - 依 `error.code` 分派到 §Copywriting Contract 表格指定的 i18n key。**絕不依賴錯誤出現順序**。
    - 診斷列：常駐低調單列（**不用 `<details>`、不加複製按鈕** —— U-07），只露 `error.code` 與 `requestId`，**不露**後端 `error.message`。非 `ApiClientError` 時 traceId 為 null → 該 span `v-if` **不渲染**（不顯示 `null`）。`overflow-wrap: anywhere`，`user-select` 不得被禁用。

    **D. CSRF 403 的版位（U-08 —— 刻意覆寫 `02-UI-SPEC.md:144`）**
    `02-UI-SPEC.md:144` 把 `AUTH_CSRF_TOKEN_INVALID` **無條件**列入全域 banner 清單。Phase 4 依 D-16 **刻意收窄**它：
    > **app 啟動時的 CSRF bootstrap 失敗 → 全域 banner；單一 unsafe 請求被 CSRF 拒絕 → 該請求的發起處（即 ticket 底部）。**
    這個邊界**已由既有 code 支撐，不需改動 `apiClient`**（`apiClient.ts:212` 的 bootstrap 失敗丟 `AUTH_CSRF_TOKEN_MISSING`；`:315`/`:323` 是唯二觸發 `onRefreshFailed` 的 401 路徑；`apiClient.test.ts:395` 已鎖「CSRF 403 只 reject 成 typed error」）。本 plan 只決定 ticket 這個呼叫端怎麼呈現自己拿到的 typed error。
    401 / refresh 失敗仍**不在 ticket 顯示**，走全域 `SessionBanner`。

    **E. key 處置（U-04，§7 表格）**
    | code | 處置 |
    |---|---|
    | `TRADE_IDEMPOTENCY_KEY_REUSED` | **丟棄 key**（設 `dirtySinceSubmit = true` 或直接 `currentKey = null`）—— 否則使用者照文案「重新送出」會用同一把 key 再吃一次 409，形成無出路迴圈 |
    | `TRADE_CONFLICT` | **保留** —— 「這次沒寫入，但意圖沒變」，沿用同一把重送是安全且正確的（這也是文案敢寫「不會建立重複交易」的前提） |
    | 網路失敗 / 5xx | **保留** |
    | `VALIDATION_FAILED` / `fields` 類 | 保留（使用者一改欄位就會自動丟棄） |
    | `TRADE_INSUFFICIENT_HOLDING` | 保留（同上） |
    | CSRF / 403 | 保留 |

    **F. SELL 預檢（D-15，§Interaction Contract 4）**
    - side 切到 `SELL` 時**載入一次** `api.portfolio.listHoldings()` 並在該 ticket 生命週期內快取（Q9.2 的取捨：只有 SELL 需要；一次拿全部之後換 symbol 不必再打；`HoldingDto[]` 對單一使用者是小陣列）。
    - `sellableQty = holdings.find(h => h.symbol === symbol)?.totalQuantity ?? 0`。
    - 三態：`sellableQtyLoading` / 正常顯示 / `sellableQtyFailed`（**失敗不阻擋送出**）。
    - **只讀 `symbol` 與 `totalQuantity` 兩個欄位**。`HoldingDto` 的 `avgCost` / `costBasis` / `marketPrice` / `marketValue` / `realizedPnl` / `unrealizedPnl` / `roi` **一律不得使用** —— 用它們算「賣出後的損益預估」會踩 Phase 3 D-04 與 judgment §7。Test 50 用 `?raw` 鎖住這條。
    - 超量時顯示 `tradeErrOversell` 且 `canSubmit` 為 false。
    - 成功後（`notifyTradeCreated` 之後）ticket 內的 holdings 快取必須失效 —— ticket 也 `watch(portfolioRevision)` 即可。
    - 文案硬規則：零持倉顯示「可賣數量：0」，**不得**寫「您未持有此標的」。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npx vitest run src/components/OrderTicket.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/OrderTicket.vue` 含 `data-testid="ticket-error"`、`ticket-error-code`、`ticket-error-trace-id`、`ticket-field-error-`、`ticket-sellable-qty`、`ticket-sellable-loading`、`ticket-sellable-failed`
    - 該檔含 `aria-invalid`、`aria-describedby`、`role="alert"`
    - 該檔**不含**任何直接輸出 `fields` value 的表達式（`grep -nE "fields\.[a-z]+ *}}" src/components/OrderTicket.vue` 無輸出）
    - 該檔的 SELL 預檢區塊**不含** `avgCost`、`costBasis`、`unrealizedPnl`、`realizedPnl`、`roi`
    - 該檔含 `portfolio.listHoldings`
    - 該檔**不含** `error.message`（不顯示後端訊息）
    - `VITE_DATA_MODE=api npx vitest run src/components/OrderTicket.test.ts` exit 0（本 plan 的 29 條 + 04-09/04-10 的 34 條全綠）
    - `npm test` exit 0；`VITE_DATA_MODE=api npm test` exit 0；`npm run build` exit 0
  </acceptance_criteria>
  <done>11 種 error code 各有分派測試、英文 value 不入 DOM 的 negative test 綠、SELL 預檢三態就位且不重算損益、預檢通過仍被後端拒絕的路徑有測試。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 使用者連點 → 不可逆帳本寫入 | 前端 guard 是第一道 UX 防線，server-side 冪等是唯一真防護（judgment §5） |
| idempotency key 的生命週期 | key 重用於不同意圖會導致 409 死鎖；key 提前更換會導致重複交易 |
| 後端 `error.fields` / `error.message` → DOM | 英文 Bean Validation 訊息與後端內部訊息都不得成為使用者可見輸出 |
| 前端 SELL 預檢 → 使用者的信心 | 預檢通過不代表可以賣（併發：另一個 tab 剛賣掉了） |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Spoofing | idempotency key 的隨機來源 | mitigate | `crypto.randomUUID()`（Web Crypto，CSPRNG）。**絕不用 `Math.random()`** —— 它不是密碼學隨機，碰撞會導致別人的交易被當成你的重試回傳。acceptance criteria 用 grep 強制。`(user_id, key)` 的 user_id 維度另使碰撞影響限縮在同一使用者內 |
| T-04-01 | Tampering / Repudiation | key 的生命週期規則 | mitigate | D-14 兩條規則 + U-04 的 code 處置表。`TRADE_CONFLICT`／網路失敗**保留** key（重送安全）；`KEY_REUSED` **丟棄** key（否則死鎖）。Test 26/27/42 各自鎖住 |
| T-04-04 | Denial of Service | 連點送出 | mitigate | 明確 `:disabled="submitting"` + 整張 ticket 凍結（U-02）。現況只靠「按鈕從 DOM 消失」的副作用（Q6.2），不是 guard。Test 22/23/24 鎖住 |
| T-04-09 | Information Disclosure | `fields` 的英文 value | mitigate | 只用 `fields` 的 **key** 判斷欄位，文案全由前端 i18n 提供。Test 36 斷言 `not.toContain('must be greater than or equal to')` —— **這是 D-16 最重要的 negative test** |
| T-04-09 | Information Disclosure | 後端 `error.message` | mitigate | 只顯示 `error.code` 與 `requestId`；acceptance criteria 用 `grep` 強制元件不含 `error.message` |
| T-04-03 | Information Disclosure | idempotency key 顯示於 UI | mitigate | `04-UI-SPEC.md` §Security & Diagnostics 明文「UI 絕不顯示 idempotency key」—— 顯示它只會邀請使用者手動改動。元件不得把 `currentKey` 綁進任何模板節點 |
| T-04-06 | Tampering / Spoofing | CSRF 403 的呈現版位 | mitigate | U-08：bootstrap 失敗 → 全域 banner；單一請求被拒 → ticket 底部（`tradeErrCsrf`，文案指示「重新整理頁面」）。**不需改動 `apiClient`**（`apiClient.test.ts:395` 已鎖 CSRF 403 不進 session 升級路徑） |
| T-04-07 | Tampering | oversell 預檢 | mitigate | 預檢**只是 UX**。Test 48 明確覆蓋「前端預檢通過但後端 409」的路徑 —— **這才是 judgment §5「前端 guard 不是防護」的實質驗收** |
| — | Information Disclosure | SELL 預檢讀取的欄位 | mitigate | 只碰 `symbol` 與 `totalQuantity`。Test 50 用 `?raw` 斷言不引用 `avgCost` 等六個損益欄位（Phase 3 D-04 / judgment §7：前端絕不重算成本或損益） |
| — | Repudiation | result 畫面的數值來源 | mitigate | Test 30 的「刻意矛盾」fixture（回傳值與表單值不同）讓任何用表單值渲染的實作立刻紅。畫面只呈現後端事實 |
| T-04-SC | Tampering | npm installs | accept | 零新增依賴。UUID 用內建 `crypto.randomUUID()`（**不裝 `uuid`**）；日期用既有 `toLocalIso`（**不裝 `date-fns`/`dayjs`**） |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Vue 元件內部符號（`src/components/OrderTicket.vue`）**
- `submitting` / `currentKey` / `dirtySinceSubmit` refs
- `submitError`（`{ code, traceId, fields }`）與 `fieldErrorKeys` computed
- `sellableQty` / `sellPrecheckError` computed 與 holdings 快取
- 依 `error.code` 分派到 i18n key 的對照函式

**`data-testid`**
`ticket-submitting-status`、`ticket-error`、`ticket-error-code`、`ticket-error-trace-id`、`ticket-field-error-{field}`（field ∈ symbol/type/quantity/price/fee/note/executedAt）、`ticket-sellable-qty`、`ticket-sellable-loading`、`ticket-sellable-failed`、`ticket-result`、`ticket-result-trade-id`、`ticket-result-price`、`ticket-result-executed-at`

**HTML id**
`trade-{field}-error`（field ∈ symbol/type/quantity/price/fee/note/executedAt）

**消費的後端錯誤契約（由 04-01 ~ 04-05 提供）**
`TRADE_IDEMPOTENCY_KEY_REUSED`、`TRADE_INSUFFICIENT_HOLDING`、`TRADE_CONFLICT`、`ASSET_NOT_FOUND`、`TRADE_UNSUPPORTED_TYPE`、`TRADE_INVALID_QUANTITY`、`TRADE_INVALID_PRICE`、`VALIDATION_FAILED`（含 `fields`）、`AUTH_CSRF_TOKEN_INVALID`、`AUTH_CSRF_TOKEN_MISSING`
</artifacts_this_phase_produces>

<verification>
- `VITE_DATA_MODE=api npx vitest run src/components/OrderTicket.test.ts` 綠
- `npm test` 綠、`VITE_DATA_MODE=api npm test` 綠、`npm run build` 綠
- `grep -n "Math.random\|error.message\|fields\." src/components/OrderTicket.vue` 的輸出人工檢視確認無違規
- **人工檢視**（不假裝自動化）：送出中的視覺凍結是否明顯、錯誤區塊在 320px 寬度下 code 與 traceId 是否完整換行不水平滾動、`記錄中…` / `Recording…` 的按鈕寬度是否造成版位跳動

**不得宣稱覆蓋**：真實 CSRF／cookie 行為（jsdom 不實作 HttpOnly）、真實 network call 的證據 —— 屬 **Phase 5 / VER-03**。
</verification>

<success_criteria>
- 連按兩次只送一次，且靠明確的 `:disabled` 而非副作用
- D-14 三條 key 規則各有測試；D-14/D-07 互鎖路徑（400 → 改欄位 → 成功）走得通
- `fields` 的英文 value 在 DOM 零出現
- 11 種 error code 各有正確文案、code、traceId
- SELL 預檢顯示可賣數量但不重算損益，且預檢通過仍可能被後端拒絕
- 成功畫面只渲染後端 `TradeDto`
- 401 走 SessionBanner、CSRF 403 走 ticket 底部
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-11-SUMMARY.md`。

SUMMARY 必須記錄：
1. **U-08 是刻意覆寫 `02-UI-SPEC.md:144`**（不是「不衝突」）：CSRF 403 的分界為「bootstrap 失敗 → banner；單一請求被拒 → 發起處」，依 D-16，且未改動 `apiClient`。
2. U-04 的 key 處置表實作結果，特別是 `TRADE_IDEMPOTENCY_KEY_REUSED` 丟棄 key 的理由（否則使用者無出路）。
3. U-03：冪等命中不做「已存在」變體的理由（API 契約沒有 replay 訊號；加訊號是 shape 變更，judgment §9 需先問 Yuan）。
4. D-15 只讀 `symbol` 與 `totalQuantity` 的自我約束，與 Test 50 的 `?raw` 斷言。
5. 人工檢視項目的實際結果（版位、換行、按鈕寬度）。
</output>
