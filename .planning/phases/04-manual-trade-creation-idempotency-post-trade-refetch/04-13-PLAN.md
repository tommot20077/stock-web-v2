---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 13
type: execute
wave: 8
depends_on: ["04-05", "04-12"]
files_modified: []
autonomous: false
requirements: [TRAD-01, TRAD-02, TRAD-03, TRAD-04, TRAD-05, TRAD-06]

must_haves:
  truths:
    - "後端兩個套件（unit + IT）與前端兩個 mode 的測試與 build 全部綠，且是在同一份程式碼狀態下跑的"
    - "六條 TRAD 需求各自有可指名的自動化證據，沒有任何一條靠「畫面看起來正常」宣稱覆蓋"
    - "Phase 4 明確不能證明的事情已被列出並歸屬到 Phase 5 / PR #15，沒有 over-claim"
    - "Yuan 已親眼確認 API mode 與 mock mode 的 ticket 行為符合 UI 契約"
  artifacts:
    - path: ".planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-13-SUMMARY.md"
      provides: "Phase 4 的跨 repo 驗證證據與 over-claim 稽核"
      contains: "judgment §8"
  key_links:
    - from: "後端 repo feature/phase-04-trade-idempotency"
      to: "前端 repo feature/phase-04-manual-trade-creation"
      via: "同一個 Phase 的兩半，收尾必須兩邊都跑（judgment §8）"
      pattern: "mvnw|npm test"
---

<objective>
Phase 4 的收尾閘門：跑完 judgment §8 要求的**跨 repo 四項驗證**，逐條稽核六個 TRAD 需求的證據，並由 Yuan 做一次人工的雙 mode 視覺與互動確認。

Purpose：本 Phase 橫跨兩個 git repository，兩邊各自綠**不等於**整體綠 —— 這正是 `judgment.md` §8 存在的理由。而 `04-VALIDATION.md` 明列了一批「Phase 4 不能證明、屬 Phase 5」的項目；若沒有一道刻意的稽核，SUMMARY 很容易寫出 over-claim（例如「使用者可以完整走完下單流程」）。

Output：`04-13-SUMMARY.md`（驗證證據 + 需求對照 + over-claim 稽核 + 人工確認結果）。

**本 plan 不寫任何程式碼。** 若驗證發現缺口，處置是回報給 orchestrator 開 gap-closure，**不是**在本 plan 順手修。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/judgment.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-VALIDATION.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-CONTEXT.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md
</context>

<repo_and_branch>
兩個 repo 都要動到（只讀與跑測試，不改檔）：
- 後端 `D:\end\workspace\java\stock-web-v2`，分支 `feature/phase-04-trade-idempotency`
- 前端 `D:\end\workspace\vue\stock-v2`（真專案根 `vue-app/`），分支 `feature/phase-04-manual-trade-creation`

Docker 必須在跑（後端 IT 依賴 Testcontainers）。
</repo_and_branch>

<tasks>

<task type="auto">
  <name>Task 1：跑完跨 repo 四項驗證並逐條稽核需求證據</name>
  <files>（不修改任何檔案；產出寫進本 plan 的 SUMMARY）</files>
  <read_first>
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-VALIDATION.md（§Sampling Rate 的 Phase gate 四項、§Coverage anchor 的六條需求對照、§Manual-Only Verifications、**§Explicitly Out of Scope for Phase 4**、**§Validation Sign-Off**——第 5 項 feedback latency < 60s 為**刻意不勾**（6 個 backend IT task 受 Testcontainers 啟動 ~3-5min 限制，屬已接受的取捨而非可修正缺口），SUMMARY 必須複述此理由，不得默默視為通過）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-05-SUMMARY.md（後端收尾狀態，特別是併發 IT 的穩定性結論）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-12-SUMMARY.md（前端收尾狀態）
    - ai-docs/judgment.md §8（跨 repo 變更兩邊驗證都要跑）與 §10（同一測試修兩輪還紅就停）
    - .planning/REQUIREMENTS.md（TRAD-01 ~ TRAD-06 的字面）
  </read_first>
  <action>
    **A. 跑滿四項（judgment §8 / `04-VALIDATION.md` §Sampling Rate 的 Phase gate）**，逐項記下 exit code 與測試數：
    1. 後端 `./mvnw test`（全 unit）
    2. 後端 `./mvnw -pl stock-start -am verify`（全 IT，含 `TradingApiIT` / `TransactionsIdempotencyIT` / `TransactionsAppendOnlyIT` / `ErrorHandlingIT` / `BrowserAuthFlowIT`）
    3. 前端 `cd ../../vue/stock-v2/vue-app && npm test && npm run build`
    4. 前端 `cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npm test`

    四項必須在**同一份程式碼狀態**下跑（跑之前先 `git status --short` 兩個 repo，確認工作樹乾淨；不乾淨就先停下來問）。

    **B. 併發 IT 的穩定性複驗**：`./mvnw -pl stock-start -am verify -Dit.test=TradingApiIT` 連跑 **3 次**。若出現任何一次紅，依 04-03/04-05 SUMMARY 記錄的判準處置 —— 切換到方案 E（`pg_advisory_xact_lock`），**不得**調高 timeout、加重試、或放寬斷言（judgment §10）。若真的發生，回報 orchestrator 開 gap-closure，本 plan 不自行修。

    **C. 六條需求的證據對照表**：依 `04-VALIDATION.md` §Coverage anchor，為 TRAD-01 ~ TRAD-06 各填一列，欄位為「需求 → 證明它的測試檔與測試名 → 執行指令 → 該次執行的實際結果」。**每一列的證據必須是可指名的測試**，不得寫「由 X plan 涵蓋」這種無法驗證的說法。

    **D. over-claim 稽核**（本 task 最重要的一步）：逐條檢查 `04-13-SUMMARY.md` 草稿與**所有既有的 04-NN-SUMMARY.md**，確認沒有任何一處宣稱覆蓋了 `04-VALIDATION.md` §Explicitly Out of Scope 列出的項目：
    - 真實瀏覽器的完整流程（login → `/me` → portfolio reads → create trade → refetch → logout）→ **VER-03 / Phase 5**
    - 真實 cookie / CSRF 在瀏覽器中的行為（SameSite / Secure / HttpOnly 實際生效）→ **Phase 5**
    - 前端 → 後端真實 network call 的證據（Phase 4 全部 stub `fetch`）→ **Phase 5**
    - 跨 repo 契約在真實 payload 上的一致性（`KlineDto` 的 string vs number）→ **Phase 5**
    - `executedAt` 未來時間驗證 → **draft PR #15**（DP-1 已裁定為 (c)）
    - `TRADE_EXECUTE` 權限缺失 → 403：`Role.USER` 已含該權限、repo 內沒有缺少它的角色 → 必須誠實標為「**由 `@PreAuthorize` 註解保證，無獨立測試**」
    - 走勢圖的視覺正確性 → 人工檢視 / Phase 5

    只要找到任何一句 over-claim，就在 SUMMARY 明確更正，並在回報中列出。

    **E. 決策覆蓋複查**：確認 `04-CONTEXT.md` 的 D-01 ~ D-16 每一條都能指到至少一個已完成的 plan 與其驗收證據。任何一條指不到 → 回報 orchestrator。

    **F. Deferred 未被誤做的複查**：確認以下**沒有**被實作 —— pending order / cancel / partial fill / TIF、批次補登（CSV import）、可用現金 / 日級損益 / 資產分類 / watchlist 的 UI（屬 Phase 04.1）、notifications API 化、多幣別呈現。用 `grep -rniE "pending|partial fill|time.in.force|cashAfter|available_cash" ` 對兩個 repo 的本 Phase 變更檔做一次掃描，把命中逐一判定為「mock mode 既有保留（D-04 允許）」或「違規」。
  </action>
  <verify>
    <automated>./mvnw test && ./mvnw -pl stock-start -am verify</automated>
  </verify>
  <acceptance_criteria>
    - 四項驗證各自的指令輸出與 exit code 已記錄在 SUMMARY，四者皆為 0
    - `./mvnw -pl stock-start -am verify -Dit.test=TradingApiIT` 連跑 3 次皆 exit 0（併發穩定性）
    - 兩個 repo 的 `git status --short` 在跑測試前皆無輸出（工作樹乾淨）
    - SUMMARY 含一張六列的需求證據表，每列都指名了具體的測試檔與測試名
    - SUMMARY 含一張 over-claim 稽核表，七個 out-of-scope 項目各有「已確認未宣稱」的勾選
    - SUMMARY 含 D-01 ~ D-16 的覆蓋對照（16 列）
    - SUMMARY 含 Deferred 掃描的結果與逐一判定
  </acceptance_criteria>
  <done>四項驗證全綠且證據可追溯、六條需求各有指名測試、零 over-claim、16 條決策全覆蓋、Deferred 未被誤做。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2：Yuan 的雙 mode 人工確認</name>
  <what-built>
    Phase 4 的全部成果：
    - **後端**：`transactions.idempotency_key` + partial unique index（V10）、`POST /api/v1/trades` 的必填 `Idempotency-Key` header、insert-first 的冪等 `createTrade`、D-07 的 payload 比對與 `TRADE_IDEMPOTENCY_KEY_REUSED`（409）、以及 10 條端到端 IT（含 8 併發同 key 只建 1 列）。
    - **前端**：`marketApi` / `tradingApi` / `portfolioRevision` 三個新模組、重建的 `OrderTicket.vue`（三步驟、真實標的與報價與走勢圖、fee 與成交時間欄位、D-04 隱藏、D-14 key 生命週期、D-15 SELL 預檢、D-16 錯誤分派）、三個 portfolio 頁的 post-trade 重讀與 fresh 高亮。

    自動化已經涵蓋：payload 形狀、header、冪等不變量、錯誤分派、狀態機轉換、a11y 屬性存在性、key 生命週期。
    **自動化涵蓋不到、需要你親眼看的**：版面比例、動畫觀感、文案在實際版位下的可讀性 —— 這些在 `04-VALIDATION.md` §Manual-Only 與 `04-UI-SPEC.md` 已被誠實標為人工項。
  </what-built>
  <how-to-verify>
    **前置**：`cd D:\end\workspace\vue\stock-v2\vue-app`

    **甲、mock mode（預設）** —— `npm run dev`，開啟瀏覽器：
    1. 從任一頁點「記錄交易」開啟 ticket。確認：step dots 是 **3 顆**（不是 4 顆）；焦點自動落在 symbol 輸入框。
    2. 確認**仍然看得到**訂單類型（MKT/LMT）、TIF（DAY/GTC）、交易後現金 —— D-04 明文「mock mode 四樣全部保留」。
    3. 填一筆買進送出。確認：**沒有**「路由撮合」之類的三階段假進度；送出中送出鈕變成「記錄中…」且不可按；成功後顯示「交易已記錄」與交易編號。
    4. 確認 mock 通知仍有推送。

    **乙、API mode** —— 停掉 dev server，改以 `$env:VITE_DATA_MODE='api'; npm run dev`（PowerShell）或 `VITE_DATA_MODE=api npm run dev`（Git Bash）。**需要後端在跑**（`./mvnw -pl stock-start spring-boot:run` 或你慣用的方式）並已登入：
    5. 開啟 ticket，在 symbol 欄位逐字打 `AAP`。確認：下拉出現**真實**的後端標的（不是 `data.ts` 的假清單）；打字期間不會每個字都閃一次列表（250ms debounce 生效）。
    6. 用**鍵盤**（↓ / Enter）選一個標的。確認可以選得到（Phase 3 之前只綁 `@mousedown`，鍵盤使用者選不到）。
    7. 確認報價卡的數字與走勢圖有出現。若走勢圖顯示「無走勢資料」是**正常的**（`market_prices` 需要 backfill 過才有值）—— 重點是**此時送出鈕仍可按**（走勢圖不是交易前提）。
    8. 確認**看不到**訂單類型、TIF、交易後現金，且**沒有留下空白版位**。
    9. 確認手續費預設 `0` 且下方有常駐說明；成交時間預設現在且下方有常駐說明。
    10. 送出一筆。確認成功畫面的價格／數量是**後端回傳值**，且交易編號是完整 36 字元 UUID（沒有被截斷）。
    11. 切到 Positions 與 Trades 頁。確認資料已更新，且剛成交的那一列有高亮與「新」標記。
    12. 回到 ticket 送一筆 SELL。確認顯示「可賣數量：N」；把數量打超過 N，確認出現錯誤且送出鈕不可按。
    13. 把瀏覽器切成離線（DevTools → Network → Offline）再送出。確認 ticket 底部出現「無法連線到伺服器，這筆交易可能尚未記錄。直接再送出一次不會建立重複交易。」，且下方有 error code 與追蹤 ID 一列。
    14. 在 320px 寬度（DevTools 手機模式）下看第 13 步的錯誤區塊。確認 code 與追蹤 ID **完整換行顯示**，沒有水平滾動。

    **丙、雙語**：把語言切成 English 再走一次第 3、10、13 步，確認按鈕文字不溢出、文案沒有出現 `Place order` / `Filled` / `Routing` / `Avg fill` 之類的字眼。
  </how-to-verify>
  <resume-signal>
    輸入「approved」表示通過；或逐條描述哪一步與預期不符（請帶步驟編號）。

    若你在第 5~14 步發現的是**實作缺陷**，我會回報 orchestrator 開 gap-closure plan；若你發現的是**設計想法**（例如「走勢圖希望改成日線」），我會記進 Deferred 而不是在 Phase 4 順手改。
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 「兩個 repo 各自綠」→「整體可用」 | 這一步不成立是 judgment §8 存在的理由，也是 Phase 5 存在的理由 |
| SUMMARY 的宣稱 → 未來 session 的信任 | over-claim 的 SUMMARY 會讓後續 phase 建立在錯誤前提上 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Repudiation | 併發冪等的穩定性 | mitigate | Task 1 B 的三次連跑複驗；偶發紅即切方案 E（advisory lock），不得靠調參數遮掉（judgment §10） |
| T-04-08 | Tampering / Repudiation | V8 append-only trigger | mitigate | `./mvnw -pl stock-start -am verify` 必然涵蓋 `TransactionsAppendOnlyIT` 三條回歸 |
| T-04-06 | Tampering / Spoofing | CSRF | accept（既有控制已覆蓋） | `BrowserAuthFlowIT` 在全 IT 回歸中；真實瀏覽器的 cookie/CSRF 行為明確歸屬 **Phase 5**，本 plan 的稽核步驟 D 會確保沒有人宣稱它已被覆蓋 |
| T-04-05 | Elevation of Privilege | `TRADE_EXECUTE` 權限 | accept（誠實標示） | `Role.USER` 已含該權限，repo 內沒有缺少它的角色可供測試 → 稽核步驟 D 強制標為「由 `@PreAuthorize` 保證，無獨立測試」，**不得**假裝覆蓋 |
| — | Repudiation | SUMMARY 的 over-claim | mitigate | Task 1 步驟 D 的七項 out-of-scope 逐條稽核；`04-VALIDATION.md` 明文「plan 若在任何 task 的 verification 裡寫『使用者可以完整走完下單流程』，那是 over-claim」 |
| — | Tampering | Deferred 項目被誤做 | mitigate | Task 1 步驟 F 的 grep 掃描，逐一判定「mock mode 既有保留（D-04 允許）」或「違規」 |
| T-04-SC | Tampering | npm/maven installs | accept | 整個 Phase 4 零新增依賴（`04-RESEARCH.md` §Package Legitimacy Audit）；Task 1 應順帶確認兩個 repo 的 `package.json` / `pom.xml` 除了 04-04 的兩個 Spring Boot 官方 test starter 之外沒有新增項 |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**文件**
- `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-13-SUMMARY.md`，內含：
  - 跨 repo 四項驗證的指令輸出與 exit code
  - 併發 IT 三次連跑的結果
  - TRAD-01 ~ TRAD-06 的六列證據表（每列指名測試檔 + 測試名）
  - 七項 out-of-scope 的 over-claim 稽核表
  - D-01 ~ D-16 的 16 列覆蓋對照
  - Deferred 掃描結果
  - Yuan 的人工確認結論

**不產生任何程式碼變更。**
</artifacts_this_phase_produces>

<verification>
- 四項驗證指令皆 exit 0，且在同一份乾淨的程式碼狀態下執行
- 併發 IT 三次連跑穩定
- 六條需求各有指名證據
- 零 over-claim
- Yuan 回覆 `approved`

**本 plan 本身即是「不 over-claim」的執行者** —— 若任一項不成立，正確的處置是如實記錄並回報 orchestrator，**不是**放寬標準讓 Phase 看起來完成了。
</verification>

<success_criteria>
- 後端 unit + IT、前端兩個 mode 的測試與 build 四項全綠
- 每一條 TRAD 需求都能指到具體的測試證據
- Phase 4 不能證明的事情已明確歸屬到 Phase 5 / PR #15
- Yuan 親眼確認雙 mode 行為符合 UI 契約
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-13-SUMMARY.md`。

SUMMARY 額外必須記錄：
1. **兩個 repo 的收尾狀態**：各自的分支名、最後一個 commit、是否已 push、是否已開 PR。Phase 3 曾因「STATE.md 記載與現實不符」而產生過對帳成本，本次必須逐一實查 `git branch -r --contains` 並記錄。
2. Phase 5（VER-01 ~ VER-03）將接手的完整清單。
3. 若併發 IT 曾切換到方案 E，記錄切換理由與新的實作形狀。
</output>
