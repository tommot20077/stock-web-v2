---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 07
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - "[FE] src/services/tradingApi.ts"
  - "[FE] src/services/tradingApi.test.ts"
  - "[FE] src/services/portfolioRevision.ts"
  - "[FE] src/services/portfolioRevision.test.ts"
autonomous: true
requirements: [TRAD-01, TRAD-02, TRAD-05]

must_haves:
  truths:
    - "前端有一個只送後端 CreateTradeRequest 七個合約欄位的 createTrade 方法，不含 ordType / tif / orderId / cashAfter / slippage"
    - "createTrade 必定帶 Idempotency-Key header，且該 header 值由呼叫端決定（不是 adapter 自產）"
    - "401 refresh + replay 會沿用同一個 Idempotency-Key（避免 refresh 前那次已成功的交易被重複建立）"
    - "mock mode 的 createTrade 委派 mock store 的 executeOrder，並在 oversell 時丟出與 API mode 同形的 ApiClientError"
    - "交易建立成功後有一個共用訊號可以同時提供「要重讀」「哪一列是新的」「新交易的 id」三件事"
  artifacts:
    - path: "[FE] src/services/tradingApi.ts"
      provides: "trading domain adapter 三件組 + CreateTradeRequest 契約型別"
      exports: ["TradingApi", "CreateTradeRequest", "createTradingApi", "createHttpTradingApi", "createMockTradingApi"]
    - path: "[FE] src/services/portfolioRevision.ts"
      provides: "D-10 revision counter + D-13 API mode lastFill + D-11 lastCreatedTradeId"
      exports: ["portfolioRevision", "apiLastFill", "lastCreatedTradeId", "notifyTradeCreated", "resetPortfolioRevisionForTests"]
  key_links:
    - from: "[FE] src/services/tradingApi.ts"
      to: "POST /api/v1/trades"
      via: "apiRequest + headers['Idempotency-Key']"
      pattern: "'Idempotency-Key'"
    - from: "[FE] src/services/portfolioRevision.ts"
      to: "三個 portfolio 頁的 watch"
      via: "模組級 readonly ref"
      pattern: "export const portfolioRevision"
---

<objective>
建立交易送出的兩個 client 端原語：**`tradingApi.ts`**（把交易送到後端，帶 D-05 的必填 header）與 **`portfolioRevision.ts`**（成交後廣播給已掛載的 portfolio 頁）。

Purpose：兩者是同一個生命週期的兩端 —— 一次成功的 `createTrade` 必定緊接著一次 `notifyTradeCreated`，而後者同時產生「要重讀」「哪一列是新的」「新交易的 id」三個訊號。分成兩個 plan 會讓「先 bump 還是先 set lastFill」變成需要跨 plan 協調的順序問題。

Output：`tradingApi.ts` 三件組、`portfolioRevision.ts`、兩支測試檔。

**依賴 04-01**：`TRADE_IDEMPOTENCY_KEY_REUSED` 的字面已在後端定案，本 plan 的測試會用到它。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/judgment.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-CONTEXT.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-PATTERNS.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-01-SUMMARY.md
</context>

<repo_and_branch>
sibling 前端 repo `D:\end\workspace\vue\stock-v2`（真專案根 `vue-app/`），分支 `feature/phase-04-manual-trade-creation`（從 `origin/develop` 開，承 04-06）。`[FE]` 前綴表示相對於 `vue-app/`。
</repo_and_branch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：tradingApi.ts 三件組（HTTP + mock）</name>
  <files>
    [FE] src/services/tradingApi.test.ts,
    [FE] src/services/tradingApi.ts
  </files>
  <read_first>
    - [FE] src/services/opsApi.ts（**`:142-165` 是全 repo 唯一的「POST + `Idempotency-Key` header」前例** —— `const headers: Record<string,string> = {}` → `headers['Idempotency-Key'] = ...` → `apiRequest(path, { method: 'POST', headers, json })`）
    - [FE] src/services/portfolioApi.ts（`:39-46` 介面 javadoc 語氣、`:47-54` interface 形狀、`:98-113` `tradeDtoFrom` 的 mock DTO 合成慣例、`:140-202` 三件組、`:141-142` 「延遲解析 store」的註解與理由、`:150-152` `live` getter）
    - [FE] src/services/apiClient.ts（`apiRequest`（:328）；`ApiClientError`（:3，含 `status` / `code` / `requestId` / `fields`）；:271-297、:299-326 的單飛 refresh 與**一次 replay** —— replay 用**同一份 options** 重打，所以同一把 key 自動沿用）
    - [FE] src/services/apiTypes.ts（`TradeDto` 的 11 個欄位）
    - [FE] src/stores/mockPortfolio.ts（`executeOrder` 的參數形狀與回傳值、`lastFill` 的形狀）
    - [FE] src/stores/mockNotifications.ts（`pushNotification` 簽章）
    - [FE] src/components/OrderTicket.vue（`:377-386` 現有的 `executeOrder` 呼叫、`:393-399` 現有的通知推送 —— 這兩段要**搬進** mock adapter）
    - [FE] src/services/opsApi.test.ts（`:29`、`:118` 已有 idempotencyKey 的斷言樣板）
    - [FE] src/services/portfolioApi.test.ts（`jsonResponse` helper、pinia/stubGlobal 的 setup/teardown）
  </read_first>
  <behavior>
    - Test 1（**TRAD-01/02 的核心斷言**）：stub `fetch`，呼叫 `createHttpTradingApi().createTrade(req, 'key-1')` → 攔截 `RequestInit.body`，`Object.keys(JSON.parse(body)).sort()` **`toEqual`** `['executedAt','fee','note','price','quantity','symbol','type']`。**必須用 `toEqual` 比 sorted key 陣列，不是 `toMatchObject`** —— 後者不會抓到多送的欄位。
    - Test 2（TRAD-02 冗餘保險）：`expect(body).not.toHaveProperty('ordType')`、`'tif'`、`'orderId'`、`'cashAfter'`、`'slippage'`、`'side'`（讓失敗訊息直接指出是哪一個）。
    - Test 3：`RequestInit.method === 'POST'`，`headers` 含 `'Idempotency-Key': 'key-1'`，URL 為 `/api/v1/trades`。
    - Test 4（**Q5.4 / D-14 的免費紅利**）：stub `fetch` 讓第 1 次回 401、`/auth/refresh` 回 200、replay 回 200 → 斷言 replay 那次 `fetch` 的 `init.headers` 的 `Idempotency-Key` 與第 1 次**相同**。這條證明「refresh 前那次可能已成功的交易不會被重複建立」。
    - Test 5：後端回 409 `TRADE_IDEMPOTENCY_KEY_REUSED` → reject 成 `ApiClientError`，`error.code` 為該字串、`error.status` 為 409、`error.requestId` 來自 `meta.traceId`。adapter **不 catch、不重包**。
    - Test 6：後端回 400 `VALIDATION_FAILED` 且 `fields:{quantity:'must be...'}` → `error.fields.quantity` 可讀（D-16 依賴這個）。
    - Test 7（mock）：`createMockTradingApi().createTrade(req, 'key-1')` 委派 `useMockPortfolioStore().executeOrder(...)`，回傳一個 `TradeDto` 形狀的物件（`id` / `symbol` / `type` / `quantity` / `price` / `fee` / `note` / `executedAt` / `createdAt` 齊全），且**不發任何 fetch**。
    - Test 8（mock oversell）：讓 `executeOrder` 回 falsy → `createTrade` **reject 成 `ApiClientError`**，`code === 'TRADE_INSUFFICIENT_HOLDING'`、`status === 409`。這讓 OrderTicket 的錯誤處理在兩個 mode 只有一條路徑。
    - Test 9（mock）：成功後 `live.lastFill` 反映 store 的 `lastFill`；且有推一則 mock 通知（`useMockNotificationsStore` 的 spy 被呼叫）。
    - Test 10：`createTradingApi('api').live === undefined`；`createTradingApi('mock').live` 存在（分支判斷依據是 `live` 的存在性，不是 mode 字串）。
  </behavior>
  <action>
    RED 先行：先完整寫 `tradingApi.test.ts`（模組不存在 → import 失敗），再實作。

    新建 `[FE] src/services/tradingApi.ts`：

    **`export interface CreateTradeRequest`** —— 定義在**本檔**（不放 `apiTypes.ts`，避免與 04-06 的 `apiTypes.ts` 產生檔案衝突；且它是 request 型別而非 response DTO）。七個欄位逐字與後端 `CreateTradeRequest.java` 同形：`symbol: string`、`type: 'BUY' | 'SELL'`、`quantity: number`、`price: number`、`fee: number`（D-02：使用者手動輸入，預設 0，前端**不發明費率**）、`note: string | null`、`executedAt: string`（D-03：ISO-8601 含 offset）。javadoc 必須寫「逐欄同形，勿增刪（judgment §4）」與 D-02/D-03 的理由。

    **`export interface TradingLiveMockData { readonly lastFill: { sym: string; type: 'BUY' | 'SELL'; qty: number; px: number } | null }`** —— 形狀與 `portfolioApi.ts:36` 的 `PortfolioLiveMockData.lastFill` **逐字一致**。

    **`export interface TradingApi`** —— `mode: RuntimeDataMode`、`createTrade(request: CreateTradeRequest, idempotencyKey: string): Promise<TradeDto>`、`live?: TradingLiveMockData`。`createTrade` 的 javadoc 必須寫「@param idempotencyKey D-05：必填。同一次送出嘗試的重試必須沿用同一個 key（D-14）」。**key 是參數不是 adapter 自產** —— D-14 的生命週期規則屬於 UI 層（哪一次是「同一次嘗試」只有 UI 知道）。

    **`createHttpTradingApi(basePath = '/api/v1')`** —— 照 `opsApi.ts:146-154` 的形狀，差別只有 key 是必填（不做 `if`）：`apiRequest<TradeDto>(`${basePath}/trades`, { method: 'POST', headers: { 'Idempotency-Key': idempotencyKey }, json: request })`。
    - 用 `apiRequest` 而**不是** `apiPaginatedRequest`（`POST /trades` 回 `ApiResponse<TradeDto>`）。
    - **不需要任何 transport 層擴充**：CSRF `X-XSRF-TOKEN`、`credentials: 'include'`、401 單飛 refresh + 一次 replay 全部由 `apiClient` 處理（Phase 2 D-20）。**不得**在此檔出現 `fetch(`、`credentials`、`X-XSRF-TOKEN`。

    **`createMockTradingApi()`** —— 把 `OrderTicket.vue:377-386`（`executeOrder`）與 `:393-399`（`pushNotification`）兩段**搬進來**：
    - store 一律**延遲解析**（每次呼叫才 `useMockPortfolioStore()`，照 `portfolioApi.ts:141-142` 的註解理由：reactivity 與跨測試隔離）。
    - `executeOrder` 需要 `sector`，但 `CreateTradeRequest` 刻意**不含** `sector`（保持與後端逐欄同形）→ mock 實作自己從 `data.ts` 用 symbol 反查 sector。**髒活留在 mock 裡，介面契約保持乾淨。**
    - `executeOrder` 回 falsy（oversell）時 `throw new ApiClientError({ status: 409, code: 'TRADE_INSUFFICIENT_HOLDING', message: '...', requestId: null })`（`ApiClientError` 是 `apiClient.ts:3` 的 exported class，mock 用它完全合理）。
    - 成功後合成 `TradeDto`（`id` 用 `mock-` 前綴，照 `portfolioApi.ts:98-113` 的 `tradeDtoFrom` 慣例）。**絕不**用 `Math.random()` 產生 id 或成交價（U-16 已裁定移除）。
    - 推一則 mock 通知（D-04：mock mode 保留通知推送；API mode 不推）。
    - `live.lastFill` 用 getter 讀 `useMockPortfolioStore().lastFill`（照 `portfolioApi.ts:150-152`）。
    - mock 實作**可以** import mock store（judgment §3 只禁**元件** import mock store；`portfolioApi.ts:2` 本身就這樣做）。

    **`createTradingApi(mode, basePath = '/api/v1')`** —— `mode === 'api' ? createHttpTradingApi(basePath) : createMockTradingApi()`。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npx vitest run src/services/tradingApi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/services/tradingApi.ts` export `TradingApi`、`CreateTradeRequest`、`createTradingApi`、`createHttpTradingApi`、`createMockTradingApi`
    - 該檔含 `'Idempotency-Key'` 與 `method: 'POST'`
    - 該檔**不含** `fetch(`、`credentials`、`X-XSRF-TOKEN`、`Math.random`
    - 該檔的 `CreateTradeRequest` 恰好七個欄位，且**不含** `ordType`、`tif`、`orderId`、`cashAfter`、`slippage`、`sector`
    - `[FE] src/services/tradingApi.test.ts` 含 sorted key 陣列的 `toEqual` 斷言（不是 `toMatchObject`）
    - `cd ../../vue/stock-v2/vue-app && npx vitest run src/services/tradingApi.test.ts` exit 0，測試數 ≥ 10
  </acceptance_criteria>
  <done>payload 恰為七個合約欄位、header 必帶、401 replay 沿用同一 key、mock oversell 丟同形錯誤、兩種 factory 的 live 差異正確。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：portfolioRevision.ts（D-10 revision + D-13 lastFill + D-11 tradeId）</name>
  <files>
    [FE] src/services/portfolioRevision.test.ts,
    [FE] src/services/portfolioRevision.ts
  </files>
  <read_first>
    - [FE] src/services/pageApiClients.ts（**模組級 singleton + 顯式測試 reset 的既有慣例**：`:19` `let clients = null`、`:21-35` getter、`:37-39` `resetRuntimeApiClientsForTests()`）
    - [FE] src/services/apiClient.ts（`:62-67` 另一個模組級可變狀態 + 設定函式的前例）
    - [FE] src/services/portfolioApi.ts（`:36` `PortfolioLiveMockData.lastFill` 的**逐字形狀** `{ sym, type, qty, px }` —— 新的 `LastFill` 必須與它一致）
    - [FE] src/services/apiTypes.ts（`TradeDto` 的欄位名：`id` / `symbol` / `type` / `quantity` / `price`）
    - [FE] src/testSetup.ts（`:10-12` 的註解：**刻意不 import 任何 service module**，因為會搶在 `vi.mock` 生效前綁定真實實作）
    - [FE] src/services/runtimeDataMode.test.ts（模組級狀態 + 各測試檔自行 reset 的樣板）
    - [FE] vite.config.ts（`:26-27` `pool: 'threads'` + `fileParallelism: false` —— 同一測試檔內的多個測試共用模組狀態）
  </read_first>
  <behavior>
    - Test 1：`portfolioRevision.value` 初值為 0；`bumpPortfolioRevision()` 之後為 1，再呼叫為 2。
    - Test 2（**單一原子動作**）：`notifyTradeCreated(tradeDto)` 之後，`portfolioRevision.value` 加 1、`apiLastFill.value` 等於 `{ sym: trade.symbol, type: trade.type, qty: trade.quantity, px: trade.price }`、`lastCreatedTradeId.value` 等於 `trade.id`。三件事在同一次呼叫內完成，**沒有順序協調問題**。
    - Test 3（唯讀性）：`portfolioRevision` / `apiLastFill` / `lastCreatedTradeId` 是 `readonly` ref，外部直接賦值在型別層被拒（用 `@ts-expect-error` 斷言，`npm run build` 會驗）。
    - Test 4（測試隔離）：`resetPortfolioRevisionForTests()` 把三個值歸回 `0` / `null` / `null`。
    - Test 5（D-11 的清除）：`clearLastCreatedTrade()` 只清 `lastCreatedTradeId`，**不動** `portfolioRevision` 也**不動** `apiLastFill`（提示訊息的壽命與 fresh 高亮的壽命不同）。
    - Test 6（**Pitfall 13 跨測試污染**）：連續兩個測試各自 bump，第二個測試在 `afterEach` reset 之後看到的初值是 0 而不是 1。
  </behavior>
  <action>
    RED 先行：先寫 `portfolioRevision.test.ts`（模組不存在 → import 失敗），再實作。

    新建 `[FE] src/services/portfolioRevision.ts`，比照 `pageApiClients.ts:19,37-39` 的**模組級 singleton + 顯式測試 reset**：

    - `import { readonly, ref, type Ref } from 'vue'`
    - 私有 `const revision = ref(0)`、`const lastFill = ref<LastFill | null>(null)`、`const lastTradeId = ref<string | null>(null)`
    - `export interface LastFill { sym: string; type: 'BUY' | 'SELL'; qty: number; px: number }` —— **形狀與 `portfolioApi.ts:36` 的 mock `lastFill` 逐字一致**。這是為了讓兩頁的模板寫成 `const effectiveLastFill = computed(() => live ? live.lastFill : apiLastFill.value)`，**fresh class 的綁定表達式完全不用改，只是來源換了**（最小改動路徑）。
    - `export const portfolioRevision: Readonly<Ref<number>>`、`export const apiLastFill: Readonly<Ref<LastFill | null>>`、`export const lastCreatedTradeId: Readonly<Ref<string | null>>` —— 全部 `readonly(...)`。
    - `export function notifyTradeCreated(trade: TradeDto): void` —— 同時設 `lastFill`、`lastTradeId`，並 `revision.value += 1`。
    - `export function bumpPortfolioRevision(): void` —— 只提升 revision（給不需要 lastFill 的情境備用）。
    - `export function clearLastCreatedTrade(): void` —— 只把 `lastTradeId` 設 `null`（D-11 的提示清除時機：使用者變更篩選／排序／頁碼，或再次開啟 ticket）。
    - `export function resetPortfolioRevisionForTests(): void` —— 三個都歸零（比照 `resetRuntimeApiClientsForTests`）。

    **為什麼不用 Pinia**：`src/stores/` 底下全是 `mock*` 前綴的 mock 專用 store，把跨 mode 的協調狀態放進去會模糊 judgment §3 的界線（「mock store 是 mock mode 專屬」）。模組級 singleton 是 `pageApiClients.ts` 與 `apiClient.ts` 已在用的慣例。

    **為什麼三個訊號放同一個模組**：一次成交同時產生「要重讀」「哪一列是新的」「新交易的 id」三個訊號，分模組會讓「先 bump 還是先 set lastFill」變成需要協調的順序問題。

    檔案頂端的繁中 javadoc 必須寫出 D-10 的核心論證：`App.vue:36` 用 `v-if` 切頁，**非當前頁是卸載的**；而 OrderTicket 是全域 overlay（`App.vue:52-59`），使用者可能在 Markets/Chart 頁下單，此時三個 portfolio 頁全部未掛載。所以「成功後一律打三個 domain 請求」在目前架構下是純粹的無效工（沒有 client-side cache 可放，沒掛載的頁沒人消費）。

    **⚠️ 測試隔離硬規則**：`resetPortfolioRevisionForTests()` 必須在**各測試檔自己**的 `afterEach` 呼叫，**絕對不得**加進 `testSetup.ts`（該檔 `:10-12` 明文說明：import service module 會搶在 `vi.mock` 生效前綁定真實實作）。這條規則要寫進 `portfolioRevision.ts` 的 javadoc，讓後續 plan 的 executor 看得到。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npx vitest run src/services/portfolioRevision.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/services/portfolioRevision.ts` export `portfolioRevision`、`apiLastFill`、`lastCreatedTradeId`、`notifyTradeCreated`、`bumpPortfolioRevision`、`clearLastCreatedTrade`、`resetPortfolioRevisionForTests`、`LastFill`
    - `LastFill` 的欄位名為 `sym` / `type` / `qty` / `px`（與 `portfolioApi.ts:36` 逐字一致，**不是** `symbol`/`quantity`/`price`）
    - 該檔含 `readonly(` 三次
    - 該檔**不含** `defineStore`、`pinia`
    - `[FE] src/testSetup.ts` **未被修改**（`git diff --name-only` 不含它）
    - `cd ../../vue/stock-v2/vue-app && npx vitest run src/services/portfolioRevision.test.ts` exit 0，測試數 ≥ 6
    - `npm test` exit 0；`VITE_DATA_MODE=api npm test` exit 0；`npm run build` exit 0
  </acceptance_criteria>
  <done>三個唯讀訊號 + 一個原子通知函式存在、形狀與 mock lastFill 逐字一致、測試 reset 未污染 testSetup。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 瀏覽器 JS → `POST /api/v1/trades` | 唯一的不可逆帳本寫入路徑；CSRF 與 session 的攻擊面 |
| idempotency key 的產生與傳遞 | key 若可預測或被重用於不同意圖，會導致「別人的交易被當成你的重試回傳」或「使用者被自己的 key 鎖死」 |
| mock 錯誤 → UI 錯誤處理 | mock 與 API 的錯誤形狀若不同，UI 會出現只在某一 mode 才走到的分支 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering / Spoofing | `POST /trades` 的 CSRF | accept（既有控制已覆蓋） | `apiClient.ts:220-223` 對所有 unsafe method 自動注入 `X-XSRF-TOKEN`（`UNSAFE_METHODS` 含 `POST`，`:60`）。**本 plan 明文禁止另造轉接層**（acceptance criteria 已用 grep 強制 `tradingApi.ts` 不含 `fetch(`／`X-XSRF-TOKEN`） |
| T-04-01 | Tampering / Repudiation | 401 replay 的 key | mitigate | Test 4 斷言 replay 沿用同一把 key。若 replay 換 key，refresh 前那次**可能已成功**的交易會被重複建立 —— 這是本 Phase 冪等設計中最容易被忽略的一條 |
| T-04-10 | Spoofing | key 的隨機來源 | mitigate | key 由呼叫端（04-11 的 OrderTicket）用 `crypto.randomUUID()`（Web Crypto，CSPRNG）產生。本 plan 的 adapter **不自產 key**，也不得提供 `Math.random()` 的預設值。`(user_id, key)` 的 user_id 維度另使碰撞影響限縮在同一使用者內 |
| T-04-09 | Information Disclosure | adapter 的錯誤處理 | mitigate | adapter **不 catch、不重包**，讓 `ApiClientError`（`code` / `status` / `requestId` / `fields`）原樣往上拋。後端 `message` 由 UI 層負責不顯示（`04-UI-SPEC.md` §Security & Diagnostics） |
| — | Tampering | payload 夾帶未支援欄位 | mitigate | Test 1 的 sorted key 陣列 `toEqual` 斷言是 TRAD-02 的機械化保證：多送一個欄位測試就紅。`CreateTradeRequest` 型別本身也不含 `ordType`/`tif`/`orderId` |
| — | Information Disclosure | mock 與 API 的錯誤形狀不一致 | mitigate | mock oversell 一律丟 `ApiClientError` with `code: 'TRADE_INSUFFICIENT_HOLDING'`，讓 D-16 的錯誤呈現在兩個 mode 走同一條路徑（少一條只在某 mode 才測得到的分支） |
| T-04-SC | Tampering | npm installs | accept | 零新增依賴。key 用內建 `crypto.randomUUID()`（**不得**裝 `uuid`）；日期用 `Date.prototype.toISOString()` 或既有 `Trades.vue:218-226` 的 `toLocalIso` helper（**不得**裝 `date-fns`/`dayjs`） |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**TypeScript 匯出（`src/services/tradingApi.ts`）**
- `interface CreateTradeRequest`（七欄：symbol / type / quantity / price / fee / note / executedAt）
- `interface TradingLiveMockData`
- `interface TradingApi`
- `createTradingApi(mode, basePath?)`
- `createHttpTradingApi(basePath?)`
- `createMockTradingApi()`

**TypeScript 匯出（`src/services/portfolioRevision.ts`）**
- `interface LastFill`（`{ sym, type, qty, px }`）
- `const portfolioRevision: Readonly<Ref<number>>`
- `const apiLastFill: Readonly<Ref<LastFill | null>>`
- `const lastCreatedTradeId: Readonly<Ref<string | null>>`
- `notifyTradeCreated(trade: TradeDto): void`
- `bumpPortfolioRevision(): void`
- `clearLastCreatedTrade(): void`
- `resetPortfolioRevisionForTests(): void`

**新檔案**
- `[FE] src/services/tradingApi.ts`、`[FE] src/services/tradingApi.test.ts`
- `[FE] src/services/portfolioRevision.ts`、`[FE] src/services/portfolioRevision.test.ts`

**消費的 HTTP 契約（後端由 04-01~04-05 提供）**
- `POST /api/v1/trades`，必填 request header `Idempotency-Key`
- 錯誤 code：`TRADE_IDEMPOTENCY_KEY_REUSED`、`TRADE_INSUFFICIENT_HOLDING`、`TRADE_CONFLICT`、`ASSET_NOT_FOUND`、`VALIDATION_FAILED`
</artifacts_this_phase_produces>

<verification>
- `npx vitest run src/services/tradingApi.test.ts src/services/portfolioRevision.test.ts` 綠
- `npm test` 綠、`VITE_DATA_MODE=api npm test` 綠、`npm run build` 綠
- `grep -rn "fetch(" src/services/tradingApi.ts src/services/portfolioRevision.ts` 無輸出

**不得宣稱覆蓋**：真實 network call 的證據（測試全部 stub `fetch`）—— judgment §3 的「API mode 功能驗證必須看到真實 network calls」屬 **Phase 5 / VER-03**。
</verification>

<success_criteria>
- payload 恰為後端 `CreateTradeRequest` 的七個欄位，多一個就測試紅
- `Idempotency-Key` 必帶，且 401 replay 沿用同一把
- mock 與 API 的成功與失敗形狀一致（OrderTicket 只需一條路徑）
- 一次 `notifyTradeCreated` 同時產生三個訊號，無順序協調問題
- 零新增 npm 依賴、零繞過 `apiClient`、`testSetup.ts` 未被污染
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-07-SUMMARY.md`。

SUMMARY 必須記錄：
1. Q5.4 的「401 replay 自動沿用同一 key」在測試中的實際驗證結果（這是 D-14 的免費紅利，也是安全上必要的行為）。
2. mock 的 `sector` 反查解法（`CreateTradeRequest` 保持與後端逐欄同形，髒活留在 mock 內）。
3. `LastFill` 的欄位名刻意用 `{ sym, type, qty, px }` 與 mock 逐字一致的理由（兩頁的 fresh 綁定表達式不用改）。
4. `resetPortfolioRevisionForTests` **不得**加進 `testSetup.ts` 的理由與規則，供後續 plan 的 executor 遵循。
</output>
