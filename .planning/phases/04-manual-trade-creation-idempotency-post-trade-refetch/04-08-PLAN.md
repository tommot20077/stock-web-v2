---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 08
type: execute
wave: 3
depends_on: ["04-06", "04-07"]
files_modified:
  - "[FE] src/services/pageApiClients.ts"
  - "[FE] src/api-adapter-wiring.test.ts"
  - "[FE] src/i18n.ts"
  - "[FE] src/i18n.test.ts"
autonomous: true
requirements: [TRAD-02, TRAD-06]

must_haves:
  truths:
    - "元件可以用 getRuntimeApiClients().trading 與 .market 取得對應 adapter，不需要 import 任何 mock store"
    - "API mode 下 trading 與 market 的 mock factory 一次都沒有被呼叫（不會靜默回退 mock）"
    - "Phase 4 需要的每一個文案 key 在 zh 與 en 兩邊都存在，且 t() 回傳值不等於 key 本身"
    - "沒有為既有語意重複新增同義 key（loading / loadFailed / authRetry / authRequestId / cancel / fee / qty / price / notes 一律複用）"
  artifacts:
    - path: "[FE] src/services/pageApiClients.ts"
      provides: "RuntimeApiClients 註冊 trading 與 market"
      contains: "trading: createTradingApi(mode, basePath)"
    - path: "[FE] src/i18n.ts"
      provides: "Phase 4 的 zh/en 文案"
      contains: "tradeErrKeyReused"
    - path: "[FE] src/api-adapter-wiring.test.ts"
      provides: "API mode 不回退 mock 的防線（Phase 2 D-20）"
      contains: "mockFactoryCalls.trading"
  key_links:
    - from: "[FE] src/services/pageApiClients.ts"
      to: "[FE] src/services/tradingApi.ts"
      via: "factory 註冊"
      pattern: "createTradingApi"
    - from: "[FE] src/services/pageApiClients.ts"
      to: "[FE] src/services/marketApi.ts"
      via: "factory 註冊"
      pattern: "createMarketApi"
---

<objective>
把 04-06 / 04-07 的兩個新 adapter 接進 runtime 客戶端工廠，並一次補齊 Phase 4 全部的 zh/en 文案。

Purpose：`OrderTicket.vue` 的重建（04-09 ~ 04-11）與三頁的 refetch（04-12）都需要這兩樣東西就位才寫得出來；把它們集中在一個 plan 完成，可避免後續每個 plan 都零星改 `i18n.ts` 造成的檔案衝突與漏加一邊語言。

Output：`pageApiClients.ts` 的 `trading` / `market` 欄位、`api-adapter-wiring.test.ts` 的三處擴充、`i18n.ts` 的 ~42 個新 key（zh + en）、`i18n.test.ts` 的 key 清單。

**本 plan 不改任何元件** —— 純 wiring 與文案。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/judgment.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-PATTERNS.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-06-SUMMARY.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-07-SUMMARY.md
</context>

<repo_and_branch>
sibling 前端 repo `D:\end\workspace\vue\stock-v2`（真專案根 `vue-app/`），分支 `feature/phase-04-manual-trade-creation`。`[FE]` 前綴表示相對於 `vue-app/`。
</repo_and_branch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：pageApiClients 註冊 trading / market + 不回退 mock 的防線</name>
  <files>
    [FE] src/api-adapter-wiring.test.ts,
    [FE] src/services/pageApiClients.ts
  </files>
  <read_first>
    - [FE] src/services/pageApiClients.ts（全檔 39 行：`:1-7` import 區、`:9-17` `interface RuntimeApiClients`（**未 export**，所以加欄位不會產生外部型別破壞）、`:19` singleton、`:21-35` getter 與快取 key（`mode` + `basePath`）、`:37-39` reset）
    - [FE] src/api-adapter-wiring.test.ts（**全檔**：`:16-22` `vi.hoisted` 的 `mockFactoryCalls` 物件、`:24-36` `afterEach` 的 reset 與 `vi.doUnmock`、`:166-220` `'API mode creates HTTP clients and never calls mock adapter factories'` 的三處斷言）
    - [FE] src/services/tradingApi.ts（04-07 的 `createTradingApi` 簽章）
    - [FE] src/services/marketApi.ts（04-06 的 `createMarketApi` 簽章）
  </read_first>
  <behavior>
    - Test 1：`VITE_DATA_MODE=api` 下 `getRuntimeApiClients().trading.mode === 'api'` 且 `.live === undefined`；`.market.mode === 'api'`。
    - Test 2（**Phase 2 D-20 的防線**）：同一情境下 `mockFactoryCalls.trading` 與 `mockFactoryCalls.market` **一次都沒被呼叫**（`expect(...).not.toHaveBeenCalled()`）。靜默回退 mock 在此變成測試失敗。
    - Test 3：mock mode 下 `.trading.live` 存在、`.market.mode === 'mock'`。
    - Test 4：`resetRuntimeApiClientsForTests()` 之後重新取得 clients，兩個新 adapter 也跟著重建（快取 key 邏輯未被破壞）。
    - 回歸：`api-adapter-wiring.test.ts` 既有的五個 adapter（auth / aiAccess / backtest / ops / portfolio）斷言全綠。
  </behavior>
  <action>
    RED 先行：先在 `api-adapter-wiring.test.ts` 加上述斷言 —— `clients.trading` 為 `undefined` 會同時產生型別錯誤與執行期失敗，這就是 RED。

    **`api-adapter-wiring.test.ts` 的三處擴充**（照該檔既有結構，不要另造新檔）：
    1. `:16-22` 的 `vi.hoisted` 物件加 `trading: vi.fn()` 與 `market: vi.fn()`。
    2. `:24-36` 的 `afterEach` 加 `mockFactoryCalls.trading.mockReset()` / `mockFactoryCalls.market.mockReset()` 與 `vi.doUnmock('./services/tradingApi')` / `vi.doUnmock('./services/marketApi')`。
    3. `:166-220` 的斷言區加上 Test 1~3 的內容。

    **`pageApiClients.ts` 的三處改動**（全檔只有 39 行，改動極小）：
    1. import 區加 `import { createTradingApi, type TradingApi } from './tradingApi';` 與 `import { createMarketApi, type MarketApi } from './marketApi';`（放在 `createPortfolioApi` 的 import 之後，維持字母序附近的既有排列）。
    2. `interface RuntimeApiClients` 加兩個欄位：`trading: TradingApi;` 與 `market: MarketApi;`（放在 `portfolio: PortfolioApi;` 之後）。
    3. `getRuntimeApiClients` 的 `clients = { ... }` 物件加兩行：`trading: createTradingApi(mode, basePath),` 與 `market: createMarketApi(mode, basePath),`。

    **不需要改動** `:19` 的 singleton 宣告、`:23` 的快取判斷（key 是 `mode` + `basePath`）、`:37-39` 的 `resetRuntimeApiClientsForTests()`。

    **禁止**：不得在 `pageApiClients.ts` 加入任何 `try/catch` 或「adapter 建不起來就退回 mock」的降級路徑（Phase 2 D-20 / FAPI-07：API mode 不得靜默退回 mock）。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npx vitest run src/api-adapter-wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/services/pageApiClients.ts` 含 `trading: createTradingApi(mode, basePath)` 與 `market: createMarketApi(mode, basePath)`
    - `interface RuntimeApiClients` 含 `trading: TradingApi;` 與 `market: MarketApi;`
    - `pageApiClients.ts` **不含** `catch`、`fallback`、`?? createMock`
    - `[FE] src/api-adapter-wiring.test.ts` 含 `mockFactoryCalls.trading` 與 `mockFactoryCalls.market`，且含對兩者的 `not.toHaveBeenCalled()` 斷言
    - `cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npx vitest run src/api-adapter-wiring.test.ts` exit 0
    - `npx vitest run src/api-adapter-wiring.test.ts` exit 0（mock mode）
  </acceptance_criteria>
  <done>兩個新 adapter 已註冊、API mode 不回退 mock 的斷言就位、既有五個 adapter 回歸綠。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：i18n 新增 Phase 4 全部文案（zh + en）</name>
  <files>
    [FE] src/i18n.test.ts,
    [FE] src/i18n.ts
  </files>
  <read_first>
    - [FE] src/i18n.ts（全檔 283 行：`:4` `export const I18N: Record<Lang, Record<string, string>>` 是**扁平**結構無 nesting；zh 從 `:5` 起、en 從 `:144` 起；命名慣例是扁平 camelCase 語意縮寫；**既有可複用的 key**：`loading`(:131) / `loadFailed`(:132) / `authRetry`(:122) / `authRequestId`(:129) / `cancel`(:49,:187) / `fee` / `qty` / `price` / `symbol` / `side` / `notes` / `selectSymbol` / `viewPositions`）
    - [FE] src/i18n.test.ts（`:6-33` 的 key 陣列迴圈樣板；`:30-33` 的「複用 authRetry 而非新增同義 key」case —— 這條鎖的是慣例，新 key 必須遵守）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §Copywriting Contract（**文案的權威來源，逐字照抄 zh/en 兩欄**）
    - [FE] src/components/OrderTicket.vue（確認 `placeOrder` / `filled` / `avgFillPx` / `routingMatch` / `placing` 五個 key 目前被哪些行引用；本 task **不刪除**它們，只是不再新增同類）
  </read_first>
  <behavior>
    - Test 1（**主要斷言**）：把下方 42 個新 key 列成一個 `PHASE4_TRADE_KEYS` 陣列，迴圈斷言 `I18N.zh[key]` 與 `I18N.en[key]` 皆 truthy，且 `t('zh', key) !== key`、`t('en', key) !== key`（照 `i18n.test.ts:6-33` 的樣板）。漏加任一邊語言會直接紅。
    - Test 2（**不新增同義 key** —— 沿用 `i18n.test.ts:30-33` 的慣例）：斷言 `I18N.zh` **不含** 下列會與既有 key 重複語意的名稱：`tradeLoading`、`tradeLoadFailed`、`tradeRetry`、`tradeTraceId`、`tradeCancel`。它們必須複用既有的 `loading` / `loadFailed` / `authRetry` / `authRequestId` / `cancel`。
    - Test 3（judgment §1 禁止用語）：斷言下列**新增**的 key 其 zh 與 en 文案都**不含**禁止用語 —— en 側不含 `order`（大小寫不敏感，但 `Record trade` 系列本來就沒有）、`pending`、`filled`、`routing`、`cancel order`、`time-in-force`；zh 側不含「下單」「委託」「撮合」「已成交」「成交均價」「有效期」。這條把 judgment §1 從人工審查變成自動化閘門。
    - Test 4（D-16 硬規則 1）：除 `tradeErrNetwork` 外，每一個 `tradeErr*` 的 zh 文案都含「交易未記錄」、en 文案都含 `nothing was recorded`（`04-UI-SPEC.md` 文案硬規則 1：防 D-12 點名的最糟失敗模式）。
    - Test 5（D-07 語意，UI-SPEC 文案硬規則 3）：`tradeErrKeyReused` 的 zh 不含「重複」、en 不含 `duplicate`（那會讓使用者以為交易被建了兩筆，而事實相反）。
    - Test 6（UI-SPEC 文案硬規則 2）：`tradeErrNetwork` 的 zh 含「不會建立重複交易」、en 含 `will not create a duplicate`（唯一「結果未知」的情境，也是本 Phase 冪等工作對使用者的可見價值）。
  </behavior>
  <action>
    RED 先行：先在 `i18n.test.ts` 寫上述六條（key 不存在 → 第一條就紅），再補文案。

    在 `i18n.ts` 的 zh 區塊與 en 區塊**各加 42 個 key**，文案**逐字照抄** `04-UI-SPEC.md` §Copywriting Contract 的 zh/en 兩欄。完整 key 清單（依 UI-SPEC 分組）：

    - 動作與標題（8）：`recordTrade`、`reviewTrade`、`recordingTrade`、`tradeRecorded`、`backToEdit`、`recordAnother`、`closeTicket`、`tradeIrreversibleNote`
    - 欄位標籤與說明（7）：`tradeExecutedAt`、`tradeExecutedAtHint`、`tradeFeeHint`、`sellableQty`、`sellableQtyLoading`、`sellableQtyFailed`、`tradeId`
    - 空狀態（5）：`symbolNoResults`、`symbolNoTradable`、`symbolMoreResults`、`quoteChartEmpty`、`quoteChartError`
    - typeahead 錯誤（1）：`symbolSearchFailed`
    - 欄位級錯誤（7，key 對應後端 `error.fields` 的 key）：`tradeErrSymbol`、`tradeErrType`、`tradeErrQuantity`、`tradeErrPrice`、`tradeErrFee`、`tradeErrNote`、`tradeErrExecutedAt`
    - 底部錯誤（依 `error.code` 分派，9）：`tradeErrOversell`、`tradeErrAssetNotFound`、`tradeErrValidation`、`tradeErrConflict`、`tradeErrKeyReused`、`tradeErrForbidden`、`tradeErrCsrf`、`tradeErrNetwork`、`tradeErrUnknown`
    - Post-trade refetch（5）：`portfolioRefreshing`、`portfolioStaleAfterTrade`、`tradeNotInCurrentView`、`freshBadge`、`tradeRecordedToast`

    **`closeTicket` 為什麼必須是新 key**：`i18n.ts` 內 `close` **零命中**（UI-SPEC 已實跑 grep 驗證）；`cancel`（`:49` zh / `:187` en）已被 footer 的「取消」按鈕佔用。同一 dialog 內兩個按鈕共用同一 label，螢幕閱讀器會連續播報兩個無法分辨的「取消」。

    **必須複用、不得新增同義 key 的既有項**（UI-SPEC §Copywriting 明列）：`loading`、`loadFailed`、`authRetry`、`authRequestId`、`cancel`、`fee`、`qty`、`price`、`symbol`、`side`、`notes`、`selectSymbol`、`viewPositions`。

    **本 task 不刪除任何既有 key**。`placeOrder` / `filled` / `avgFillPx` / `routingMatch` / `placing` 五個 key 的**引用**會在 04-09 的 U-16 範圍內從 `OrderTicket.vue` 移除，但 key 本身是否刪除要等 04-09 grep 確認沒有其他元件在用（已知 `filled` 仍被 mock notification 文字使用）。在此刪除會讓 04-09 的 RED 變成 build error 而非測試紅燈。

    文案撰寫的四條硬規則（照抄進 `i18n.ts` 新區塊上方的繁中註解）：
    1. 除 `tradeErrNetwork` 外，每一條錯誤文案都必須明說「交易未記錄」。
    2. `tradeErrNetwork` 是唯一「結果未知」的情境，必須明講「重試不會建立重複交易」。
    3. `tradeErrKeyReused` **不得**說「重複的請求／duplicate request」。
    4. 任何文案不得回射 idempotency key、後端 `error.message`、或 `error.fields` 的英文 value。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npx vitest run src/i18n.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/i18n.ts` 的 zh 與 en 各含 42 個新 key（用 `i18n.test.ts` 的迴圈斷言驗證，非人工計數）
    - `i18n.ts` 含 `tradeErrKeyReused`、`closeTicket`、`portfolioStaleAfterTrade`、`tradeNotInCurrentView`、`freshBadge`
    - `i18n.ts` **不含** `tradeLoading`、`tradeLoadFailed`、`tradeRetry`、`tradeTraceId`、`tradeCancel`（同義 key 未被新增）
    - `[FE] src/i18n.test.ts` 含 `PHASE4_TRADE_KEYS`（或等價命名）陣列與六個 `it(...)`
    - `cd ../../vue/stock-v2/vue-app && npx vitest run src/i18n.test.ts` exit 0
    - `npm test` exit 0；`VITE_DATA_MODE=api npm test` exit 0；`npm run build` exit 0
    - `git diff src/i18n.ts` 顯示**沒有任何既有 key 被刪除**
  </acceptance_criteria>
  <done>42 個 key 在兩種語言就位、禁止用語與四條文案硬規則都被自動化斷言鎖住、既有 key 零刪除。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime mode 設定 → adapter 選擇 | 設定錯誤時若靜默回退 mock，整合失敗會被掩蓋（FAPI-07 明文列為 Out of Scope 的反模式） |
| 後端錯誤 code → 使用者可見文案 | 文案是唯一暴露給使用者的錯誤資訊；措辭錯誤會直接造成錯誤的重試行為 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | i18n 文案內容 | mitigate | 文案全部是前端自有字串；**不得**引用後端 `error.message` 或 `error.fields` 的英文 value（D-16 硬規則）。文案中不含 idempotency key、token、cookie 名、Redis key |
| T-04-01 | Tampering / Repudiation | `tradeErrNetwork` 與 `tradeErrConflict` 的措辭 | mitigate | 兩者明講「重試不會建立重複交易」→ 使用者會沿用同一把 key 重送（安全）。若文案暗示「可能已建立，請不要重試」，使用者會關掉 ticket 重開（換新 key）→ **真的**建出第二筆。Test 6 鎖住這條 |
| — | Repudiation | `tradeErrKeyReused` 的措辭 | mitigate | 不得說「重複的請求」（會讓使用者誤以為建了兩筆而去刪除／回報）。Test 5 鎖住 |
| — | Elevation of Privilege | API mode 靜默回退 mock | mitigate | `mockFactoryCalls.trading` / `.market` 的 `not.toHaveBeenCalled()` 斷言把回退變成測試失敗（Phase 2 D-20 的既有防線延伸到新 adapter）；`pageApiClients.ts` 禁止任何 catch/fallback |
| T-04-SC | Tampering | npm installs | accept | 零新增依賴 |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**TypeScript 型別欄位**
- `RuntimeApiClients.trading: TradingApi`
- `RuntimeApiClients.market: MarketApi`

**i18n key（zh + en 各 42 個）**
`recordTrade`、`reviewTrade`、`recordingTrade`、`tradeRecorded`、`backToEdit`、`recordAnother`、`closeTicket`、`tradeIrreversibleNote`、`tradeExecutedAt`、`tradeExecutedAtHint`、`tradeFeeHint`、`sellableQty`、`sellableQtyLoading`、`sellableQtyFailed`、`tradeId`、`symbolNoResults`、`symbolNoTradable`、`symbolMoreResults`、`symbolSearchFailed`、`quoteChartEmpty`、`quoteChartError`、`tradeErrSymbol`、`tradeErrType`、`tradeErrQuantity`、`tradeErrPrice`、`tradeErrFee`、`tradeErrNote`、`tradeErrExecutedAt`、`tradeErrOversell`、`tradeErrAssetNotFound`、`tradeErrValidation`、`tradeErrConflict`、`tradeErrKeyReused`、`tradeErrForbidden`、`tradeErrCsrf`、`tradeErrNetwork`、`tradeErrUnknown`、`portfolioRefreshing`、`portfolioStaleAfterTrade`、`tradeNotInCurrentView`、`freshBadge`、`tradeRecordedToast`

**測試常數**
- `PHASE4_TRADE_KEYS`（`i18n.test.ts` 內的 key 清單陣列）
</artifacts_this_phase_produces>

<verification>
- `npx vitest run src/api-adapter-wiring.test.ts src/i18n.test.ts` 綠（mock mode）
- `VITE_DATA_MODE=api npx vitest run src/api-adapter-wiring.test.ts` 綠
- `npm test` 綠、`VITE_DATA_MODE=api npm test` 綠、`npm run build` 綠
- 人工檢視：42 條文案是否逐字對應 `04-UI-SPEC.md` §Copywriting Contract 的 zh/en 兩欄
</verification>

<success_criteria>
- `getRuntimeApiClients().trading` / `.market` 可用，且 API mode 下 mock factory 零呼叫
- 42 個 key 在 zh 與 en 兩邊都存在且已翻譯
- judgment §1 的禁止用語被自動化斷言擋住
- UI-SPEC 的四條文案硬規則各有對應測試
- 沒有新增同義 key、沒有刪除既有 key
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-08-SUMMARY.md`。

SUMMARY 必須記錄：
1. 42 個 key 的完整清單與其複用了哪些既有 key。
2. `placeOrder` / `filled` / `avgFillPx` / `routingMatch` / `placing` 五個舊 key **尚未刪除**，刪除與否留給 04-09 在移除引用後 grep 確認（已知 `filled` 仍被 mock notification 文字使用）。
3. judgment §1 禁止用語已被 `i18n.test.ts` 自動化，人工審查不再是唯一防線。
</output>
