---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - "[FE] src/services/apiTypes.ts"
  - "[FE] src/services/marketApi.ts"
  - "[FE] src/services/marketApi.test.ts"
autonomous: true
requirements: [TRAD-01]

must_haves:
  truths:
    - "前端可以用一個 domain adapter 查後端真實的可交易標的（symbol / name / sector / latestPrice / change / changePercent / high / low / volumeText / tradeable）"
    - "前端可以用同一個 adapter 取得該標的的 K 線序列並轉成 LineChart 需要的 number[]"
    - "searchAssets 走分頁契約（items / page / size / totalElements / totalPages），不是裸陣列"
    - "KlineDto 的 OHLCV 在 JSON 裡是 string，adapter 負責轉 number，前端其他地方不需要知道這件事"
    - "mock mode 由 data.ts 組出同形資料，OrderTicket 只有一條資料路徑"
  artifacts:
    - path: "[FE] src/services/marketApi.ts"
      provides: "asset 搜尋與 klines 的 domain adapter 三件組"
      exports: ["MarketApi", "createMarketApi", "createHttpMarketApi", "createMockMarketApi"]
      min_lines: 80
    - path: "[FE] src/services/apiTypes.ts"
      provides: "AssetDto / KlineDto / KlineInterval 型別"
      contains: "export interface AssetDto"
    - path: "[FE] src/services/marketApi.test.ts"
      provides: "分頁契約與 string→number 轉換的鎖定"
      contains: "vi.stubGlobal('fetch'"
  key_links:
    - from: "[FE] src/services/marketApi.ts"
      to: "/api/v1/assets"
      via: "apiPaginatedRequest（分頁端點）"
      pattern: "apiPaginatedRequest<AssetDto>"
    - from: "[FE] src/services/marketApi.ts"
      to: "/api/v1/market/{symbol}/klines"
      via: "apiRequest（非分頁）"
      pattern: "klines"
---

<objective>
建立 D-01 需要但 `04-CONTEXT.md` **未列出**的 adapter：前端目前對 `api/v1/assets` / `klines` / `market/` **零命中**（`Markets.vue:129` 仍 `import { SYMBOLS, CRYPTO, FX, BONDS } from '../data'`），沒有這個 adapter，D-01（symbol 選單、報價卡、走勢圖全接真後端）做不到。

Purpose：這是 `04-RESEARCH.md` 最重要的**範圍**發現。planner 已確認它屬於 Phase 4 範圍（D-01 的直接前提），不是可以延後的項目。DP-5 裁定：**合併成一個 `marketApi.ts`**，不拆 `assetApi` + `marketApi`，也**絕不**塞進 `tradingApi.ts`（VER-02 把不同 domain 列為不同 adapter 的邏輯同樣適用）。

Output：`src/services/marketApi.ts` 三件組、`apiTypes.ts` 的 `AssetDto` / `KlineDto` / `KlineInterval`、`marketApi.test.ts`。

**本 plan 不碰 `Markets.vue` / `Chart.vue`** —— 那兩頁的 API 化屬 PORT-06（v2）。本 plan 只建立 OrderTicket 需要的能力。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/judgment.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-CONTEXT.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-PATTERNS.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md
</context>

<repo_and_branch>
**本 plan 全部在 sibling 前端 repo** `D:\end\workspace\vue\stock-v2`，真專案根為其中的 `vue-app/`。`files_modified` 的 `[FE]` 前綴表示「相對於 `vue-app/`」。

執行前先從 sibling repo 的 **`develop`** 開分支（**不是** `feature/phase-03-portfolio-read` —— Phase 3 的全部 commit 已經 merge 進 `develop @ a03e030`，該 feature branch 已無用）：
```
cd /d/end/workspace/vue/stock-v2
git fetch origin && git switch -c feature/phase-04-manual-trade-creation origin/develop
```

驗證指令一律在 `vue-app/` 下跑。PowerShell 使用者用 `$env:VITE_DATA_MODE='api'` 取代前綴語法。
</repo_and_branch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：apiTypes 新增 AssetDto / KlineDto / KlineInterval</name>
  <files>[FE] src/services/apiTypes.ts</files>
  <read_first>
    - [FE] src/services/apiTypes.ts（全檔：扁平 interface + 每個型別上方一行繁中「對應後端 XXX」javadoc 的既有慣例；`PaginatedResponse<T>`、`TradeDto`、`HoldingDto` 的寫法）
    - stock-module-asset 的 `AssetDto.java`（後端 repo：`D:\end\workspace\java\stock-web-v2`，欄位 uuid / symbol / name / assetType / market / currency / sector / tradeable / latestPrice / change / changePercent / volumeText / high / low）
    - stock-module-market-data 的 `KlineDto.java`（後端 repo；**五個 BigDecimal 欄位都掛 `@JsonSerialize(using = ToStringSerializer.class)`，所以 JSON 裡是 string**）
    - stock-module-asset 的 `AssetController.java`（`ApiResponse<PageResponse<AssetDto>>`；`query` 預設 `""`、`page` 預設 0、`size` 預設 20 上限 100）
    - stock-module-market-data 的 `MarketController.java:96-105`（klines 的五個參數）
  </read_first>
  <behavior>
    - Test（由 Task 2 的 adapter 測試間接覆蓋）：型別宣告必須讓「把 `KlineDto.close` 當 number 用」在 `npm run build`（`vue-tsc --noEmit`）時就報錯。
  </behavior>
  <action>
    在 `apiTypes.ts` 新增三個宣告，位置放在 `TradeDto` 之後、backtest 型別之前，並照該檔既有慣例各加一行繁中「對應後端 XXX；逐欄同形，勿增刪（judgment §4）」的 javadoc：

    1. `export interface AssetDto` —— 欄位逐字：`uuid: string`、`symbol: string`、`name: string`、`assetType: string`、`market: string`、`currency: string`、`sector: string`、`tradeable: boolean`、`latestPrice: number | null`、`change: number | null`、`changePercent: number | null`、`volumeText: string`、`high: number | null`、`low: number | null`。
       - 五個價格欄位宣告為 `number | null` 是刻意的：後端是 `BigDecimal`（非 primitive），沒有價格資料的 asset 會是 `null`。宣告成 `number` 會讓報價卡顯示 `NaN`（`04-UI-SPEC.md` §2 已規定 `null` 顯示 `—`）。
       - `assetType` / `currency` 宣告為 `string` 而非 union：後端 enum 常數名未在本 Phase 查證（RESEARCH A6），**不得憑猜測寫死 union**。若之後要做分類顯示，必須先讀後端的 `AssetType` / `CurrencyCode` enum。
    2. `export interface KlineDto` —— `bucket: string`（Java `Instant` → JSON ISO 字串）、`open: string`、`high: string`、`low: string`、`close: string`、`volume: string`。
       - **五個 OHLCV 必須是 `string`**。javadoc 必須逐字寫出理由與後端出處：「`KlineDto.java` 的五個 BigDecimal 欄位掛 `@JsonSerialize(using = ToStringSerializer.class)`，序列化為 JSON 字串以避免浮點精度損失。**注意與 `AssetDto` 相反** —— `AssetDto` 的 BigDecimal 沒有這個註解，是 JSON number。」
    3. `export type KlineInterval = '1m' | '5m' | '15m' | '1h' | '1d'` —— 這五個值是 `MarketController` 明確驗證的白名單（非法值回 400 `KLINE_INTERVAL_INVALID`），寫成 union 可讓錯誤在編譯期就被抓到。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/services/apiTypes.ts` 含 `export interface AssetDto`、`export interface KlineDto`、`export type KlineInterval`
    - `AssetDto` 的 `latestPrice`、`change`、`changePercent`、`high`、`low` 皆宣告為 `number | null`
    - `KlineDto` 的 `open`/`high`/`low`/`close`/`volume` 皆宣告為 `string`（`grep -A8 "interface KlineDto" src/services/apiTypes.ts` 中不出現 `: number`）
    - `cd ../../vue/stock-v2/vue-app && npm run build` exit 0
  </acceptance_criteria>
  <done>三個型別存在、null 與 string 的陷阱都在型別層被鎖住、build 綠。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：marketApi.ts 三件組 + 測試</name>
  <files>
    [FE] src/services/marketApi.test.ts,
    [FE] src/services/marketApi.ts
  </files>
  <read_first>
    - [FE] src/services/portfolioApi.ts（**三件組骨架的樣板**：`:39-46` 介面 javadoc 的語氣、`:47-54` interface 形狀、`:140-202` `createMockPortfolioApi` / `createHttpPortfolioApi` / `createPortfolioApi`；特別是 `:141-142` 的註解「每個 getter 都重新呼叫 useMockPortfolioStore()：延遲解析是 reactivity 與跨測試隔離的關鍵」）
    - [FE] src/services/apiClient.ts（`apiRequest<T>`（:328）、`apiPaginatedRequest<T>`（:352）、`buildQueryString`（:69）、`ApiRequestOptions extends Omit<RequestInit,'body'>`（:32，**已支援 `signal`**）、`ApiClientError`（:3，含 `code` / `requestId` / `fields`））
    - [FE] src/services/portfolioApi.test.ts（`jsonResponse` helper、`beforeEach(setActivePinia(createPinia()))`、`afterEach(vi.unstubAllGlobals())`、`vi.stubGlobal('fetch', ...)` 的樣板）
    - [FE] src/data.ts（`SYMBOLS` / `CRYPTO` / `FX` / `BONDS` 的形狀，以及 `genSeries()` 的簽章 —— mock 實作要用它們組出 `AssetDto` / `KlineDto[]`）
    - [FE] src/testSetup.ts（:10-12 的註解：為什麼刻意不 import 任何 service module）
    - .planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-UI-SPEC.md §2 §3（typeahead 七態與走勢圖三態的契約）
  </read_first>
  <behavior>
    `marketApi.test.ts`（照 `portfolioApi.test.ts` 的樣板）：
    - Test 1（**Pitfall 7：分頁**）：stub `fetch` 回 `{success:true, data:{items:[assetFixture], page:0, size:10, totalElements:37, totalPages:4}, error:null, meta:{traceId:'t1'}}` → `searchAssets({query:'AAP', page:0, size:10})` 回傳的物件有 `.items`（陣列）與 `.totalElements === 37`。若實作誤用 `apiRequest` 而非 `apiPaginatedRequest`，這條會紅。
    - Test 2：攔截到的 URL 為 `/api/v1/assets?query=AAP&page=0&size=10`（用 `buildQueryString`，參數順序與字面逐字斷言）。
    - Test 3：`searchAssets` 傳入 `AbortSignal` 時，`fetch` 收到的 `RequestInit.signal` 為同一個物件。
    - Test 4（**Pitfall 8：string → number**）：klines 的 fixture 的 OHLCV **必須用字串**（例如 `close: "218.40000000"`）→ `listKlines('AAPL', {interval:'1h', from:'...', limit:48})` 之後，`closeSeries(klines)` 回傳 `number[]` 且第一個元素 `=== 218.4`（`typeof` 為 `'number'`）。fixture 用 number 寫是**錯的**，會讓這條測試失去意義。
    - Test 5：klines 的 URL 含 `interval=1h`、`from=`（完整 ISO instant）、`limit=48`，且**不含** `page` / `size`（非分頁端點）。
    - Test 6：後端回 404 `ASSET_NOT_FOUND` 時，`listKlines` reject 成 `ApiClientError` 且 `error.code === 'ASSET_NOT_FOUND'`、`error.requestId` 來自 `meta.traceId`（沿用 `apiClient` 既有行為，adapter 不得吞掉或重包）。
    - Test 7（mock 實作）：`createMockMarketApi().searchAssets({query:'AAP'})` 回傳的 `items` 每一個都符合 `AssetDto` 形狀（有 `symbol` / `tradeable` / `latestPrice`），且**不發任何 fetch**（`vi.stubGlobal('fetch', spy)` 後 `expect(spy).not.toHaveBeenCalled()`）。
    - Test 8（mock 實作）：`createMockMarketApi().listKlines('AAPL', ...)` 回傳非空 `KlineDto[]`，且 `close` 是 **string**（與 API mode 同形，讓 OrderTicket 只有一條資料路徑）。
    - Test 9：`createMarketApi('api')` 的 `.mode === 'api'` 且 `.live === undefined`；`createMarketApi('mock')` 的 `.mode === 'mock'`。
  </behavior>
  <action>
    RED 先行：先完整寫 `marketApi.test.ts`（模組不存在 → import 失敗），再實作。

    新建 `[FE] src/services/marketApi.ts`，照 `portfolioApi.ts` 的三件組結構：

    **介面**（含照抄 `portfolioApi.ts:39-46` 語氣的繁中 javadoc，說明「頁面用法」與「分支判斷依據是 `api.live` 是否存在，不是 mode 字串」）：
    - `export interface MarketApi { mode: RuntimeDataMode; searchAssets(params, signal?): Promise<PaginatedResponse<AssetDto>>; listKlines(symbol, params): Promise<KlineDto[]>; }`
    - `searchAssets` 的 `params`：`{ query: string; page?: number; size?: number }`，預設 `page = 0`、`size = 10`（`04-UI-SPEC.md` §2 規定 typeahead 最多列 10 筆；後端 `size` 上限 100）。第二參數 `signal?: AbortSignal`。
    - `listKlines` 的 `params`：`{ interval: KlineInterval; from: string; to?: string; limit?: number }`。
    - **不需要** `live` —— mock 實作與 API 實作回傳同形資料，沒有需要 reactive 委派的 mock 專屬狀態（與 `portfolioApi` 的 `lastFill` 情境不同）。

    **`createHttpMarketApi(basePath = '/api/v1')`**：
    - `searchAssets` 用 **`apiPaginatedRequest<AssetDto>`**（端點回 `ApiResponse<PageResponse<AssetDto>>`）+ `buildQueryString({ query, page, size })`，options 傳 `{ signal }`。
    - `listKlines` 用 **`apiRequest<KlineDto[]>`**（非分頁）+ `buildQueryString({ interval, from, to, limit })`；path 為 `${basePath}/market/${encodeURIComponent(symbol)}/klines`。symbol **大小寫敏感**（後端 javadoc 明文），不得 `toUpperCase()`。
    - **不得**在此檔實作任何 retry／CSRF／credentials／401 處理 —— `apiClient` 是唯一 transport 邊界（Phase 2 D-20），全部已就緒。

    **`createMockMarketApi()`**：從 `data.ts` 的 `SYMBOLS` / `CRYPTO` / `FX`（`BONDS` 依需要）組出 `AssetDto` 形狀的陣列並依 `query` 做本地 `filter`（比對 symbol 與 name，大小寫不敏感），回傳與 `PaginatedResponse<AssetDto>` 同形的物件（`items` / `page` / `size` / `totalElements` / `totalPages` 都要填，`totalElements` 為 filter 後總數以便 UI-SPEC §2 的 `truncated` 態可觸發）。`tradeable` 一律填 `true`。`listKlines` 用既有 `genSeries()` 組 `KlineDto[]`，**OHLCV 一律 `String(...)`** 以與 API mode 同形。
    - mock 實作可以 import `data.ts`（judgment §3 只禁**元件** import mock 資料源；`portfolioApi.ts:2` 本身就 import mock store）。

    **`createMarketApi(mode, basePath = '/api/v1')`**：`mode === 'api' ? createHttpMarketApi(basePath) : createMockMarketApi()`。

    **另外匯出一個純函式** `export function closeSeries(klines: KlineDto[]): number[]` —— `klines.map(k => Number(k.close))`。理由：`LineChart.vue` 的 `data` prop 是 `number[]`，而 string→number 的轉換是「最容易寫錯且型別檢查抓不到」的一點（Pitfall 8）。做成獨立純函式讓它能被直接單測，且 OrderTicket 不需要自己寫 `Number()`。

    測試檔照 `portfolioApi.test.ts`：`jsonResponse` helper、`beforeEach(() => setActivePinia(createPinia()))`、`afterEach(() => vi.unstubAllGlobals())`。**不要**把任何 reset 加進 `testSetup.ts`（該檔 `:10-12` 明文說明會搶在 `vi.mock` 生效前綁定真實實作）。
  </action>
  <verify>
    <automated>cd ../../vue/stock-v2/vue-app && npx vitest run src/services/marketApi.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `[FE] src/services/marketApi.ts` 存在且 export `MarketApi`、`createMarketApi`、`createHttpMarketApi`、`createMockMarketApi`、`closeSeries`
    - 該檔含 `apiPaginatedRequest<AssetDto>` **且** 含 `apiRequest<KlineDto[]>`（兩個端點用對了不同的 helper）
    - 該檔**不含** `fetch(`、`credentials`、`X-XSRF-TOKEN`、`Authorization`（transport 全走 `apiClient`）
    - `[FE] src/services/marketApi.test.ts` 的 klines fixture 中 `close` 的值是**帶引號的字串**
    - `cd ../../vue/stock-v2/vue-app && npx vitest run src/services/marketApi.test.ts` exit 0，且測試數 ≥ 9
    - `cd ../../vue/stock-v2/vue-app && npm test` exit 0
    - `cd ../../vue/stock-v2/vue-app && VITE_DATA_MODE=api npm test` exit 0
    - `cd ../../vue/stock-v2/vue-app && npm run build` exit 0
  </acceptance_criteria>
  <done>三件組存在、分頁與非分頁各用對 helper、string→number 轉換被 fixture 鎖住、mock 與 API 回傳同形、兩個 mode 的測試皆綠。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 使用者輸入的 query 字串 → `GET /api/v1/assets` | typeahead 每次按鍵都可能觸發網路請求 |
| 後端 JSON → 前端型別假設 | `AssetDto`（number）與 `KlineDto`（string）的 BigDecimal 序列化方式**相反**，猜錯不會有型別錯誤只會有錯誤畫面 |
| symbol 字串 → URL path segment | 使用者可控字串進入 URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Denial of Service | `searchAssets` 的呼叫頻率 | transfer（本 plan 提供能力，節流在 04-10 的 OrderTicket） | adapter 支援 `signal?: AbortSignal`（`ApiRequestOptions extends Omit<RequestInit,'body'>` 已就緒，`buildRequestInit` 的 `...requestOptions` 會原樣傳給 `fetch`）。250ms debounce + 只採最後一次結果由 OrderTicket 實作（DP-12） |
| T-04-12 | Tampering | symbol 進入 URL path | mitigate | `encodeURIComponent(symbol)`；query 參數一律走 `buildQueryString`（其內部已 `encodeURIComponent` 鍵與值） |
| — | Information Disclosure | adapter 的錯誤處理 | mitigate | adapter **不 catch、不重包**錯誤，一律讓 `ApiClientError`（含 `code` / `requestId`）原樣往上拋。UI 層只顯示 `code` 與 `traceId`，不顯示後端 `message`（`04-UI-SPEC.md` §Security & Diagnostics） |
| — | Spoofing | 未經 transport 邊界的請求 | mitigate | 禁止在 `marketApi.ts` 內出現 `fetch(`／自訂 credentials／自訂 CSRF header —— CSRF、cookie、401 refresh+replay 全部由 `apiClient` 負責（Phase 2 D-20）。acceptance criteria 已用 grep 強制 |
| T-04-SC | Tampering | npm installs | accept | 本 plan 零新增 npm 依賴（`04-RESEARCH.md` §Package Legitimacy Audit）。debounce 用 `setTimeout`/`clearTimeout`，**不得**引入 `lodash.debounce`；UUID 用 `crypto.randomUUID()`，**不得**引入 `uuid` |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**TypeScript 型別（`src/services/apiTypes.ts`）**
- `AssetDto`（14 欄，價格類為 `number | null`）
- `KlineDto`（`bucket: string` + 五個 OHLCV `string`）
- `KlineInterval`（`'1m' | '5m' | '15m' | '1h' | '1d'`）

**TypeScript 匯出（`src/services/marketApi.ts`）**
- `interface MarketApi`
- `createMarketApi(mode, basePath?)`
- `createHttpMarketApi(basePath?)`
- `createMockMarketApi()`
- `closeSeries(klines: KlineDto[]): number[]`

**新檔案**
- `[FE] src/services/marketApi.ts`
- `[FE] src/services/marketApi.test.ts`

**消費的既有後端端點（非本 Phase 新建）**
- `GET /api/v1/assets?query=&page=&size=`（分頁、公開）
- `GET /api/v1/market/{symbol}/klines?interval=&from=&to=&limit=`
</artifacts_this_phase_produces>

<verification>
- `cd ../../vue/stock-v2/vue-app && npx vitest run src/services/marketApi.test.ts` 綠
- `npm test` 綠、`VITE_DATA_MODE=api npm test` 綠（judgment §3：兩個 mode 都要跑）
- `npm run build` 綠（`vue-tsc --noEmit && vite build`）

**不得宣稱覆蓋**：真實 payload 與 fixture 的一致性（fixture 是手寫的）—— 屬 Phase 5 / VER-03。但 fixture 的註解必須引用後端的 `file:line` 以降低漂移（`04-VALIDATION.md` 明文要求）。
</verification>

<success_criteria>
- 前端有一個能查真實可交易標的與 K 線的 domain adapter
- 分頁端點用 `apiPaginatedRequest`、非分頁用 `apiRequest`（用錯會被 Test 1 抓到）
- OHLCV 的 string→number 轉換被 string fixture 鎖住
- mock 與 API 回傳同形，OrderTicket 之後只需要一條資料路徑
- 未新增任何 npm 依賴、未繞過 `apiClient`
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-06-SUMMARY.md`。

SUMMARY 必須記錄：
1. **這是 `04-CONTEXT.md` 未列出的必要新檔**（由 RESEARCH 發現），DP-5 裁定合併為一個 `marketApi.ts`。
2. `AssetDto`（JSON number）與 `KlineDto`（JSON string）的序列化差異，以及為什麼 `latestPrice` 等宣告為 `number | null`（RESEARCH A7 未查證 `AssetQueryService.search` 是否保證非 null，保守處理）。
3. `assetType` / `currency` 刻意宣告為 `string` 而非 union（後端 enum 常數名未查證，RESEARCH A6）。
4. sibling repo 的分支起點是 `develop @ a03e030`。
</output>
