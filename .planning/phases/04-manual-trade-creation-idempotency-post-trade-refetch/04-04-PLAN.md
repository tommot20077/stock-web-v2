---
phase: 04-manual-trade-creation-idempotency-post-trade-refetch
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - stock-module-trading/pom.xml
  - stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/TradingTestApplication.java
  - stock-module-trading/src/main/java/dowob/xyz/stockwebv2/trading/api/TradingController.java
  - stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/api/TradingControllerTest.java
  - stock-start/src/main/java/dowob/xyz/stockwebv2/start/error/GlobalExceptionHandler.java
  - stock-start/src/test/java/dowob/xyz/stockwebv2/start/ErrorHandlingIT.java
autonomous: true
requirements: [TRAD-03, TRAD-06]

must_haves:
  truths:
    - "POST /api/v1/trades 缺少 Idempotency-Key header 時回 400，而不是靜默建立交易"
    - "缺 header 的 400 回應在 error.fields 指出 header 名稱，讓前端能與 body 欄位錯誤區分開"
    - "controller 把 header 值原樣往下傳給 service，自己不做任何業務判斷（含空白檢查）"
    - "成功與失敗都會寫 audit log，且失敗的 audit 內容不含 idempotency key"
  artifacts:
    - path: "stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/TradingTestApplication.java"
      provides: "@WebMvcTest 需要的 @SpringBootConfiguration 錨點"
      contains: "@SpringBootApplication"
    - path: "stock-module-trading/src/main/java/dowob/xyz/stockwebv2/trading/api/TradingController.java"
      provides: "必填 Idempotency-Key header"
      contains: "@RequestHeader"
    - path: "stock-start/src/main/java/dowob/xyz/stockwebv2/start/error/GlobalExceptionHandler.java"
      provides: "MissingRequestHeaderException → 400 + fields"
      contains: "MissingRequestHeaderException"
  key_links:
    - from: "TradingController.createTrade"
      to: "TradingService.createTrade"
      via: "第三個參數傳入 header 值"
      pattern: "tradingService.createTrade\\(userId, request, "
    - from: "GlobalExceptionHandler"
      to: "ApiError.of(ErrorCode.VALIDATION_FAILED, ..., fields)"
      via: "MissingRequestHeaderException handler"
      pattern: "getHeaderName"
---

<objective>
把冪等契約推到 HTTP 邊界：`Idempotency-Key` 成為 `POST /api/v1/trades` 的**必填** header（D-05／D-06），缺漏時回可辨識的 400 envelope（D-16），並補上 `stock-module-trading` 缺失的 `@WebMvcTest` 基礎設施。

Purpose：D-05 明文選擇 `required = true` 而非 `BackfillController:90` 的 `required = false` —— 選填等於防護可被繞過，而那是 append-only 帳本上改不回來的錯。遷移成本只有更新 `TradingControllerTest`（目前**沒有任何真實 client** 在呼叫 `POST /trades`）。

Output：`TradingTestApplication`、`@WebMvcTest` 版 `TradingControllerTest`、帶 header 的 `TradingController.createTrade`、`GlobalExceptionHandler` 的 `MissingRequestHeaderException` handler。

**⚠️ 層級界線（必須嚴守，RESEARCH Wave 0 Gaps 明文）**：`GlobalExceptionHandler` 位於 `stock-start`，**不在 `@WebMvcTest` 切片範圍內**。所以 module 層的 `@WebMvcTest` 只能證明「`@RequestHeader` 綁定失敗會拋 `MissingRequestHeaderException`」；**「缺 header → 400 envelope」的真正驗收只能在 `stock-start` 的 IT**。本 plan 不得把 module 層測試當成 envelope 的證明。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@ai-docs/testing-standards.md
@ai-docs/code-standards.md
@ai-docs/browser-auth-contract.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-CONTEXT.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-PATTERNS.md
@.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-03-SUMMARY.md
</context>

<repo_and_branch>
後端 repo `D:\end\workspace\java\stock-web-v2`，分支 `feature/phase-04-trade-idempotency`（承 04-03）。
</repo_and_branch>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1：補上 stock-module-trading 的 @WebMvcTest 基礎設施</name>
  <files>
    stock-module-trading/pom.xml,
    stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/TradingTestApplication.java
  </files>
  <read_first>
    - stock-module-market-data/src/test/java/dowob/xyz/stockwebv2/marketdata/MarketDataTestApplication.java（**全檔 15 行，直接照抄改 package**）
    - stock-module-market-data/pom.xml（:103-104 與 :108-109 的兩個 test-scope 依賴宣告方式；版本由 parent BOM 管理，**不要寫死 version**）
    - stock-module-trading/pom.xml（:49-50 目前只有 `spring-boot-starter-test`）
    - stock-module-market-data/src/test/java/.../api/BackfillControllerTest.java（:56-62 的 `@Import({AdminOnlySecurityConfig.class, TestExceptionHandler.class})` 樣板，以及其 javadoc 對「GlobalExceptionHandler 不在切片內」的明文說明）
    - ai-docs/testing-standards.md:33（web 層必須用 @WebMvcTest 的規範來源）
  </read_first>
  <behavior>
    - Test 1：`./mvnw -pl stock-module-trading -am test` 在加入依賴與 `TradingTestApplication` 後仍 exit 0（基礎設施本身不改變任何行為）。
    - Test 2：Task 2 的 `@WebMvcTest` 版 `TradingControllerTest` 能成功啟動切片並找到 `@SpringBootConfiguration`（沒有 `TradingTestApplication` 時會拋 `Unable to find a @SpringBootConfiguration`）。
  </behavior>
  <action>
    1. `stock-module-trading/pom.xml` 的 `<dependencies>` 加入兩個 test-scope 依賴，artifactId 與 market-data 的 pom 逐字一致：`spring-boot-starter-webmvc-test`、`spring-boot-starter-security-test`（`<scope>test</scope>`，**不寫 `<version>`** —— parent BOM 已管理，market-data 也是這樣宣告）。
    2. 新建 `stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/TradingTestApplication.java`：`@SpringBootApplication` 標註的空 class，package `dowob.xyz.stockwebv2.trading`，加繁中 javadoc 說明「這個類別存在的唯一目的是給 `@WebMvcTest` 提供 `@SpringBootConfiguration` 錨點，不是可執行的應用程式」+ `@author Yuan`。逐字比照 `MarketDataTestApplication`。

    **不要**在 `stock-module-trading` 內建立任何 `*IT` 或 Testcontainers 設定 —— IT 一律放 `stock-start`（`04-PATTERNS.md` 對 planner 的明文警告）。
  </action>
  <verify>
    <automated>./mvnw -pl stock-module-trading -am test</automated>
  </verify>
  <acceptance_criteria>
    - `stock-module-trading/pom.xml` 含 `spring-boot-starter-webmvc-test` 與 `spring-boot-starter-security-test`，兩者皆 `<scope>test</scope>` 且**無** `<version>`
    - `stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/TradingTestApplication.java` 存在且含 `@SpringBootApplication`
    - `find stock-module-trading/src/test -name "*IT.java"` 無輸出（IT 未被誤放進 module）
    - `./mvnw -pl stock-module-trading -am test` exit 0
  </acceptance_criteria>
  <done>兩個 test 依賴就位、`TradingTestApplication` 存在、模組測試仍綠。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2：TradingController 的必填 Idempotency-Key header</name>
  <files>
    stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/api/TradingControllerTest.java,
    stock-module-trading/src/main/java/dowob/xyz/stockwebv2/trading/api/TradingController.java
  </files>
  <read_first>
    - stock-module-trading/src/main/java/dowob/xyz/stockwebv2/trading/api/TradingController.java（:36-53 `createTrade` 的完整現況：`@PreAuthorize("hasAuthority('TRADE_EXECUTE')")`、`authenticatedUserId(authentication)`、成功與失敗**都**寫 `auditLogger.log`、例外重拋不吞）
    - stock-module-trading/src/test/java/dowob/xyz/stockwebv2/trading/api/TradingControllerTest.java（全檔；:52-54 的 `tradeRequest()` helper；`assertThatThrownBy` + `verify(auditLogger).log(eq(...), ...)` 的既有樣板）
    - stock-module-market-data/src/main/java/dowob/xyz/stockwebv2/marketdata/api/BackfillController.java:90（**僅 header 接收的語法慣例**；:105-106 是「把 key 串進錯誤訊息」的反例，**不得照抄**）
    - stock-module-market-data/src/test/java/.../api/BackfillControllerTest.java:56-62（`@WebMvcTest` + `@Import` 的樣板）
    - stock-module-trading/src/main/java/dowob/xyz/stockwebv2/trading/service/TradingService.java（04-03 的三參數簽章）
  </read_first>
  <behavior>
    既有的純 Mockito `TradingControllerTest` 保留並擴充（簽章改動會讓 `rejectedTradeEmitsFailureAudit` 等既有 case 直接編譯失敗 —— 那就是 D-05 的第一個 RED），另新增 `@WebMvcTest` 切片測試：
    - Test 1（單元）：`controller.createTrade(request, "key-1", auth, servletRequest)` 把 `"key-1"` 原樣傳給 `tradingService.createTrade(userId, request, "key-1")`（用 `ArgumentCaptor<String>` 攔截）。
    - Test 2（單元）：service 拋 `BusinessException(TRADE_IDEMPOTENCY_KEY_REUSED, ...)` 時，controller **重拋**該例外並寫一筆 `failure:TRADE_IDEMPOTENCY_KEY_REUSED` 的 audit；audit 的 target 與 detail 參數**不含**傳入的 canary key 字串。
    - Test 3（單元）：controller 對**空白** header 值（`"   "`）**不做**任何判斷，原樣往下傳（業務判斷屬 service，Pitfall 14 的分層界線）。
    - Test 4（`@WebMvcTest` 切片）：`POST /api/v1/trades` 帶合法 body 但**不帶** `Idempotency-Key` header → 拋 `MissingRequestHeaderException`（在切片內用 `assertThatThrownBy` 或 `andExpect(status().isBadRequest())` 依 `TestExceptionHandler` 的行為斷言）。**這條只證明綁定行為，不證明 envelope** —— envelope 是 Task 3 + 04-05 的事。
    - 回歸：既有 `TradingControllerTest` 全部 case 補上 key 參數後仍綠。
  </behavior>
  <action>
    RED 先行：先在 `TradingControllerTest` 把所有 `controller.createTrade(...)` 呼叫改成四參數版 → 編譯失敗 → 那就是 RED。

    **`TradingController.createTrade` 的具體改動**（只加一個參數，其餘一律不動）：
    在 `@Valid @RequestBody CreateTradeRequest request` 之後加入
    `@RequestHeader(name = "Idempotency-Key") String idempotencyKey`
    —— **不寫 `required = false`**（Spring 預設即 `required = true`；為了讓意圖可讀，可顯式寫 `required = true`）。header 名稱逐字為 `Idempotency-Key`（D-06 沿用 `BackfillController` 的既有慣例，不新造名稱）。

    呼叫端改為 `tradingService.createTrade(userId, request, idempotencyKey)`。

    **必須原封不動保留的四件事**：`@PostMapping("/trades")`、`@PreAuthorize("hasAuthority('TRADE_EXECUTE')")`、成功／失敗兩條路徑**都**呼叫 `auditLogger.log(...)`、`catch (BusinessException) { audit; throw; }` 的重拋語意。

    **三條禁止**：
    - controller **不得**做任何業務判斷（含 `StringUtils.isBlank(idempotencyKey)`）—— 空白 key 的 400 屬 service（04-03 已實作）。
    - `auditLogger.log(...)` 的任何參數**不得**含 `idempotencyKey`（它是使用者可控字串，audit 目標應維持既有的 `"trade:" + trade.id()` / `"trade"`）。
    - **不得**照抄 `BackfillIdempotencyService` 的語意（它是 `tryAcquire` 失敗即回 409 拒絕；本 Phase 必須回既有交易 —— D-06 明文的同名不同語意反例）。

    `@WebMvcTest` 版測試：新增一個測試類別或在同檔加切片測試，照 `BackfillControllerTest:56-62` 的 `@WebMvcTest(TradingController.class)` + `@Import({...TestSecurityConfig, ...TestExceptionHandler})` 形狀；`@MockitoBean`（或該檔既有用法）替換 `TradingService` 與 `AuditLogger`。類別 javadoc 必須明寫「`GlobalExceptionHandler` 在 stock-start，不在本切片內；envelope 的驗收在 `stock-start` 的 IT」。
  </action>
  <verify>
    <automated>./mvnw -pl stock-module-trading -am test</automated>
  </verify>
  <acceptance_criteria>
    - `TradingController.java` 含 `@RequestHeader(name = "Idempotency-Key")`，且該檔**不含** `required = false`
    - `TradingController.java` 含 `tradingService.createTrade(userId, request, idempotencyKey)`
    - `TradingController.java` **不含** `isBlank`、`StringUtils.isBlank(idempotencyKey)`、`idempotencyKey.length()`（業務判斷不在 controller）
    - `grep -n "auditLogger.log" stock-module-trading/src/main/java/dowob/xyz/stockwebv2/trading/api/TradingController.java` 仍為兩處（成功 + 失敗），且兩處的參數皆**不含** `idempotencyKey`
    - `TradingControllerTest.java` 含 `ArgumentCaptor` 對 key 的攔截斷言，且含至少一個 `@WebMvcTest`
    - `./mvnw -pl stock-module-trading -am test` exit 0
  </acceptance_criteria>
  <done>header 為必填、值原樣下傳、audit 不含 key、既有 controller 測試全部遷移完成且綠。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3：GlobalExceptionHandler 的 MissingRequestHeaderException handler</name>
  <files>
    stock-start/src/test/java/dowob/xyz/stockwebv2/start/ErrorHandlingIT.java,
    stock-start/src/main/java/dowob/xyz/stockwebv2/start/error/GlobalExceptionHandler.java
  </files>
  <read_first>
    - stock-start/src/main/java/dowob/xyz/stockwebv2/start/error/GlobalExceptionHandler.java（:49-54 `handleBusiness`、**:56-64 `handleValidation` 是要複製的形狀**、`handleUnexpected` 的 `ErrorResponse` 分支）
    - stock-start/src/test/java/dowob/xyz/stockwebv2/start/ErrorHandlingIT.java（envelope 斷言的既有樣板）
    - stock-common 的 `ApiError`（`of(code, message)` 與 `of(code, message, fields)` 兩個多載）
    - ai-docs/code-standards.md:79-84
  </read_first>
  <behavior>
    - Test 1：`POST /api/v1/trades` 缺 `Idempotency-Key` → HTTP 400，`$.success` 為 `false`，`$.error.code` 為 `VALIDATION_FAILED`。
    - Test 2（D-16 的可區分性）：同一回應的 `$.error.fields['Idempotency-Key']` **存在且非空** —— 這讓前端能把「缺 header」與「body 欄位錯」分開，不需要解析 message。
    - Test 3（T-04-03）：該回應的完整 body **不含**任何請求中出現過的使用者可控字串。
    - 回歸：`ErrorHandlingIT` 既有 case 全綠（既有的 `VALIDATION_FAILED` body 欄位錯誤路徑不得被新 handler 攔走）。
  </behavior>
  <action>
    RED 先行：先在 `ErrorHandlingIT` 加上述三條 → 現況會因 `handleUnexpected` 的 `ErrorResponse` 分支而回 400 但 `fields` 為空 → Test 2 紅。

    在 `GlobalExceptionHandler` 新增
    `@ExceptionHandler(MissingRequestHeaderException.class)`（`org.springframework.web.bind.MissingRequestHeaderException`）
    的方法，形狀逐字複製 `handleValidation`：建一個 `Map<String, String> fields`，`fields.put(exception.getHeaderName(), "required header is missing")`，然後
    `ApiError.of(ErrorCode.VALIDATION_FAILED, ErrorCode.VALIDATION_FAILED.defaultMessage(), fields)`，回
    `ResponseEntity.badRequest().body(ApiResponse.failure(error, ApiMetaFactory.current()))`。

    **落點（DP-3）**：把新 handler 插在 `handleValidation` **之前**（`handleBusiness` 與 `handleValidation` 之間）。理由是避開 draft PR #15 在 `handleValidation` 附近的 hunk，降低未來合併衝突面。

    **兩條禁止**：
    - **絕不**把 `exception.getMessage()` 直接當 API message（`GlobalExceptionHandler` 全檔既有慣例）。`fields` 的 value 用**靜態英文字串**，不用例外訊息。
    - **不得**改動 `handleValidation` 的任何一行（PR #15 也會動那一帶）。

    方法需有繁中 javadoc 說明「為什麼要獨立 handler 而不是讓它落到 `handleUnexpected`」：不加也能得到 400 `VALIDATION_FAILED`，但 `fields` 為空、message 通用，前端無法區分「缺 header」與「body 欄位錯」（D-16 的依據）。
  </action>
  <verify>
    <automated>./mvnw -pl stock-start -am verify -Dit.test=ErrorHandlingIT</automated>
  </verify>
  <acceptance_criteria>
    - `GlobalExceptionHandler.java` 含 `@ExceptionHandler(MissingRequestHeaderException.class)` 與 `getHeaderName()`
    - 新 handler 的行號**小於** `handleValidation` 的行號
    - `git diff stock-start/src/main/java/dowob/xyz/stockwebv2/start/error/GlobalExceptionHandler.java` 顯示 `handleValidation` 方法本體**零改動**
    - `GlobalExceptionHandler.java` 的新 handler **不含** `exception.getMessage()`
    - `./mvnw -pl stock-start -am verify -Dit.test=ErrorHandlingIT` exit 0
    - `./mvnw test` exit 0
  </acceptance_criteria>
  <done>缺 header 回 400 且 `fields['Idempotency-Key']` 存在；`handleValidation` 未被改動；既有錯誤處理回歸綠。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 瀏覽器 / bearer client → `POST /api/v1/trades` | 未受信任的請求跨越此界線，帶 body、header、cookie 三種輸入 |
| 例外 → HTTP 回應 body | 例外訊息與 `fields` 的內容是使用者可見輸出 |
| `Authentication` → `userId` | 交易歸屬的唯一來源 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Repudiation | `@RequestHeader` 的 `required` 值 | mitigate | `required = true`（D-05）。選填等於防護可被繞過：任何一條 client 路徑忘了帶 key，連點就會建重複交易。acceptance criteria 用「檔案不含 `required = false`」強制 |
| T-04-03 | Information Disclosure | `auditLogger.log` 參數 / 新 handler 的 `fields` value | mitigate | audit 目標維持 `"trade:" + trade.id()` / `"trade"`，不含 key；`fields` 的 value 是靜態英文字串，不是例外訊息 |
| T-04-05 | Elevation of Privilege | `authenticatedUserId(authentication)` | mitigate | **不改動**任何認證語意（judgment §9 第 1 條）。`CreateTradeRequest` 仍無 userId 欄位，本 plan 不得新增。`@PreAuthorize("hasAuthority('TRADE_EXECUTE')")` 原封不動保留 |
| T-04-06 | Tampering / Spoofing | `POST /trades` 的 CSRF | accept（既有控制已覆蓋） | `POST` 已在 Phase 1 的 `SecurityFilterChain` unsafe method 清單內，前端 `apiClient.ts:220-223` 自動注入 `X-XSRF-TOKEN`。本 plan **不改動** security 設定；`BrowserAuthFlowIT` 已覆蓋。04-05 只需確認 `POST /trades` 在該清單內 |
| T-04-09 | Information Disclosure | 錯誤 envelope 的 message | mitigate | 新 handler 不使用 `exception.getMessage()`；沿用 `GlobalExceptionHandler` 全檔「絕不把 exception 原始 message 當 API message」的既有慣例 |
| T-04-SC | Tampering | maven installs | mitigate | 本 plan 新增兩個 **test-scope** 依賴（`spring-boot-starter-webmvc-test`、`spring-boot-starter-security-test`），兩者皆為 Spring Boot 官方 starter、版本由既有 parent BOM 管理、且 `stock-module-market-data/pom.xml:103-109` 已在同一 BOM 下使用。無 `[ASSUMED]`／`[SUS]` 判定，不需人工閘門 |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this plan produces

**Java 符號**
- 類別 `dowob.xyz.stockwebv2.trading.TradingTestApplication`（test scope）
- `TradingController.createTrade` 的新參數 `@RequestHeader(name = "Idempotency-Key") String idempotencyKey`
- `GlobalExceptionHandler` 新方法：`MissingRequestHeaderException` 的 `@ExceptionHandler`

**HTTP 契約**
- `POST /api/v1/trades` 的必填請求 header `Idempotency-Key`
- 缺 header 的回應：HTTP 400、`error.code = VALIDATION_FAILED`、`error.fields["Idempotency-Key"]` 非空

**建置設定**
- `stock-module-trading/pom.xml` 兩個 test-scope 依賴：`spring-boot-starter-webmvc-test`、`spring-boot-starter-security-test`
</artifacts_this_phase_produces>

<verification>
- `./mvnw -pl stock-module-trading -am test` 綠
- `./mvnw -pl stock-start -am verify -Dit.test=ErrorHandlingIT` 綠
- `./mvnw test` 綠

**不得宣稱覆蓋**：module 層的 `@WebMvcTest` **不是** envelope 的證明（`GlobalExceptionHandler` 不在切片內）。完整的「缺 header → 400 envelope」端到端驗收在 04-05 的 `TradingApiIT`。
</verification>

<success_criteria>
- 缺 `Idempotency-Key` 的請求得到 400 而不是建立交易
- 400 回應的 `fields` 指出 header 名稱，前端可與 body 欄位錯誤區分
- controller 零業務判斷、零 key 洩漏到 audit
- `@PreAuthorize` 與認證語意零改動
- `handleValidation` 零改動（PR #15 衝突面最小化）
</success_criteria>

<output>
完成後建立 `.planning/phases/04-manual-trade-creation-idempotency-post-trade-refetch/04-04-SUMMARY.md`。

SUMMARY 必須記錄：
1. `TradingControllerTest` 的遷移範圍（D-05 預估的「遷移成本只有更新 TradingControllerTest」是否成立）。
2. `@WebMvcTest` 切片的**能力界線**：它證明了什麼、沒證明什麼（envelope 在 IT）。
3. 新 handler 插在 `handleValidation` 之前是為了避開 PR #15 的 hunk（DP-3）。
</output>
